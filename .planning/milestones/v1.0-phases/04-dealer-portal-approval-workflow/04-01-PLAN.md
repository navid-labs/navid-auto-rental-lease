---
phase: 04-dealer-portal-approval-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/features/vehicles/types/index.ts
  - src/features/vehicles/utils/approval-machine.ts
  - src/features/vehicles/actions/approve-vehicle.ts
  - src/features/vehicles/actions/approve-vehicle.test.ts
  - src/features/vehicles/actions/create-vehicle.ts
  - src/features/vehicles/actions/create-vehicle.test.ts
  - src/features/vehicles/actions/update-vehicle.ts
  - src/features/vehicles/actions/update-vehicle.test.ts
  - src/features/vehicles/actions/resubmit-vehicle.ts
  - src/features/vehicles/components/approval-badge.tsx
  - src/components/ui/tooltip.tsx
  - src/components/ui/checkbox.tsx
autonomous: true
requirements: [VEHI-07]

must_haves:
  truths:
    - "Dealer-created vehicles start with PENDING approval status"
    - "Admin-created vehicles are auto-approved on creation"
    - "Admin can approve a PENDING vehicle and it becomes APPROVED"
    - "Admin can reject a PENDING vehicle with a mandatory reason"
    - "Admin can batch-approve multiple PENDING vehicles in one action"
    - "Dealer editing an APPROVED vehicle resets approval to PENDING"
    - "Dealer can resubmit a REJECTED vehicle for re-approval"
    - "Admin can revoke approval on an APPROVED vehicle"
    - "All approval status changes are logged in VehicleApprovalLog"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "ApprovalStatus enum, approval fields on Vehicle, VehicleApprovalLog model"
      contains: "enum ApprovalStatus"
    - path: "src/features/vehicles/actions/approve-vehicle.ts"
      provides: "approveVehicle, batchApproveVehicles server actions"
      exports: ["approveVehicle", "batchApproveVehicles"]
    - path: "src/features/vehicles/actions/resubmit-vehicle.ts"
      provides: "resubmitVehicle server action"
      exports: ["resubmitVehicle"]
    - path: "src/features/vehicles/components/approval-badge.tsx"
      provides: "ApprovalBadge component with tooltip for rejection reason"
      exports: ["ApprovalBadge"]
    - path: "src/features/vehicles/utils/approval-machine.ts"
      provides: "Approval status transition validation"
      exports: ["canTransitionApproval", "APPROVAL_LABELS", "REJECTION_PRESETS"]
  key_links:
    - from: "src/features/vehicles/actions/approve-vehicle.ts"
      to: "prisma.vehicle + prisma.vehicleApprovalLog"
      via: "$transaction for atomic approve/reject + audit log"
      pattern: "prisma\\.\\$transaction"
    - from: "src/features/vehicles/actions/create-vehicle.ts"
      to: "Vehicle.approvalStatus"
      via: "Role-based initial approval status"
      pattern: "approvalStatus.*ADMIN.*APPROVED.*PENDING"
    - from: "src/features/vehicles/actions/update-vehicle.ts"
      to: "Vehicle.approvalStatus"
      via: "Reset to PENDING on dealer edit of approved vehicle"
      pattern: "shouldResetApproval"
---

<objective>
Extend the Vehicle model with ApprovalStatus lifecycle and create all approval-related server actions, validation, and components.

Purpose: Establish the data layer and business logic for the dealer vehicle approval workflow before building the UI pages that consume it.
Output: Prisma migration with ApprovalStatus enum, approval server actions (approve/reject/batch/revoke/resubmit), modified create/update actions with approval logic, ApprovalBadge component, shadcn tooltip + checkbox components.
</objective>

<execution_context>
@/Users/kiyeol/.claude-work/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dealer-portal-approval-workflow/04-CONTEXT.md
@.planning/phases/04-dealer-portal-approval-workflow/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/features/vehicles/types/index.ts:
```typescript
export type VehicleWithDetails = Vehicle & {
  trim: Trim & {
    generation: Generation & {
      carModel: CarModel & { brand: Brand }
    }
  }
  images: VehicleImage[]
  dealer: { id: string; name: string; email: string; phone: string | null }
}
```

From src/features/vehicles/actions/create-vehicle.ts:
```typescript
export async function createVehicle(data: VehicleFormData): Promise<{ success: true; vehicleId: string } | { error: string }>
```

From src/features/vehicles/actions/update-vehicle.ts:
```typescript
export async function updateVehicle(vehicleId: string, data: Partial<VehicleFormData>): Promise<{ success: true } | { error: string }>
```

From src/features/vehicles/components/status-badge.tsx:
```typescript
// Uses CVA (class-variance-authority) for variant styling
// Pattern to follow for ApprovalBadge
```

From prisma/schema.prisma (Vehicle model):
```prisma
model Vehicle {
  id             String        @id @default(uuid()) @db.Uuid
  trimId         String        @map("trim_id") @db.Uuid
  dealerId       String        @map("dealer_id") @db.Uuid
  year           Int
  mileage        Int
  color          String
  licensePlate   String?       @map("license_plate")
  price          Int
  monthlyRental  Int?          @map("monthly_rental")
  monthlyLease   Int?          @map("monthly_lease")
  status         VehicleStatus @default(AVAILABLE)
  description    String?
  createdAt      DateTime      @default(now()) @map("created_at")
  updatedAt      DateTime      @updatedAt @map("updated_at")
  // Relations...
  @@map("vehicles")
}
```

From src/lib/auth/helpers.ts:
```typescript
export async function getCurrentUser(): Promise<UserProfile | null>
// UserProfile has: id, email, role, name, phone, avatarUrl
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema migration, approval machine, and approval server actions with tests</name>
  <files>
    prisma/schema.prisma,
    src/features/vehicles/utils/approval-machine.ts,
    src/features/vehicles/actions/approve-vehicle.ts,
    src/features/vehicles/actions/approve-vehicle.test.ts,
    src/features/vehicles/actions/resubmit-vehicle.ts,
    src/features/vehicles/types/index.ts
  </files>
  <behavior>
    - approveVehicle('APPROVED') sets vehicle.approvalStatus to APPROVED, sets approvedBy and approvedAt
    - approveVehicle('REJECTED') requires non-empty reason string, sets rejectionReason
    - approveVehicle rejects if user is not ADMIN
    - approveVehicle creates VehicleApprovalLog entry with fromStatus, toStatus, reason, changedBy
    - batchApproveVehicles approves multiple PENDING vehicles atomically, creates log entries for each
    - batchApproveVehicles skips non-PENDING vehicles (only approves PENDING ones)
    - resubmitVehicle sets REJECTED vehicle back to PENDING, clears rejectionReason
    - resubmitVehicle rejects if user is not DEALER or not vehicle owner
  </behavior>
  <action>
    1. Update prisma/schema.prisma:
       - Add `enum ApprovalStatus { PENDING APPROVED REJECTED }` below VehicleStatus enum
       - Add to Vehicle model: `approvalStatus ApprovalStatus @default(PENDING) @map("approval_status")`, `rejectionReason String? @map("rejection_reason")`, `approvedBy String? @map("approved_by") @db.Uuid`, `approvedAt DateTime? @map("approved_at")`
       - Add relation: `approver Profile? @relation("VehicleApprovers", fields: [approvedBy], references: [id])`
       - Add `approvalLogs VehicleApprovalLog[]` to Vehicle relations
       - Add corresponding `approvedVehicles Vehicle[] @relation("VehicleApprovers")` and `approvalLogs VehicleApprovalLog[]` to Profile model
       - Create VehicleApprovalLog model per RESEARCH.md pattern (id, vehicleId, fromStatus, toStatus, reason, changedBy, createdAt with relations, @@map("vehicle_approval_logs"))
       - Run `yarn db:generate` (NOT migrate -- using db push for dev)
       - Run `yarn db:push` to apply schema

    2. Create src/features/vehicles/utils/approval-machine.ts:
       - Export `APPROVAL_LABELS: Record<ApprovalStatus, string>` = { PENDING: '승인 대기', APPROVED: '승인됨', REJECTED: '거절됨' }
       - Export `REJECTION_PRESETS = ['사진 품질 불량', '정보 불일치', '가격 비현실적'] as const`
       - Export `canTransitionApproval(from: ApprovalStatus, to: ApprovalStatus, role: UserRole): boolean` -- admin can do any transition; dealer can only go REJECTED -> PENDING (resubmit)

    3. Update src/features/vehicles/types/index.ts:
       - Add ApprovalStatus to VehicleWithDetails type (approvalStatus, rejectionReason, approvedBy, approvedAt fields)

    4. Create approve-vehicle.ts with `approveVehicle(vehicleId, action: 'APPROVED' | 'REJECTED', reason?: string)` and `batchApproveVehicles(vehicleIds: string[])` -- both admin-only, both use $transaction with VehicleApprovalLog creation. Follow exact patterns from RESEARCH.md code examples.

    5. Create resubmit-vehicle.ts with `resubmitVehicle(vehicleId: string)` -- dealer-only, owner-only, resets REJECTED to PENDING, clears rejectionReason, logs in VehicleApprovalLog.

    6. Write tests in approve-vehicle.test.ts following existing test patterns (mock prisma, mock getCurrentUser). Test: approve sets APPROVED, reject requires reason, non-admin rejected, batch approves multiple, resubmit resets to PENDING, resubmit rejected for non-owner.
  </action>
  <verify>
    <automated>cd /Users/kiyeol/development/navid/navid-auto-rental-lease && yarn test src/features/vehicles/actions/approve-vehicle.test.ts --run && yarn type-check</automated>
  </verify>
  <done>ApprovalStatus enum in schema, approval actions with audit logging, all approval tests passing, TypeScript compiles</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Modify create/update actions for approval logic, add ApprovalBadge and shadcn components</name>
  <files>
    src/features/vehicles/actions/create-vehicle.ts,
    src/features/vehicles/actions/create-vehicle.test.ts,
    src/features/vehicles/actions/update-vehicle.ts,
    src/features/vehicles/actions/update-vehicle.test.ts,
    src/features/vehicles/components/approval-badge.tsx,
    src/components/ui/tooltip.tsx,
    src/components/ui/checkbox.tsx
  </files>
  <behavior>
    - createVehicle sets approvalStatus to APPROVED when user.role is ADMIN, sets approvedBy and approvedAt
    - createVehicle sets approvalStatus to PENDING when user.role is DEALER (default, no explicit set needed since DB default is PENDING)
    - updateVehicle resets approvalStatus to PENDING when dealer edits an APPROVED vehicle, clears approvedBy/approvedAt/rejectionReason
    - updateVehicle does NOT reset approval when admin edits a vehicle
    - updateVehicle creates VehicleApprovalLog when approval is reset
    - ApprovalBadge renders correct color and label for each status
    - ApprovalBadge shows tooltip with rejection reason when status is REJECTED
  </behavior>
  <action>
    1. Install shadcn Tooltip and Checkbox components:
       - Run `npx shadcn@latest add tooltip checkbox` (use npx for one-off shadcn CLI, not npm -- this is the standard shadcn install method)
       - If shadcn CLI prompts, select defaults

    2. Modify src/features/vehicles/actions/create-vehicle.ts:
       - In the prisma.vehicle.create call, add: `approvalStatus: user.role === 'ADMIN' ? 'APPROVED' : 'PENDING'`, `approvedBy: user.role === 'ADMIN' ? user.id : null`, `approvedAt: user.role === 'ADMIN' ? new Date() : null`
       - This prevents Pitfall 1 from RESEARCH.md (admin vehicles appearing as PENDING)

    3. Modify src/features/vehicles/actions/update-vehicle.ts:
       - After fetching the existing vehicle, check: `const shouldResetApproval = user.role === 'DEALER' && vehicle.approvalStatus === 'APPROVED'`
       - Need to select `approvalStatus` in the findUnique call
       - In the update data, spread: `...(shouldResetApproval && { approvalStatus: 'PENDING', approvedBy: null, approvedAt: null, rejectionReason: null })`
       - If shouldResetApproval, create VehicleApprovalLog entry in the same transaction (wrap update in $transaction if not already)

    4. Add new tests to create-vehicle.test.ts: "sets APPROVED for admin", "sets PENDING for dealer"
    5. Add new tests to update-vehicle.test.ts: "resets approval to PENDING when dealer edits approved vehicle", "does NOT reset approval when admin edits"

    6. Create src/features/vehicles/components/approval-badge.tsx:
       - Follow StatusBadge pattern with CVA variants
       - Colors: PENDING = yellow-100/yellow-700, APPROVED = green-100/green-700, REJECTED = red-100/red-700
       - When status is REJECTED and rejectionReason exists, wrap badge in shadcn Tooltip showing the reason
       - Export `ApprovalBadge` component accepting `{ status: ApprovalStatus, rejectionReason?: string | null }`
  </action>
  <verify>
    <automated>cd /Users/kiyeol/development/navid/navid-auto-rental-lease && yarn test --run && yarn type-check</automated>
  </verify>
  <done>Admin vehicles auto-approved on creation, dealer edits reset approval, ApprovalBadge renders with tooltip for rejected, all existing + new tests passing</done>
</task>

</tasks>

<verification>
- `yarn test --run` -- all vehicle tests pass (existing 69 + new approval tests)
- `yarn type-check` -- no TypeScript errors
- ApprovalStatus enum exists in Prisma client
- VehicleApprovalLog model exists and can be queried
- create-vehicle sets correct approval status by role
- update-vehicle resets approval for dealer edits of approved vehicles
</verification>

<success_criteria>
- ApprovalStatus enum and fields exist on Vehicle model in database
- VehicleApprovalLog table exists with proper relations
- approveVehicle, batchApproveVehicles, resubmitVehicle actions work with audit logging
- createVehicle auto-approves for admin, defaults to PENDING for dealer
- updateVehicle resets approval when dealer edits approved vehicle
- ApprovalBadge component renders all 3 states with tooltip on rejected
- All tests pass, TypeScript compiles
</success_criteria>

<output>
After completion, create `.planning/phases/04-dealer-portal-approval-workflow/04-01-SUMMARY.md`
</output>
