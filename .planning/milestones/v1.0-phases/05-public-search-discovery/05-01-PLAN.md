---
phase: 05-public-search-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/vehicles/lib/search-params.ts
  - src/features/vehicles/lib/search-query.ts
  - src/features/vehicles/lib/search-query.test.ts
  - src/features/vehicles/lib/search-params.test.ts
  - src/features/vehicles/components/vehicle-card.tsx
  - src/features/vehicles/components/vehicle-grid.tsx
  - src/features/vehicles/components/search-filters.tsx
  - src/features/vehicles/components/search-sort.tsx
  - src/features/vehicles/components/public-vehicle-detail.tsx
  - src/features/vehicles/components/inquiry-form.tsx
  - src/features/vehicles/actions/create-inquiry.ts
  - src/features/vehicles/actions/create-inquiry.test.ts
  - src/app/(public)/vehicles/page.tsx
  - src/app/(public)/vehicles/[id]/page.tsx
  - src/app/(public)/layout.tsx
autonomous: true
requirements:
  - SRCH-01
  - SRCH-02
  - SRCH-03
  - SRCH-05

must_haves:
  truths:
    - "User can search vehicles with brand, model, year, price, mileage filters"
    - "User can sort results by price, year, mileage, or newest"
    - "Vehicle detail page shows photo gallery, specs, and pricing"
    - "Filter and sort state is persisted in URL query params"
    - "User can submit inquiry from vehicle detail page"
  artifacts:
    - path: "src/features/vehicles/lib/search-params.ts"
      provides: "nuqs parser definitions for all filter/sort/page URL params"
      exports: ["searchParamsParsers", "searchParamsCache"]
    - path: "src/features/vehicles/lib/search-query.ts"
      provides: "Prisma where clause and orderBy builders from parsed search params"
      exports: ["buildWhereClause", "buildOrderBy"]
    - path: "src/app/(public)/vehicles/page.tsx"
      provides: "Server Component search results page with filters, sort, grid, pagination"
    - path: "src/app/(public)/vehicles/[id]/page.tsx"
      provides: "Public vehicle detail page with gallery, specs, inquiry form"
    - path: "src/features/vehicles/actions/create-inquiry.ts"
      provides: "Server Action for inquiry form submission"
      exports: ["createInquiry"]
  key_links:
    - from: "src/features/vehicles/components/search-filters.tsx"
      to: "URL query params"
      via: "nuqs useQueryStates with shallow: false"
      pattern: "useQueryStates.*searchParamsParsers"
    - from: "src/app/(public)/vehicles/page.tsx"
      to: "prisma.vehicle.findMany"
      via: "searchParamsCache.parse -> buildWhereClause -> Prisma query"
      pattern: "buildWhereClause|buildOrderBy"
    - from: "src/features/vehicles/components/inquiry-form.tsx"
      to: "src/features/vehicles/actions/create-inquiry.ts"
      via: "Server Action form submission"
      pattern: "createInquiry"
---

<objective>
Build the vehicle search/filter/sort page with URL state persistence, public vehicle detail page with gallery and inquiry form.

Purpose: Enable visitors to find, browse, and examine approved vehicles through multi-criteria search with shareable/bookmarkable URLs, and submit inquiries from detail pages.
Output: Search results page at /vehicles with filters+sort+pagination, detail page at /vehicles/[id] with gallery+specs+inquiry form.
</objective>

<execution_context>
@/Users/kiyeol/.claude-work/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-public-search-discovery/05-CONTEXT.md
@.planning/phases/05-public-search-discovery/05-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/features/vehicles/types/index.ts:
```typescript
export type VehicleWithDetails = Vehicle & {
  trim: Trim & {
    generation: Generation & {
      carModel: CarModel & {
        brand: Brand
      }
    }
  }
  images: VehicleImage[]
  dealer: Pick<Profile, 'id' | 'name' | 'email' | 'phone'>
}
```

From src/lib/utils/format.ts:
```typescript
export function formatKRW(amount: number, options?: { monthly?: boolean }): string
export function formatDistance(km: number, options?: { compact?: boolean }): string
export function formatYearModel(year: number): string
```

From src/features/vehicles/components/vehicle-detail-view.tsx:
```typescript
// 'use client' component with photo gallery (main image + thumbnail row)
// Reuse gallery pattern; public version removes StatusBadge and dealer-only controls
export function VehicleDetailView({ vehicle }: { vehicle: VehicleWithDetails })
```

From src/features/vehicles/components/cascade-select.tsx:
```typescript
// Reuse pattern for brand/model cascade. On search page, only brand + model needed (optional generation).
// Don't require trimId. Use getBrands, getModelsByBrand, getGenerationsByModel server actions.
```

From prisma/schema.prisma:
```prisma
model Vehicle {
  // Key filter fields: year (Int), mileage (Int), price (Int), monthlyRental (Int?), monthlyLease (Int?)
  // approvalStatus ApprovalStatus @default(PENDING) -- filter for APPROVED only
  // status VehicleStatus -- exclude HIDDEN
}

model Inquiry {
  customerId String? // optional, for logged-in users
  vehicleId  String?
  name       String
  phone      String
  email      String?
  message    String
  status     InquiryStatus @default(NEW)
}
```

From src/app/(public)/layout.tsx:
```typescript
// Wraps Header + main + Footer. NuqsAdapter must be added here.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install deps, nuqs parsers, Prisma query builders with tests</name>
  <files>
    src/features/vehicles/lib/search-params.ts,
    src/features/vehicles/lib/search-params.test.ts,
    src/features/vehicles/lib/search-query.ts,
    src/features/vehicles/lib/search-query.test.ts,
    src/features/vehicles/actions/create-inquiry.ts,
    src/features/vehicles/actions/create-inquiry.test.ts,
    src/app/(public)/layout.tsx
  </files>
  <behavior>
    - buildWhereClause with no filters returns { approvalStatus: APPROVED, status: { not: HIDDEN } }
    - buildWhereClause with brand filter returns nested trim.generation.carModel.brandId match
    - buildWhereClause with brand+model returns nested trim.generation.carModel with both brandId and id
    - buildWhereClause with yearMin/yearMax returns year { gte, lte }
    - buildWhereClause with priceMin/priceMax returns monthlyRental { gte, lte }
    - buildWhereClause with mileageMin/mileageMax returns mileage { gte, lte }
    - buildOrderBy('price-asc') returns { monthlyRental: 'asc' }
    - buildOrderBy('newest') returns { approvedAt: 'desc' }
    - buildOrderBy defaults to newest
    - createInquiry with valid data creates inquiry record
    - createInquiry with invalid phone rejects
    - nuqs parsers: searchParamsParsers has keys brand, model, gen, yearMin, yearMax, priceMin, priceMax, mileMin, mileMax, sort, page
  </behavior>
  <action>
    1. Run `yarn add nuqs` and `npx shadcn@latest add slider` to install dependencies.

    2. Add NuqsAdapter to src/app/(public)/layout.tsx: import { NuqsAdapter } from 'nuqs/adapters/next/app' and wrap {children} with it. Keep existing Header/Footer structure.

    3. Create src/features/vehicles/lib/search-params.ts with nuqs parsers:
       - brand: parseAsString.withDefault('')
       - model: parseAsString.withDefault('')
       - gen: parseAsString.withDefault('')
       - yearMin: parseAsInteger
       - yearMax: parseAsInteger
       - priceMin: parseAsInteger
       - priceMax: parseAsInteger
       - mileMin: parseAsInteger
       - mileMax: parseAsInteger
       - sort: parseAsString.withDefault('newest')
       - page: parseAsInteger.withDefault(1)
       - Export searchParamsCache via createSearchParamsCache(searchParamsParsers)
       - Export PAGE_SIZE = 12

    4. Create src/features/vehicles/lib/search-query.ts with buildWhereClause and buildOrderBy functions per the RESEARCH.md code examples. Key: always filter approvalStatus: APPROVED and status: { not: HIDDEN }. Brand/model/generation filter via nested trim relations. Range filters for year (vehicle.year), price (vehicle.monthlyRental), mileage (vehicle.mileage). buildOrderBy maps sort strings to Prisma orderBy objects.

    5. Create src/features/vehicles/actions/create-inquiry.ts as a Server Action with Zod validation: name (min 2 chars), phone (Korean mobile regex /^01[016789]-?\d{3,4}-?\d{4}$/), message (10-500 chars), vehicleId (uuid). Uses prisma.inquiry.create with status 'NEW'.

    6. Write tests FIRST (RED), then implement (GREEN):
       - search-query.test.ts: test buildWhereClause with each filter combination and buildOrderBy for each sort option
       - search-params.test.ts: verify parser keys exist and default values
       - create-inquiry.test.ts: mock prisma, test valid/invalid input
  </action>
  <verify>
    <automated>cd /Users/kiyeol/development/navid/navid-auto-rental-lease && yarn test src/features/vehicles/lib/search-query.test.ts src/features/vehicles/lib/search-params.test.ts src/features/vehicles/actions/create-inquiry.test.ts --run</automated>
  </verify>
  <done>All unit tests pass for search query builders, URL param parsers, and inquiry action. nuqs and slider installed. NuqsAdapter in public layout.</done>
</task>

<task type="auto">
  <name>Task 2: Search page with filters, sort, grid, pagination + detail page with inquiry</name>
  <files>
    src/features/vehicles/components/vehicle-card.tsx,
    src/features/vehicles/components/vehicle-grid.tsx,
    src/features/vehicles/components/search-filters.tsx,
    src/features/vehicles/components/search-sort.tsx,
    src/features/vehicles/components/public-vehicle-detail.tsx,
    src/features/vehicles/components/inquiry-form.tsx,
    src/app/(public)/vehicles/page.tsx,
    src/app/(public)/vehicles/[id]/page.tsx
  </files>
  <action>
    1. Create VehicleCard component (src/features/vehicles/components/vehicle-card.tsx):
       - Link wrapping Card, 16:9 aspect ratio image with Next/Image lazy loading
       - Placeholder: ImageIcon from lucide-react on gray bg when no images
       - Shows: brand+model name, year+mileage (compact format), monthly price (formatKRW with monthly: true) or vehicle price
       - Use existing Card, CardContent from shadcn
       - sizes prop: "(max-width: 640px) 100vw, (max-width: 1024px) 50vw, 33vw"

    2. Create VehicleGrid component (src/features/vehicles/components/vehicle-grid.tsx):
       - CSS grid: grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6
       - Accepts vehicles array, maps to VehicleCard
       - Empty state: SearchX icon + "검색 결과가 없습니다" + filter reset link

    3. Create SearchFilters component (src/features/vehicles/components/search-filters.tsx):
       - 'use client' component using useQueryStates(searchParamsParsers, { shallow: false })
       - Brand select: fetches brands via getBrands server action on mount, shows select dropdown
       - Model select: fetches models via getModelsByBrand when brand selected, shows select dropdown
       - Generation select (optional): fetches via getGenerationsByModel when model selected
       - Price range: dual-handle shadcn Slider (range mode with array value [min, max])
         Range 0-2,000,000 won, step 50,000. Display formatted values above slider.
       - Year range: dual-handle Slider, range currentYear-15 to currentYear+1, step 1
       - Mileage range: dual-handle Slider, range 0-200,000 km, step 10,000
       - "초기화" reset button to clear all filters
       - Desktop: visible as left sidebar (hidden lg:block, w-72)
       - Mobile: wrap in Sheet component triggered by filter button, show sheet from bottom

    4. Create SearchSort component (src/features/vehicles/components/search-sort.tsx):
       - 'use client' component using useQueryState for sort param
       - Select dropdown with options: 최신 등록순 (newest), 가격 낮은순 (price-asc), 가격 높은순 (price-desc), 연식 최신순 (year-desc), 연식 오래된순 (year-asc), 주행거리 짧은순 (mileage-asc)
       - Shows total result count (passed as prop)

    5. Create search results page (src/app/(public)/vehicles/page.tsx):
       - Server Component with export const dynamic = 'force-dynamic'
       - Parse searchParams via searchParamsCache.parse(await searchParams)
       - Build where/orderBy using buildWhereClause/buildOrderBy
       - Parallel Promise.all for [vehicles, totalCount] queries
       - Include vehicleWithDetails relations (trim.generation.carModel.brand, images, dealer)
       - Pagination: skip/take based on page param
       - Wrap filter/sort client components in Suspense
       - Layout: flex with sidebar + main content area
       - Mobile: filter button above grid that opens Sheet

    6. Create PublicVehicleDetail component (src/features/vehicles/components/public-vehicle-detail.tsx):
       - Reuse gallery pattern from VehicleDetailView (main image + thumbnail row)
       - Remove StatusBadge and dealer-only controls
       - Show: title (brand model generation trim), year/mileage/color subtitle
       - Price section: monthly rental/lease prominently, vehicle price secondary
       - Specs grid: 2-column key-value (brand, model, year, mileage, color, fuel type, transmission, engine)
       - Description section if available
       - CTA section with "상담 신청" button that opens inquiry form dialog

    7. Create InquiryForm component (src/features/vehicles/components/inquiry-form.tsx):
       - 'use client' with React Hook Form + Zod validation
       - Fields: name (text), phone (tel), message (textarea)
       - Hidden vehicleId field
       - Submit calls createInquiry server action
       - Success state: shows "상담 신청이 접수되었습니다" message and closes/resets form
       - Error state: shows validation errors inline

    8. Create detail page (src/app/(public)/vehicles/[id]/page.tsx):
       - Server Component, force-dynamic
       - Fetch vehicle by ID with all relations, only if APPROVED
       - 404 (notFound()) if not found or not approved
       - Render PublicVehicleDetail + InquiryForm
       - Generate metadata (title, description) for SEO

    9. Add simple Pagination component if not already existing:
       - Shows page numbers, prev/next buttons
       - Uses nuqs page param to navigate
  </action>
  <verify>
    <automated>cd /Users/kiyeol/development/navid/navid-auto-rental-lease && yarn type-check && yarn lint && yarn test --run</automated>
  </verify>
  <done>
    - /vehicles page renders search results with 3-column grid, left sidebar filters, sort dropdown, and pagination
    - Filters (brand, model, price, year, mileage) update URL params and trigger server re-render
    - Mobile shows filter button opening bottom sheet
    - /vehicles/[id] page shows gallery, specs, pricing, and working inquiry form
    - Only APPROVED vehicles are shown
    - All filter/sort state persisted in URL
  </done>
</task>

</tasks>

<verification>
1. `yarn test --run` -- all unit tests pass (search query, params, inquiry)
2. `yarn type-check` -- no TypeScript errors
3. `yarn lint` -- no ESLint errors
4. Visit /vehicles -- search page renders with filters and grid
5. Apply filters -- URL updates, results change
6. Visit /vehicles/[id] -- detail page with gallery and inquiry form
7. Submit inquiry -- creates record in DB
</verification>

<success_criteria>
- Search page displays APPROVED vehicles in 3-col/2-col/1-col responsive grid
- All 5 filter types (brand, model, year, price, mileage) work and update URL
- Sort by 6 options works
- Pagination works with page param in URL
- Vehicle detail page shows gallery, full specs, pricing
- Inquiry form validates and creates Inquiry record
- URL is shareable/bookmarkable with all filter state
</success_criteria>

<output>
After completion, create `.planning/phases/05-public-search-discovery/05-01-SUMMARY.md`
</output>
