---
phase: 06-pricing-calculation
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/features/pricing/components/pricing-calculator.tsx
  - src/features/pricing/components/comparison-columns.tsx
  - src/features/pricing/components/period-slider.tsx
  - src/features/pricing/components/deposit-slider.tsx
  - src/app/(public)/calculator/page.tsx
  - src/features/vehicles/components/public-vehicle-detail.tsx
  - src/features/vehicles/components/vehicle-card.tsx
  - src/app/(public)/vehicles/[id]/page.tsx
  - src/components/layout/header.tsx
autonomous: false
requirements: [PRIC-01, PRIC-02, SRCH-04]

must_haves:
  truths:
    - "User can calculate monthly rental and lease payments by adjusting period and deposit sliders"
    - "Rental vs lease side-by-side comparison updates simultaneously when sliders change"
    - "Standalone calculator at /calculator works without login"
    - "Vehicle detail page shows calculator section below specs, above inquiry CTA"
    - "Vehicle cards in search results show monthly rental estimate text"
    - "Calculator pre-fills vehicle when navigated from detail page"
  artifacts:
    - path: "src/features/pricing/components/pricing-calculator.tsx"
      provides: "Shared calculator client component with period/deposit sliders and comparison"
      min_lines: 80
    - path: "src/features/pricing/components/comparison-columns.tsx"
      provides: "Side-by-side rental vs lease display columns"
      min_lines: 40
    - path: "src/app/(public)/calculator/page.tsx"
      provides: "Standalone calculator page"
      min_lines: 20
  key_links:
    - from: "src/features/pricing/components/pricing-calculator.tsx"
      to: "src/lib/finance/calculate.ts"
      via: "import calculateRental, calculateLease"
      pattern: "import.*calculate(Rental|Lease).*from.*finance/calculate"
    - from: "src/features/vehicles/components/public-vehicle-detail.tsx"
      to: "src/features/pricing/components/pricing-calculator.tsx"
      via: "embedded PricingCalculator component"
      pattern: "PricingCalculator"
    - from: "src/features/vehicles/components/vehicle-card.tsx"
      to: "src/lib/finance/calculate.ts"
      via: "import estimateMonthlyRental, formatEstimate"
      pattern: "estimateMonthlyRental|formatEstimate"
---

<objective>
Build the interactive pricing calculator UI and integrate it into the vehicle detail page and vehicle cards.

Purpose: Users can understand and compare rental vs lease costs with transparent calculations. The calculator appears in two places: standalone `/calculator` page and embedded in vehicle detail. Vehicle cards gain a monthly estimate line for quick browsing context.

Output: PricingCalculator component, standalone calculator page, updated vehicle detail page with calculator section, updated vehicle cards with monthly estimate.
</objective>

<execution_context>
@/Users/kiyeol/.claude-work/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-pricing-calculation/06-CONTEXT.md
@.planning/phases/06-pricing-calculation/06-01-SUMMARY.md

@src/features/vehicles/components/public-vehicle-detail.tsx
@src/features/vehicles/components/vehicle-card.tsx
@src/app/(public)/vehicles/[id]/page.tsx
@src/components/ui/slider.tsx
@src/components/ui/card.tsx
@src/lib/utils/format.ts

<interfaces>
<!-- From Plan 06-01 (will exist after wave 1) -->

From src/lib/finance/calculate.ts:
```typescript
export type RentalResult = {
  monthlyPayment: number
  deposit: number
  totalCost: number
  periodMonths: number
}

export type LeaseResult = {
  monthlyPayment: number
  deposit: number
  residualValue: number
  residualRate: number
  totalCost: number
  periodMonths: number
}

export function calculateRental(vehiclePrice: number, periodMonths: number, deposit: number): RentalResult
export function calculateLease(vehiclePrice: number, periodMonths: number, deposit: number, residualRate: number): LeaseResult
export function estimateMonthlyRental(vehiclePrice: number, defaultPeriod?: number): number
export function formatEstimate(monthlyAmount: number): string
```

From src/features/pricing/actions/residual-rate.ts:
```typescript
export async function getResidualRate(brandId: string, carModelId: string, year: number): Promise<number | null>
```

From src/features/vehicles/types/index.ts:
```typescript
export type VehicleWithDetails = Vehicle & {
  trim: Trim & {
    generation: Generation & {
      carModel: CarModel & { brand: Brand }
    }
  }
  images: VehicleImage[]
  dealer?: { id: string; name: string; email: string; phone: string | null }
}
```

From src/components/ui/slider.tsx:
```typescript
// base-ui Slider component
// onValueChange receives number | number[] -- use Array.isArray() guard
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build PricingCalculator component with sliders and comparison columns</name>
  <files>
    src/features/pricing/components/pricing-calculator.tsx
    src/features/pricing/components/comparison-columns.tsx
    src/features/pricing/components/period-slider.tsx
    src/features/pricing/components/deposit-slider.tsx
  </files>
  <action>
    Create client components for the interactive pricing calculator.

    **PricingCalculator (pricing-calculator.tsx):**
    - `'use client'` component
    - Props: `{ vehicle?: { id, price, brandName, modelName, year }, residualRate?: number | null }`
    - When `vehicle` is provided (detail page): show vehicle info header, skip vehicle selection
    - When `vehicle` is absent (standalone page): show a message to select vehicle or allow direct price input
    - State: `periodMonths` (default 36), `deposit` (default 0)
    - Real-time calculation: recalculate on every slider change (no submit button)
    - Call `calculateRental(price, period, deposit)` and `calculateLease(price, period, deposit, residualRate ?? 0.40)` on each change
    - Pass results to `ComparisonColumns`
    - When residualRate is null, use DEFAULT_RESIDUAL_RATE = 0.40 and show a note "(기본 잔존가치율 적용)"
    - Guard: if vehicle price <= 0, show empty state

    **ComparisonColumns (comparison-columns.tsx):**
    - Side-by-side layout: rental column left, lease column right
    - Each column shows: column header (렌탈/리스), monthly payment (large, bold), deposit, total cost
    - Lease column additionally shows: residual value, residual rate percentage
    - Use `formatKRW` for all money values
    - Highlight which option has lower monthly payment with subtle indicator
    - Mobile: stack vertically (flex-col on mobile, flex-row on md+)
    - Include brief educational note at bottom explaining rental vs lease difference

    **PeriodSlider (period-slider.tsx):**
    - Use existing Slider component from `@/components/ui/slider`
    - Discrete steps: 12, 24, 36, 48, 60 months (use step prop or snap to nearest)
    - Show markers/labels below slider for each period option
    - Display current value: "{N}개월"
    - Use Array.isArray() guard on onValueChange per project convention

    **DepositSlider (deposit-slider.tsx):**
    - Use existing Slider component
    - Range: 0 to min(vehiclePrice * 0.5, 10_000_000) in 100만원 steps (step=1_000_000)
    - Display current value in 만원: "보증금 {N}만원"
    - Use `formatKRW` for display
    - Use Array.isArray() guard on onValueChange

    **Styling:**
    - Use Card component for the calculator container
    - Glassmorphism accents matching project design (bg-white/80, backdrop-blur)
    - Accent color for highlighted monthly amounts
    - Responsive: single column mobile, side-by-side desktop
  </action>
  <verify>
    <automated>yarn type-check</automated>
  </verify>
  <done>PricingCalculator renders with working sliders and real-time comparison columns showing rental vs lease side by side</done>
</task>

<task type="auto">
  <name>Task 2: Standalone calculator page, vehicle detail integration, vehicle card estimate</name>
  <files>
    src/app/(public)/calculator/page.tsx
    src/features/vehicles/components/public-vehicle-detail.tsx
    src/features/vehicles/components/vehicle-card.tsx
    src/app/(public)/vehicles/[id]/page.tsx
    src/components/layout/header.tsx
  </files>
  <action>
    **Standalone calculator page (src/app/(public)/calculator/page.tsx):**
    - Server Component with `force-dynamic`
    - Page title: "렌탈/리스 계산기 | Navid Auto"
    - Layout: centered max-w-4xl container with heading and description
    - Allow user to input vehicle price directly via number input field (formatted with 만원 units)
    - Optionally: add vehicle selection via simple search (brand/model cascade from existing CascadeSelect pattern) to auto-fill price and fetch residual rate
    - Render `<PricingCalculator vehicle={selectedVehicle} residualRate={rate} />`
    - When no vehicle selected, calculator works with manually entered price and default residual rate

    **Vehicle detail page integration (src/app/(public)/vehicles/[id]/page.tsx):**
    - Fetch residual rate for this vehicle's brand + carModel + year using `getResidualRate` server action
    - Pass vehicle data and residualRate to page component

    **Vehicle detail component update (public-vehicle-detail.tsx):**
    - Add new prop: `residualRate?: number | null`
    - Insert `<PricingCalculator>` section between specs grid and CTA (inquiry dialog)
    - Add section heading: "월 납입금 계산기"
    - Pass vehicle info: `{ id: vehicle.id, price: vehicle.price, brandName: brand.name, modelName: model.name, year: vehicle.year }`

    **Vehicle card update (vehicle-card.tsx):**
    - Import `estimateMonthlyRental` and `formatEstimate` from `@/lib/finance/calculate`
    - Below the price line, add: `<p className="mt-1 text-xs text-muted-foreground">{formatEstimate(estimateMonthlyRental(vehicle.price))}</p>`
    - Only show if vehicle.price > 0

    **Header navigation (header.tsx):**
    - Add "계산기" link to the public header nav pointing to `/calculator`
    - Place between existing nav items (after "차량 검색")
    - Use Calculator icon from lucide-react
  </action>
  <verify>
    <automated>yarn type-check && yarn build</automated>
  </verify>
  <done>
    - /calculator page renders with price input and working calculator
    - Vehicle detail page shows calculator section below specs with pre-filled vehicle data
    - Vehicle cards show "렌탈 월 ~N만원부터" text
    - Header has calculator nav link
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Interactive pricing calculator with rental vs lease comparison, vehicle detail integration, and vehicle card monthly estimates</what-built>
  <how-to-verify>
    1. Visit http://localhost:3000/calculator -- verify standalone calculator page loads
    2. Enter a vehicle price (e.g., 3,000만원) -- verify sliders appear and comparison columns show rental vs lease
    3. Adjust period slider (12/24/36/48/60) -- verify both columns update in real time
    4. Adjust deposit slider -- verify monthly payments decrease as deposit increases
    5. Verify lease monthly is lower than rental monthly (residual value advantage)
    6. Visit http://localhost:3000/vehicles -- verify vehicle cards show "렌탈 월 ~N만원부터"
    7. Click a vehicle -- verify detail page has calculator section between specs and 상담신청
    8. Verify calculator is pre-filled with vehicle price and updates with sliders
    9. Check mobile responsiveness: comparison columns should stack vertically
    10. Verify header has "계산기" nav link
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
```bash
yarn type-check
yarn build
yarn test
```
</verification>

<success_criteria>
- Standalone /calculator page accessible without login
- Calculator shows side-by-side rental vs lease with working sliders
- Vehicle detail page has embedded calculator below specs, above CTA
- Vehicle cards display monthly rental estimate
- Mobile responsive layout
- All types compile, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/06-pricing-calculation/06-02-SUMMARY.md`
</output>
