---
phase: 06-pricing-calculation
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/app/admin/residual-value/page.tsx
  - src/features/pricing/components/residual-value-table.tsx
  - src/features/pricing/components/residual-value-form.tsx
  - prisma/seed.ts
autonomous: true
requirements: [PRIC-02]

must_haves:
  truths:
    - "Admin can view residual value rates in a table filtered by brand"
    - "Admin can inline-edit an existing rate"
    - "Admin can add a new rate via form (brand, model, year, rate)"
    - "Admin can delete a rate"
    - "Sample seed data exists for common Korean brands/models"
  artifacts:
    - path: "src/app/admin/residual-value/page.tsx"
      provides: "Admin residual value management page"
      min_lines: 30
    - path: "src/features/pricing/components/residual-value-table.tsx"
      provides: "Table with inline editing for residual value rates"
      min_lines: 60
    - path: "src/features/pricing/components/residual-value-form.tsx"
      provides: "Add new rate form with brand/model/year/rate inputs"
      min_lines: 40
  key_links:
    - from: "src/app/admin/residual-value/page.tsx"
      to: "src/features/pricing/actions/residual-rate.ts"
      via: "import getResidualRates for initial data fetch"
      pattern: "getResidualRates"
    - from: "src/features/pricing/components/residual-value-table.tsx"
      to: "src/features/pricing/actions/residual-rate.ts"
      via: "import upsertResidualRate, deleteResidualRate for mutations"
      pattern: "upsertResidualRate|deleteResidualRate"
---

<objective>
Build the admin residual value rate management page and seed sample data.

Purpose: Admins can configure make/model/year residual value percentages that the public calculator uses for lease calculations. Without this, all lease calculations use the default fallback rate.

Output: Admin page at `/admin/residual-value` with CRUD table, plus seed data for 5-10 common Korean vehicle brands/models.
</objective>

<execution_context>
@/Users/kiyeol/.claude-work/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-pricing-calculation/06-CONTEXT.md
@.planning/phases/06-pricing-calculation/06-01-SUMMARY.md

@src/components/layout/admin-sidebar.tsx
@src/app/admin/vehicles/page.tsx
@prisma/seed.ts

<interfaces>
<!-- From Plan 06-01 -->

From src/features/pricing/actions/residual-rate.ts:
```typescript
export async function getResidualRate(brandId: string, carModelId: string, year: number): Promise<number | null>
export async function getResidualRates(brandId?: string): Promise<ResidualRateRow[]>
export async function upsertResidualRate(data: { brandId, carModelId, year, rate }): Promise<void>
export async function deleteResidualRate(id: string): Promise<void>
```

From src/features/pricing/schemas/residual-value.ts:
```typescript
export const residualValueRateSchema: z.ZodObject<{ brandId, carModelId, year, rate }>
```

<!-- Existing admin patterns -->

From src/components/layout/admin-sidebar.tsx:
```typescript
// Already has: { href: '/admin/residual-value', label: '잔존가치 관리', icon: Calculator }
// DO NOT add duplicate nav item
```

From src/features/vehicles/actions/cascade-data.ts:
```typescript
export async function getBrands(): Promise<{ id, name }[]>
export async function getModelsByBrand(brandId: string): Promise<{ id, name }[]>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin residual value management page with table and form</name>
  <files>
    src/app/admin/residual-value/page.tsx
    src/features/pricing/components/residual-value-table.tsx
    src/features/pricing/components/residual-value-form.tsx
  </files>
  <action>
    **Admin page (src/app/admin/residual-value/page.tsx):**
    - Server Component with `export const dynamic = 'force-dynamic'`
    - Page heading: "잔존가치 관리"
    - Fetch brands via `getBrands()` for the filter dropdown
    - Accept `searchParams` for brand filter: `?brandId=xxx`
    - Fetch rates via `getResidualRates(brandId)` with optional filter
    - Render brand filter dropdown (simple select), ResidualValueTable, and ResidualValueForm
    - NOTE: admin sidebar nav item already exists at `/admin/residual-value` -- do NOT modify admin-sidebar.tsx

    **ResidualValueTable (residual-value-table.tsx):**
    - `'use client'` component
    - Props: `{ rates: ResidualRateRow[] }` where row has id, brandName, modelName, year, rate
    - Table columns: 브랜드, 모델, 연식, 잔존가치율(%), 작업
    - Inline editing: click on rate cell to toggle edit mode (input field), press Enter or blur to save via `upsertResidualRate`
    - Delete button per row: confirm dialog, then call `deleteResidualRate(id)`
    - Rate displayed as percentage (e.g., 45.0%) but stored as decimal (0.45)
    - Show loading states during mutations with `useTransition`
    - Empty state: "등록된 잔존가치율이 없습니다"

    **ResidualValueForm (residual-value-form.tsx):**
    - `'use client'` component for adding new rates
    - Props: `{ brands: { id, name }[] }`
    - Fields: brand select, model select (cascade from brand), year (number input, 2010-2030), rate (number input, 1-99 as percentage)
    - Model select populated on brand change via `getModelsByBrand(brandId)` action
    - Submit calls `upsertResidualRate` with rate converted from percentage to decimal (45 -> 0.45)
    - Success: clear form, data refreshes via revalidatePath
    - Validation with Zod schema from `src/features/pricing/schemas/residual-value.ts`
    - Use react-hook-form with zodResolver following project pattern

    **Styling:**
    - Match existing admin page style (simple table with bordered rows)
    - Use Card component for form container
    - Responsive: table scrolls horizontally on mobile
  </action>
  <verify>
    <automated>yarn type-check && yarn build</automated>
  </verify>
  <done>
    - Admin can navigate to /admin/residual-value from sidebar
    - Table displays rates with brand/model/year/rate columns
    - Admin can inline-edit rate percentages
    - Admin can delete rates
    - Admin can add new rates via form with brand/model cascade
    - Brand filter narrows the table
  </done>
</task>

<task type="auto">
  <name>Task 2: Seed sample residual value rates for demo</name>
  <files>prisma/seed.ts</files>
  <action>
    Add residual value rate seed data to the existing `prisma/seed.ts` file.

    After existing seed logic (brands, models, etc.), add a section that seeds sample ResidualValueRate entries for common Korean vehicles:

    Sample rates (brand/model/year -> rate):
    - Hyundai Sonata 2024: 0.42
    - Hyundai Sonata 2023: 0.38
    - Hyundai Tucson 2024: 0.45
    - Kia K5 2024: 0.40
    - Kia Sportage 2024: 0.44
    - Genesis G80 2024: 0.48
    - Genesis GV70 2024: 0.50
    - BMW 3 Series 2024: 0.46
    - Mercedes-Benz E-Class 2024: 0.47

    Use `prisma.residualValueRate.upsert` with the brandId_carModelId_year unique constraint to be idempotent.

    Look up brand and model IDs from the seed data that creates them earlier in the script. Use `findFirst` or reference the created objects.

    IMPORTANT: Only add if the brands/models exist in the seed data. If they don't exist, skip gracefully.
  </action>
  <verify>
    <automated>yarn type-check</automated>
  </verify>
  <done>Running `yarn db:seed` populates sample residual value rates for demo purposes</done>
</task>

</tasks>

<verification>
```bash
yarn type-check
yarn build
```
</verification>

<success_criteria>
- /admin/residual-value page renders with table and add form
- Inline editing works for rate values
- Brand filter narrows displayed rates
- Seed script includes sample residual value rates
- Admin sidebar correctly links to the page (existing nav item)
</success_criteria>

<output>
After completion, create `.planning/phases/06-pricing-calculation/06-03-SUMMARY.md`
</output>
