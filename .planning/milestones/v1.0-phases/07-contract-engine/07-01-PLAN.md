---
phase: 07-contract-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/features/contracts/types/index.ts
  - src/features/contracts/utils/contract-machine.ts
  - src/features/contracts/utils/contract-machine.test.ts
  - src/features/contracts/utils/mock-ekyc.ts
  - src/features/contracts/utils/mock-ekyc.test.ts
  - src/features/contracts/schemas/contract.ts
  - src/features/contracts/schemas/contract.test.ts
autonomous: true
requirements:
  - CONT-06
  - CONT-02

must_haves:
  truths:
    - "Contract state machine enforces valid transitions (DRAFT -> PENDING_EKYC -> PENDING_APPROVAL -> APPROVED -> ACTIVE -> COMPLETED)"
    - "Admin can force any contract status transition"
    - "Mock eKYC verifies with 6-digit code '123456' and simulates delay"
    - "Zod schemas validate each wizard step's form data"
  artifacts:
    - path: "src/features/contracts/utils/contract-machine.ts"
      provides: "Contract status transition map + canTransitionContract() + getAvailableContractTransitions()"
      exports: ["CONTRACT_STATUS_TRANSITIONS", "CONTRACT_STATUS_LABELS", "canTransitionContract", "getAvailableContractTransitions"]
    - path: "src/features/contracts/utils/mock-ekyc.ts"
      provides: "Mock eKYC provider with pluggable adapter pattern"
      exports: ["mockVerifyIdentity", "EkycResult"]
    - path: "src/features/contracts/schemas/contract.ts"
      provides: "Zod schemas for contract wizard steps"
      exports: ["vehicleConfirmSchema", "termsSchema", "ekycSchema", "reviewSchema"]
    - path: "src/features/contracts/types/index.ts"
      provides: "Contract feature type definitions"
    - path: "prisma/schema.prisma"
      provides: "EkycVerification model with Profile relation"
      contains: "model EkycVerification"
  key_links:
    - from: "src/features/contracts/utils/contract-machine.ts"
      to: "@prisma/client"
      via: "ContractStatus enum import"
      pattern: "import.*ContractStatus.*@prisma/client"
    - from: "src/features/contracts/schemas/contract.ts"
      to: "src/features/contracts/types/index.ts"
      via: "type inference from schemas"
---

<objective>
Build the contract engine foundation: state machine, validation schemas, mock eKYC provider, types, and EkycVerification schema migration.

Purpose: Provide the core business logic and type contracts that Plan 02 (wizard UI) and Plan 03 (admin approval + realtime) depend on. Following the interface-first pattern, this plan defines all contracts before implementation.

Output: Contract state machine, Zod validation schemas, mock eKYC utility, EkycVerification DB model, shared types.
</objective>

<execution_context>
@/Users/kiyeol/.claude-work/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-contract-engine/07-RESEARCH.md

@src/features/vehicles/utils/status-machine.ts
@src/features/vehicles/utils/approval-machine.ts
@src/lib/finance/calculate.ts
@prisma/schema.prisma
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Contract state machine, types, and Zod schemas with tests</name>
  <files>
    src/features/contracts/types/index.ts,
    src/features/contracts/utils/contract-machine.ts,
    src/features/contracts/utils/contract-machine.test.ts,
    src/features/contracts/schemas/contract.ts,
    src/features/contracts/schemas/contract.test.ts
  </files>
  <behavior>
    - canTransitionContract('DRAFT', 'PENDING_EKYC', 'CUSTOMER') returns true
    - canTransitionContract('DRAFT', 'APPROVED', 'CUSTOMER') returns false
    - canTransitionContract('PENDING_APPROVAL', 'APPROVED', 'ADMIN') returns true
    - canTransitionContract('COMPLETED', any, 'CUSTOMER') returns false (terminal)
    - canTransitionContract('CANCELED', any, 'CUSTOMER') returns false (terminal)
    - Admin can force any non-same transition
    - getAvailableContractTransitions('DRAFT', 'CUSTOMER') returns ['PENDING_EKYC', 'CANCELED']
    - CONTRACT_STATUS_LABELS maps all 7 statuses to Korean labels
    - termsSchema validates contractType enum ('RENTAL' | 'LEASE'), periodMonths (12-60), deposit (>= 0)
    - ekycSchema validates name (min 2), phone (Korean mobile regex), carrier enum, birthDate, gender, verificationCode (6 digits)
    - vehicleConfirmSchema validates vehicleId as uuid
  </behavior>
  <action>
    1. Create `src/features/contracts/types/index.ts`:
       - ContractWizardStep type (1|2|3|4)
       - ContractFormData type with all fields across steps
       - Re-export ContractStatus, ContractType from @prisma/client
       - EkycFormData type matching ekycSchema
       - TermsFormData type matching termsSchema
       - ContractWithVehicle type for joined queries (contract + vehicle + vehicle.trim + vehicle.trim.generation + vehicle.trim.generation.carModel + vehicle.trim.generation.carModel.brand + vehicle.images)

    2. Create `src/features/contracts/utils/contract-machine.ts` following `status-machine.ts` pattern exactly:
       - CONTRACT_STATUS_TRANSITIONS record with `to` and `roles` per status
       - DRAFT -> [PENDING_EKYC, CANCELED] by CUSTOMER
       - PENDING_EKYC -> [PENDING_APPROVAL, CANCELED] by CUSTOMER
       - PENDING_APPROVAL -> [APPROVED, CANCELED] by ADMIN
       - APPROVED -> [ACTIVE, CANCELED] by ADMIN
       - ACTIVE -> [COMPLETED] by ADMIN
       - COMPLETED -> [] (terminal)
       - CANCELED -> [] (terminal)
       - canTransitionContract(from, to, role) -- admin force any (same pattern as vehicle)
       - getAvailableContractTransitions(from, role)
       - CONTRACT_STATUS_LABELS Korean labels: 작성 중, 본인인증 대기, 승인 대기, 승인됨, 계약 진행 중, 완료, 취소됨
       - CONTRACT_STATUS_COLORS for badge styling (using existing badge color pattern)

    3. Create `src/features/contracts/schemas/contract.ts`:
       - vehicleConfirmSchema: { vehicleId: z.string().uuid() }
       - termsSchema: { contractType: z.enum(['RENTAL', 'LEASE']), periodMonths: z.coerce.number().min(12).max(60), deposit: z.coerce.number().min(0) }
       - ekycSchema: { name: z.string().min(2), phone: z.string().regex(/^01[016789]\d{7,8}$/), carrier: z.enum(['SKT', 'KT', 'LGU']), birthDate: z.string().regex(/^\d{4}-\d{2}-\d{2}$/), gender: z.enum(['M', 'F']), verificationCode: z.string().length(6) }
       - reviewSchema: z.object({}) (empty, review step has no new input)

    4. Write tests FIRST (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>yarn test src/features/contracts/utils/contract-machine.test.ts src/features/contracts/schemas/contract.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - Contract state machine enforces all valid/invalid transitions with role checks
    - Admin can force any non-same transition
    - All Zod schemas validate correct/incorrect input
    - Types exported for downstream use
    - All tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mock eKYC provider and EkycVerification schema migration</name>
  <files>
    src/features/contracts/utils/mock-ekyc.ts,
    src/features/contracts/utils/mock-ekyc.test.ts,
    prisma/schema.prisma
  </files>
  <behavior>
    - mockVerifyIdentity with code '123456' returns { verified: true, name, phone, ... }
    - mockVerifyIdentity with wrong code throws '인증번호가 일치하지 않습니다.'
    - mockVerifyIdentity simulates async delay (at least 500ms for test, 1500ms in production)
    - EkycResult type has: verified, name, phone, carrier, birthDate, gender, verifiedAt
  </behavior>
  <action>
    1. Create `src/features/contracts/utils/mock-ekyc.ts`:
       - EkycResult type: { verified: boolean, name: string, phone: string, carrier: 'SKT'|'KT'|'LGU', birthDate: string, gender: 'M'|'F', verifiedAt: Date }
       - EkycInput type: { name: string, phone: string, carrier: string, birthDate: string, gender: string, verificationCode: string }
       - mockSendVerificationCode(phone: string): Promise<{ sent: true }> -- simulates SMS send with 500ms delay
       - mockVerifyIdentity(input: EkycInput, expectedCode?: string): Promise<EkycResult> -- default expectedCode '123456', simulates 1500ms delay, throws on mismatch
       - Pattern follows MockPlateProvider from Phase 3 (pluggable adapter for v2 real API)

    2. Update `prisma/schema.prisma`:
       - Add EkycVerification model:
         id (UUID), profileId (UUID FK to Profile), contractType (ContractType), contractId (UUID, nullable -- linked after contract creation), name, phone, carrier, birthDate, gender, verified (boolean default false), verifiedAt (DateTime?), createdAt
       - Add `ekycVerifications EkycVerification[]` relation to Profile model
       - Run `yarn db:push` to apply schema changes (NOT migrate -- consistent with project convention)
       - Run `yarn db:generate` to regenerate Prisma client

    3. Add Realtime publication SQL. Create `prisma/migrations/enable_realtime.sql` (reference file, NOT auto-applied):
       ```sql
       ALTER PUBLICATION supabase_realtime ADD TABLE rental_contracts, lease_contracts;
       ```
       Note in a comment that this must be run manually in Supabase SQL editor or via `yarn db:push` won't cover it.

    4. Write mock-ekyc tests FIRST, then implement.
  </action>
  <verify>
    <automated>yarn test src/features/contracts/utils/mock-ekyc.test.ts --reporter=verbose && yarn db:generate</automated>
  </verify>
  <done>
    - Mock eKYC provider passes all tests (success + failure cases)
    - EkycVerification model exists in schema.prisma with Profile relation
    - Prisma client regenerated with new model
    - Realtime publication SQL documented for manual execution
  </done>
</task>

</tasks>

<verification>
- `yarn test src/features/contracts/ --reporter=verbose` -- all contract tests pass
- `yarn type-check` -- no TypeScript errors
- `yarn db:generate` -- Prisma client generates successfully
</verification>

<success_criteria>
- Contract state machine correctly enforces DRAFT -> PENDING_EKYC -> PENDING_APPROVAL -> APPROVED -> ACTIVE -> COMPLETED transitions
- Admin can force any transition (matching vehicle status machine pattern)
- Mock eKYC simulates Korean PASS-style verification with 6-digit code
- All Zod schemas validate contract wizard step data
- EkycVerification model added to database
- All tests pass, TypeScript compiles
</success_criteria>

<output>
After completion, create `.planning/phases/07-contract-engine/07-01-SUMMARY.md`
</output>
