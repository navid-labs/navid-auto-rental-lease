---
phase: 19-homepage-search-spacing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(public)/vehicles/page.tsx
  - src/features/vehicles/components/vehicle-grid.tsx
  - src/features/vehicles/components/vehicle-card.tsx
  - src/features/vehicles/components/quick-filter-badges.tsx
autonomous: true
requirements: [SRCH-01, SRCH-02, SRCH-03, SRCH-04]

must_haves:
  truths:
    - "Vehicle card grid has 24px gaps between cards"
    - "Search bar to breadcrumb has 24px visible spacing"
    - "Vehicle card text info section has 16px internal padding"
    - "Quick filter pill row has symmetrical 16px top and bottom margin"
  artifacts:
    - path: "src/app/(public)/vehicles/page.tsx"
      provides: "24px top padding on breadcrumb wrapper for search-to-breadcrumb gap"
      contains: "pt-6"
    - path: "src/features/vehicles/components/vehicle-grid.tsx"
      provides: "24px grid gap between vehicle cards"
      contains: "gap-6"
    - path: "src/features/vehicles/components/vehicle-card.tsx"
      provides: "16px internal padding on card info section"
      contains: '"p-4"'
    - path: "src/features/vehicles/components/quick-filter-badges.tsx"
      provides: "Symmetrical 16px vertical margin on filter badge row"
      contains: "my-4"
  key_links:
    - from: "src/app/(public)/vehicles/page.tsx"
      to: "src/features/vehicles/components/vehicle-grid.tsx"
      via: "VehicleListClient renders VehicleGrid with updated gap"
      pattern: "gap-6"
    - from: "src/features/vehicles/components/vehicle-grid.tsx"
      to: "src/features/vehicles/components/vehicle-card.tsx"
      via: "Grid renders VehicleCard with updated internal padding"
      pattern: "p-4"
---

<objective>
Apply spacing improvements to the search results page: increase vehicle card grid gap to 24px, add 24px spacing between search bar and breadcrumb, increase vehicle card internal padding, and add symmetrical margin to quick filter pills.

Purpose: Meet SRCH-01 through SRCH-04 requirements -- search results page cards and layout zones have generous, consistent spacing.
Output: 4 modified files with updated Tailwind spacing classes.
</objective>

<execution_context>
@/Users/kiyeol/.claude-work/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-global-spacing-foundation/18-01-SUMMARY.md
@.planning/phases/19-homepage-search-spacing/19-RESEARCH.md
@.planning/phases/19-homepage-search-spacing/19-UI-SPEC.md

<interfaces>
<!-- Current state of all 4 files with exact lines to change -->

From src/app/(public)/vehicles/page.tsx (current):
- Line 46: `<div className="mx-auto max-w-[1440px] px-4 lg:px-8 xl:px-[120px]">` -- needs `pt-6` added for 24px gap between search bar and breadcrumb
- Line 41: `<div className="min-h-screen bg-background">` -- DO NOT CHANGE
- Line 43: `<VehicleSearchBar />` -- DO NOT CHANGE (search bar itself is not modified)

From src/features/vehicles/components/vehicle-grid.tsx (current):
- Line 44: `<div className="grid grid-cols-1 gap-5 sm:grid-cols-2 lg:grid-cols-3">` -- change `gap-5` to `gap-6`

From src/features/vehicles/components/vehicle-card.tsx (current):
- Line 188: `<div className="p-3.5">` -- change `p-3.5` to `p-4`

From src/features/vehicles/components/quick-filter-badges.tsx (current):
- Line 55: `<div className="mb-4 flex gap-2 overflow-x-auto pb-1 ...">` -- change `mb-4` to `my-4` (REPLACE, do not add both)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Search page spacing adjustments</name>
  <files>
    src/app/(public)/vehicles/page.tsx,
    src/features/vehicles/components/vehicle-grid.tsx,
    src/features/vehicles/components/vehicle-card.tsx,
    src/features/vehicles/components/quick-filter-badges.tsx
  </files>
  <read_first>
    src/app/(public)/vehicles/page.tsx,
    src/features/vehicles/components/vehicle-grid.tsx,
    src/features/vehicles/components/vehicle-card.tsx,
    src/features/vehicles/components/quick-filter-badges.tsx
  </read_first>
  <action>
Apply all search page spacing changes across 4 files. Each change is a single Tailwind class substitution.

**File 1: `src/app/(public)/vehicles/page.tsx`**

1. Add `pt-6` to the breadcrumb wrapper div for 24px spacing between search bar and breadcrumb. On line 46, change:
   ```
   <div className="mx-auto max-w-[1440px] px-4 lg:px-8 xl:px-[120px]">
   ```
   To:
   ```
   <div className="mx-auto max-w-[1440px] px-4 pt-6 lg:px-8 xl:px-[120px]">
   ```

CRITICAL: Do NOT add `-mt-6` to the search page. The search page intentionally uses the layout's 24px `pt-6` top padding. Do NOT change the sidebar width (280px) or the sidebar gap (`gap-8`).

**File 2: `src/features/vehicles/components/vehicle-grid.tsx`**

2. Line 44 -- Grid gap: Change `gap-5` to `gap-6`
   ```
   <div className="grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-3">
   ```

**File 3: `src/features/vehicles/components/vehicle-card.tsx`**

3. Line 188 -- Info section padding: Change `p-3.5` to `p-4`
   ```
   <div className="p-4">
   ```

**File 4: `src/features/vehicles/components/quick-filter-badges.tsx`**

4. Line 55 -- Pill area margin: REPLACE `mb-4` with `my-4` (do NOT keep both). Change:
   ```
   <div className="mb-4 flex gap-2 overflow-x-auto pb-1 [-ms-overflow-style:none] [scrollbar-width:none] [&::-webkit-scrollbar]:hidden">
   ```
   To:
   ```
   <div className="my-4 flex gap-2 overflow-x-auto pb-1 [-ms-overflow-style:none] [scrollbar-width:none] [&::-webkit-scrollbar]:hidden">
   ```

**Anti-patterns to avoid:**
- Do NOT add both `mb-4` and `my-4` to QuickFilterBadges. Replace `mb-4` entirely with `my-4`. In Tailwind v4, having both would be ambiguous for margin-bottom.
- Do NOT change the vehicle card image container, aspect ratio, or image sizing. Only the `p-3.5` on the info text section changes to `p-4`.
  </action>
  <verify>
    <automated>cd /Users/kiyeol/development/navid/navid-auto-rental-lease && yarn type-check</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(public)/vehicles/page.tsx` breadcrumb wrapper div contains `pt-6` in its className
    - `src/app/(public)/vehicles/page.tsx` does NOT contain `-mt-6` anywhere (search page uses layout padding)
    - `src/features/vehicles/components/vehicle-grid.tsx` contains `gap-6` (not `gap-5`)
    - `src/features/vehicles/components/vehicle-card.tsx` info section contains `"p-4"` (not `p-3.5`)
    - `src/features/vehicles/components/quick-filter-badges.tsx` contains `my-4` (not `mb-4`)
    - `src/features/vehicles/components/quick-filter-badges.tsx` does NOT contain `mb-4` anywhere
    - `yarn type-check` exits with code 0
  </acceptance_criteria>
  <done>All 4 search page files have updated spacing classes. Vehicle grid has 24px gaps, search-to-breadcrumb has 24px transition, vehicle cards have 16px info padding, and quick filter pills have symmetrical vertical margins.</done>
</task>

</tasks>

<verification>
- `yarn type-check` passes (TypeScript integrity)
- `yarn test --run` passes (no regression in existing tests)
- grep confirms: no `gap-5` remains in `vehicle-grid.tsx`
- grep confirms: no `p-3.5` remains in `vehicle-card.tsx` info section
- grep confirms: no `mb-4` remains in `quick-filter-badges.tsx` (replaced by `my-4`)
- grep confirms: `pt-6` present in breadcrumb wrapper in `vehicles/page.tsx`
</verification>

<success_criteria>
- SRCH-01: Vehicle grid uses `gap-6` (24px between cards, up from 20px).
- SRCH-02: Breadcrumb wrapper has `pt-6` (24px gap between search bar bottom and breadcrumb top).
- SRCH-03: Vehicle card info section uses `p-4` (16px padding, up from 14px).
- SRCH-04: Quick filter badge row uses `my-4` (16px top + 16px bottom symmetrical margin).
</success_criteria>

<output>
After completion, create `.planning/phases/19-homepage-search-spacing/19-02-SUMMARY.md`
</output>
