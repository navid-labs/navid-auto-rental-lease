---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/app/(public)/layout.tsx
  - src/app/(public)/page.tsx
  - src/app/admin/layout.tsx
  - src/app/admin/dashboard/page.tsx
  - src/app/dealer/layout.tsx
  - src/app/dealer/dashboard/page.tsx
  - src/components/layout/header.tsx
  - src/components/layout/footer.tsx
  - src/components/layout/mobile-nav.tsx
  - src/components/layout/admin-sidebar.tsx
  - src/components/layout/dealer-sidebar.tsx
autonomous: false
requirements:
  - UIEX-01

must_haves:
  truths:
    - "Public layout renders header + footer on desktop and mobile viewports"
    - "Mobile navigation opens a slide-out sheet on small screens"
    - "Header navigation collapses to hamburger menu below md breakpoint"
    - "Admin layout has a sidebar navigation with dark navy background"
    - "Dealer layout has a sidebar navigation"
    - "All three route groups render independently without interfering"
    - "Footer shows minimal content: company name, copyright, contact info"
  artifacts:
    - path: "src/app/(public)/layout.tsx"
      provides: "Public page layout with header and footer"
      min_lines: 15
    - path: "src/app/admin/layout.tsx"
      provides: "Admin dashboard layout with sidebar"
      min_lines: 15
    - path: "src/app/dealer/layout.tsx"
      provides: "Dealer portal layout with sidebar"
      min_lines: 15
    - path: "src/components/layout/header.tsx"
      provides: "Responsive header with navigation and mobile hamburger"
      min_lines: 30
    - path: "src/components/layout/mobile-nav.tsx"
      provides: "Mobile slide-out navigation using shadcn Sheet"
      min_lines: 20
    - path: "src/components/layout/footer.tsx"
      provides: "Minimal footer with company info"
      min_lines: 10
    - path: "src/components/layout/admin-sidebar.tsx"
      provides: "Admin sidebar navigation"
      min_lines: 20
    - path: "src/components/layout/dealer-sidebar.tsx"
      provides: "Dealer sidebar navigation"
      min_lines: 20
  key_links:
    - from: "src/app/(public)/layout.tsx"
      to: "src/components/layout/header.tsx"
      via: "import and render"
      pattern: "import.*Header"
    - from: "src/components/layout/header.tsx"
      to: "src/components/layout/mobile-nav.tsx"
      via: "import MobileNav for small screens"
      pattern: "import.*MobileNav"
    - from: "src/app/admin/layout.tsx"
      to: "src/components/layout/admin-sidebar.tsx"
      via: "import and render"
      pattern: "import.*AdminSidebar"
---

<objective>
Build the responsive layout shell with three route groups (public, dealer, admin), each with appropriate navigation patterns.

Purpose: UIEX-01 requires responsive web design. This plan creates the visual skeleton that all subsequent feature pages will render within.
Output: Three fully responsive route group layouts with header/footer (public) and sidebar (dealer/admin) navigation.
</objective>

<execution_context>
@/Users/kiyeol/.claude-work/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs that this plan depends on -->

From src/components/ui/button.tsx (shadcn):
```typescript
export { Button, buttonVariants } from './button'
```

From src/components/ui/sheet.tsx (shadcn):
```typescript
export { Sheet, SheetContent, SheetTrigger, SheetHeader, SheetTitle } from './sheet'
```

From src/components/ui/navigation-menu.tsx (shadcn):
```typescript
export { NavigationMenu, NavigationMenuList, NavigationMenuItem, NavigationMenuLink } from './navigation-menu'
```

From src/components/ui/separator.tsx (shadcn):
```typescript
export { Separator } from './separator'
```

globals.css provides these CSS custom properties:
- --primary (dark navy), --primary-foreground (gold)
- --accent (gold), --accent-foreground (dark navy)
- --sidebar-background, --sidebar-foreground, --sidebar-accent
- --font-sans: "Pretendard", system-ui, sans-serif
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create public layout with responsive header, mobile navigation, and footer</name>
  <files>
    src/app/(public)/layout.tsx, src/app/(public)/page.tsx,
    src/components/layout/header.tsx, src/components/layout/footer.tsx,
    src/components/layout/mobile-nav.tsx
  </files>
  <action>
    **Header (`src/components/layout/header.tsx`):**
    - Server component wrapper, client interactive parts extracted
    - Desktop (md+): Horizontal nav bar with logo "Navid Auto" on left, nav links (차량검색, 렌탈/리스, 문의하기) center/right
    - Logo uses primary color (dark navy), accent gold for hover states
    - Background: white with subtle bottom border, consider subtle glassmorphism (backdrop-blur-md + bg-white/80) per CONTEXT.md design philosophy -- point element usage for header
    - Mobile (below md): Logo left, hamburger menu button right (using shadcn Button ghost variant)
    - Use `'use client'` only on the mobile nav trigger component, keep header as server component where possible
    - Install lucide-react for icons if not already installed: `yarn add lucide-react`

    **Mobile Nav (`src/components/layout/mobile-nav.tsx`):**
    - `'use client'` component
    - Uses shadcn Sheet component (side="left", width 280px)
    - SheetTrigger wraps a Button with Menu icon (lucide-react)
    - Sheet content: Logo at top, vertical nav links (차량검색, 렌탈/리스, 문의하기, 로그인), separated by Separator
    - Dark navy background for sheet content (bg-sidebar-background, text-sidebar-foreground)
    - Links close the sheet on click (use Sheet's onOpenChange)

    **Footer (`src/components/layout/footer.tsx`):**
    - Minimal per user decision: company name "Navid Auto", copyright "2026 Navid Auto. All rights reserved.", contact placeholder
    - Dark navy background (bg-primary), gold accent text for company name
    - Responsive: single column on mobile, row layout on desktop
    - Max-width container, horizontal padding matching header

    **Public Layout (`src/app/(public)/layout.tsx`):**
    - Imports Header and Footer
    - Flex column, min-h-screen, main is flex-1
    - Server component

    **Public Page (`src/app/(public)/page.tsx`):**
    - Move the placeholder from `src/app/page.tsx` here
    - Simple hero section: "프리미엄 중고차 렌탈 & 리스" heading, "Navid Auto에서 시작하세요" subtext
    - CTA button "차량 둘러보기" with gold accent styling (glassmorphism allowed for CTA per CONTEXT.md)
    - Update `src/app/page.tsx` to redirect to `/(public)` or remove it if route group handles root

    Note: Since (public) is a route group (no URL prefix), the root `/` should be handled by `src/app/(public)/page.tsx`. Remove or redirect `src/app/page.tsx` to avoid conflict.
  </action>
  <verify>
    <automated>yarn build && yarn type-check</automated>
  </verify>
  <done>
    - Header renders with logo and nav links on desktop, hamburger on mobile
    - Mobile Sheet navigation opens/closes correctly
    - Footer shows company name, copyright, contact
    - Public layout wraps content with header + footer
    - Root URL (/) renders the public landing placeholder
    - No TypeScript errors, build succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Create admin and dealer sidebar layouts with route structure</name>
  <files>
    src/app/admin/layout.tsx, src/app/admin/dashboard/page.tsx,
    src/app/dealer/layout.tsx, src/app/dealer/dashboard/page.tsx,
    src/components/layout/admin-sidebar.tsx, src/components/layout/dealer-sidebar.tsx
  </files>
  <action>
    **Admin Sidebar (`src/components/layout/admin-sidebar.tsx`):**
    - `'use client'` component (for active link highlighting)
    - Dark navy background (bg-sidebar-background), gold accent for active/hover states
    - Width: 256px (w-64) on desktop, collapsible on mobile via Sheet
    - Navigation sections:
      - 대시보드 (LayoutDashboard icon)
      - 차량 관리 (Car icon)
      - 계약 관리 (FileText icon)
      - 사용자 관리 (Users icon)
      - 잔존가치 관리 (Calculator icon)
    - Use lucide-react icons
    - Logo "Navid Admin" at top with gold accent
    - Links use `next/link`, highlight active link with sidebar-accent color
    - Mobile: render inside a Sheet (triggered by hamburger in admin layout header bar)

    **Dealer Sidebar (`src/components/layout/dealer-sidebar.tsx`):**
    - Same pattern as admin sidebar but different nav items:
      - 대시보드 (LayoutDashboard icon)
      - 내 차량 (Car icon)
      - 계약 요청 (FileText icon)
      - 프로필 (User icon)
    - Same dark navy + gold styling
    - Logo "Navid Dealer" at top

    **Admin Layout (`src/app/admin/layout.tsx`):**
    - Desktop: sidebar fixed left (w-64), main content fills remainder with left margin (ml-64)
    - Mobile: sidebar hidden, top bar with hamburger + "Navid Admin" title, sidebar opens via Sheet
    - Use responsive classes: `hidden md:flex` for desktop sidebar, `md:hidden` for mobile header bar
    - Main content area: padded (p-6), max-width for readability

    **Dealer Layout (`src/app/dealer/layout.tsx`):**
    - Same pattern as admin layout but uses DealerSidebar
    - Top bar shows "Navid Dealer" on mobile

    **Placeholder pages:**
    - `src/app/admin/dashboard/page.tsx`: heading "관리자 대시보드", brief "Phase 9에서 구현됩니다" placeholder text
    - `src/app/dealer/dashboard/page.tsx`: heading "딜러 대시보드", brief "Phase 4에서 구현됩니다" placeholder text
    - Both use proper heading hierarchy and Tailwind typography classes

    **URL structure (clean approach):**
    - `src/app/admin/` -- actual route segment, URLs: `/admin/dashboard`, `/admin/vehicles`, etc.
    - `src/app/dealer/` -- actual route segment, URLs: `/dealer/dashboard`, `/dealer/vehicles`, etc.
    - `src/app/(public)/` -- route group (no URL prefix) for public pages
  </action>
  <verify>
    <automated>yarn build && yarn type-check</automated>
  </verify>
  <done>
    - /admin/dashboard renders with admin sidebar on desktop, hamburger on mobile
    - /dealer/dashboard renders with dealer sidebar on desktop, hamburger on mobile
    - Sidebar nav items display with correct icons and labels
    - Active link highlights with gold accent
    - Mobile sidebar opens/closes via Sheet
    - Sidebar has dark navy background with gold accent text
    - No TypeScript errors, build succeeds
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify responsive layout shell visually</name>
  <files>none -- verification only</files>
  <action>
    Human visually verifies the complete responsive layout shell across three navigation patterns (public header/footer, admin sidebar, dealer sidebar) on both desktop and mobile viewports. This is a visual quality gate before closing Phase 1.
  </action>
  <verify>
    <automated>yarn build</automated>
  </verify>
  <done>Human confirms layout renders correctly on desktop and mobile with dark navy + gold theme and Pretendard font</done>
  <what-built>Complete responsive layout shell with three navigation patterns: public (header/footer), admin (sidebar), and dealer (sidebar). All responsive with mobile hamburger menus.</what-built>
  <how-to-verify>
    1. Run `yarn dev` and open http://localhost:3000
    2. Verify public layout:
       - Desktop: header with "Navid Auto" logo, nav links, footer with dark navy background
       - Resize to mobile width: hamburger appears, clicking opens slide-out Sheet nav
       - Hero section shows Korean text with Pretendard font
    3. Visit http://localhost:3000/admin/dashboard
       - Desktop: dark navy sidebar on left with gold accent, dashboard content on right
       - Mobile: sidebar hidden, top bar with hamburger, Sheet opens sidebar
    4. Visit http://localhost:3000/dealer/dashboard
       - Same sidebar pattern as admin but with dealer-specific nav items
    5. Confirm colors: dark navy primary, gold accents, glassmorphism on header
    6. Confirm Korean text renders in Pretendard font (clean, modern appearance)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `yarn build` succeeds with all layouts
- `yarn type-check` passes
- Root URL (/) shows public layout with header + hero + footer
- /admin/dashboard shows admin sidebar layout
- /dealer/dashboard shows dealer sidebar layout
- All layouts are responsive (desktop + mobile)
- Design tokens (dark navy + gold) applied consistently
- Korean text renders in Pretendard font
</verification>

<success_criteria>
- Three distinct layout patterns functional: public (header/footer), admin (sidebar), dealer (sidebar)
- All layouts responsive with mobile navigation (Sheet-based hamburger menus)
- Dark navy + gold design system visible across all layouts
- Pretendard font rendering Korean text
- Human verification confirms visual quality matches premium automotive platform aesthetic
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
