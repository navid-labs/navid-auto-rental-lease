---
phase: 03-vehicle-data-storage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/features/vehicles/schemas/vehicle.ts
  - src/features/vehicles/schemas/vehicle.test.ts
  - src/features/vehicles/utils/status-machine.ts
  - src/features/vehicles/utils/status-machine.test.ts
  - src/features/vehicles/utils/plate-adapter.ts
  - src/features/vehicles/utils/plate-adapter.test.ts
  - src/features/vehicles/utils/image-compression.ts
  - src/features/vehicles/components/status-badge.tsx
  - src/features/vehicles/types/index.ts
autonomous: true
requirements:
  - VEHI-05
  - VEHI-06
  - VEHI-01

must_haves:
  truths:
    - "Vehicle form validation enforces required fields (brand, model, year, mileage, color, price)"
    - "Status machine allows only valid transitions and respects role permissions"
    - "License plate lookup returns vehicle data for valid plates and null for invalid"
    - "Status badges render with correct colors per status"
  artifacts:
    - path: "src/features/vehicles/schemas/vehicle.ts"
      provides: "Zod validation schemas for vehicle forms"
      exports: ["vehicleStep1Schema", "vehicleStep2Schema", "vehicleFormSchema"]
    - path: "src/features/vehicles/utils/status-machine.ts"
      provides: "Vehicle status transition map and validation"
      exports: ["VEHICLE_STATUS_TRANSITIONS", "canTransition", "getAvailableTransitions"]
    - path: "src/features/vehicles/utils/plate-adapter.ts"
      provides: "License plate lookup adapter with mock provider"
      exports: ["PlateProvider", "MockPlateProvider", "PlateResult", "lookupPlate"]
    - path: "src/features/vehicles/utils/image-compression.ts"
      provides: "Client-side image compression utility"
      exports: ["compressImage", "ACCEPTED_IMAGE_TYPES", "MAX_FILE_SIZE_MB", "MAX_IMAGES_PER_VEHICLE"]
    - path: "src/features/vehicles/components/status-badge.tsx"
      provides: "Shared color-coded status badge component"
      exports: ["StatusBadge", "STATUS_LABELS"]
    - path: "src/features/vehicles/types/index.ts"
      provides: "Vehicle-specific TypeScript types"
      exports: ["VehicleFormData", "VehicleWithDetails", "ImageItem"]
    - path: "prisma/schema.prisma"
      provides: "VehicleStatusLog model for audit trail"
      contains: "model VehicleStatusLog"
  key_links:
    - from: "src/features/vehicles/schemas/vehicle.ts"
      to: "@prisma/client"
      via: "imports VehicleStatus, FuelType, Transmission enums"
      pattern: "import.*@prisma/client"
    - from: "src/features/vehicles/utils/status-machine.ts"
      to: "@prisma/client"
      via: "imports VehicleStatus enum"
      pattern: "VehicleStatus"
---

<objective>
Build the foundation layer for vehicle features: Zod schemas, status state machine, license plate adapter, image compression utility, status badge component, and vehicle types. Also add VehicleStatusLog model to Prisma schema for status change audit trail and install new dependencies (@dnd-kit, browser-image-compression).

Purpose: All subsequent vehicle plans depend on these contracts, utilities, and types. Building them first prevents the "scavenger hunt" anti-pattern.
Output: Tested utilities, validated schemas, shared components, updated Prisma schema.
</objective>

<execution_context>
@/Users/kiyeol/.claude-work/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-vehicle-data-storage/03-CONTEXT.md
@.planning/phases/03-vehicle-data-storage/03-RESEARCH.md

<interfaces>
<!-- Existing interfaces the executor needs -->

From src/lib/auth/helpers.ts:
```typescript
export type UserProfile = {
  id: string
  email: string
  name: string | null
  phone: string | null
  role: 'CUSTOMER' | 'DEALER' | 'ADMIN'
  avatar_url: string | null
  created_at: string
  updated_at: string
}
export const getCurrentUser = cache(async (): Promise<UserProfile | null> => { ... })
```

From src/types/index.ts:
```typescript
export type { Profile, Brand, CarModel, Generation, Trim, Vehicle, VehicleImage, ... } from '@prisma/client'
export { UserRole, FuelType, Transmission, VehicleStatus, ContractStatus, ... } from '@prisma/client'
```

From src/lib/db/prisma.ts:
```typescript
export const prisma = globalForPrisma.prisma ?? new PrismaClient()
```

From src/lib/supabase/admin.ts:
```typescript
export function createAdminClient()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install dependencies, update Prisma schema, create vehicle types and Zod schemas</name>
  <files>prisma/schema.prisma, src/features/vehicles/types/index.ts, src/features/vehicles/schemas/vehicle.ts, src/features/vehicles/schemas/vehicle.test.ts</files>
  <behavior>
    - vehicleStep1Schema: validates brandId (uuid), modelId (uuid), generationId (uuid), trimId (uuid) are all required non-empty strings
    - vehicleStep1Schema: licensePlate is optional string
    - vehicleStep2Schema: validates year (1990-current+1), mileage (>=0), color (non-empty), price (>0)
    - vehicleStep2Schema: description is optional
    - vehicleFormSchema: merges step1 + step2 fields
    - Rejects invalid data with appropriate error messages in Korean
  </behavior>
  <action>
    1. Install new dependencies: `yarn add @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities browser-image-compression`
    2. Add VehicleStatusLog model to prisma/schema.prisma with fields: id (uuid), vehicleId, fromStatus, toStatus, note (optional), changedBy (uuid), createdAt. Add relations to Vehicle and Profile models. Add `statusLogs VehicleStatusLog[]` to Vehicle model and `vehicleStatusLogs VehicleStatusLog[]` to Profile model.
    3. Run `yarn db:generate` to regenerate Prisma client (do NOT run db:push as we don't know if DB connection is available).
    4. Create src/features/vehicles/types/index.ts with:
       - VehicleFormData type (all wizard fields)
       - VehicleWithDetails type (Vehicle with nested trim.generation.carModel.brand, images, dealer)
       - ImageItem type ({ id: string; url: string; order: number })
    5. Create Zod schemas in src/features/vehicles/schemas/vehicle.ts:
       - vehicleStep1Schema: brandId, modelId, generationId, trimId (all z.string().uuid()), licensePlate (optional)
       - vehicleStep2Schema: year (z.coerce.number().int().min(1990).max(new Date().getFullYear()+1)), mileage (z.coerce.number().int().min(0)), color (z.string().min(1)), price (z.coerce.number().int().positive()), monthlyRental (optional), monthlyLease (optional), description (optional)
       - vehicleFormSchema: vehicleStep1Schema.merge(vehicleStep2Schema)
    6. Write tests in vehicle.test.ts covering valid/invalid cases for each schema.

    Note: Use Zod from the installed version. Check actual API — if Zod 4 is installed, use z.string().check(z.uuid()) syntax instead of z.string().uuid(). If Zod 3, use z.string().uuid().
  </action>
  <verify>
    <automated>cd /Users/kiyeol/development/navid/navid-auto-rental-lease && yarn vitest run src/features/vehicles/schemas/vehicle.test.ts</automated>
  </verify>
  <done>VehicleStatusLog model added to schema, Prisma client regenerated, vehicle types exported, Zod schemas pass all validation tests</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create status machine, plate adapter, image compression utility, and status badge</name>
  <files>src/features/vehicles/utils/status-machine.ts, src/features/vehicles/utils/status-machine.test.ts, src/features/vehicles/utils/plate-adapter.ts, src/features/vehicles/utils/plate-adapter.test.ts, src/features/vehicles/utils/image-compression.ts, src/features/vehicles/components/status-badge.tsx</files>
  <behavior>
    - canTransition('AVAILABLE', 'RESERVED', 'DEALER') returns true
    - canTransition('HIDDEN', 'AVAILABLE', 'DEALER') returns false (admin only)
    - canTransition('HIDDEN', 'AVAILABLE', 'ADMIN') returns true (admin force)
    - canTransition('AVAILABLE', 'RENTED', 'DEALER') returns false (must go through RESERVED)
    - getAvailableTransitions('AVAILABLE', 'DEALER') returns ['RESERVED', 'MAINTENANCE', 'HIDDEN']
    - MockPlateProvider.lookup('12가1234') returns mock vehicle data
    - MockPlateProvider.lookup('invalid') returns null
  </behavior>
  <action>
    1. Create src/features/vehicles/utils/status-machine.ts:
       - VEHICLE_STATUS_TRANSITIONS map as shown in RESEARCH.md Pattern 2
       - canTransition(from, to, role) function — Admin can force ANY transition, dealer follows strict map
       - getAvailableTransitions(from, role) — returns list of valid target statuses for given role
    2. Create status-machine.test.ts with tests for valid transitions, invalid transitions, admin override, and getAvailableTransitions.
    3. Create src/features/vehicles/utils/plate-adapter.ts:
       - PlateResult type, PlateProvider interface
       - MockPlateProvider class returning realistic Korean vehicle data for valid plate patterns (^\d{2,3}[가-힣]\d{4}$)
       - lookupPlate(plateNumber) factory function using MockPlateProvider
    4. Create plate-adapter.test.ts testing valid plate returns data, invalid returns null.
    5. Create src/features/vehicles/utils/image-compression.ts:
       - compressImage(file) wrapper around browser-image-compression with maxSizeMB: 2, maxWidthOrHeight: 1920, useWebWorker: true, fileType: 'image/webp'
       - Skip compression for files under 500KB
       - Export ACCEPTED_IMAGE_TYPES = ['image/jpeg', 'image/png', 'image/webp', 'image/heic']
       - Export MAX_FILE_SIZE_MB = 10, MAX_IMAGES_PER_VEHICLE = 10
    6. Create src/features/vehicles/components/status-badge.tsx:
       - Use cva (class-variance-authority) for variants
       - Colors per CONTEXT.md: green (Available), yellow (Reserved), blue (Rented/Leased), orange (Maintenance), gray (Hidden)
       - Korean labels: 판매 가능, 예약됨, 렌탈 중, 리스 중, 정비 중, 숨김
       - Export STATUS_LABELS record for reuse
  </action>
  <verify>
    <automated>cd /Users/kiyeol/development/navid/navid-auto-rental-lease && yarn vitest run src/features/vehicles/utils/status-machine.test.ts src/features/vehicles/utils/plate-adapter.test.ts</automated>
  </verify>
  <done>Status machine validates transitions with role-based enforcement, plate adapter returns mock data for valid Korean plates, image compression utility configured, status badge renders with correct color coding</done>
</task>

</tasks>

<verification>
- `yarn vitest run src/features/vehicles/` passes all tests
- `yarn type-check` passes with no errors
- prisma/schema.prisma contains VehicleStatusLog model
- @dnd-kit packages and browser-image-compression in node_modules
</verification>

<success_criteria>
- All vehicle foundation utilities tested and exported
- Status machine correctly enforces transition rules per role
- Plate adapter returns mock data for valid plates
- Zod schemas validate all vehicle form fields
- VehicleStatusLog model added to Prisma schema
- Status badge component renders with correct Korean labels and colors
</success_criteria>

<output>
After completion, create `.planning/phases/03-vehicle-data-storage/03-01-SUMMARY.md`
</output>
