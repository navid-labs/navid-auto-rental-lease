---
phase: 03-vehicle-data-storage
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/features/vehicles/actions/upload-images.ts
  - src/features/vehicles/actions/upload-images.test.ts
  - src/features/vehicles/actions/delete-image.ts
  - src/features/vehicles/actions/reorder-images.ts
  - src/features/vehicles/components/step-photos.tsx
  - src/features/vehicles/components/image-dropzone.tsx
  - src/features/vehicles/components/sortable-image-grid.tsx
  - src/features/vehicles/components/vehicle-detail-view.tsx
  - src/app/dealer/vehicles/[id]/page.tsx
  - src/app/admin/vehicles/[id]/page.tsx
autonomous: false
requirements:
  - VEHI-02
  - VEHI-06

must_haves:
  truths:
    - "Dealer can upload up to 10 photos per vehicle via drag-and-drop or file picker"
    - "Images are compressed client-side before upload to Supabase Storage"
    - "Uploaded thumbnails can be reordered by dragging, with first image as primary"
    - "Vehicle detail page displays photo gallery and full vehicle information"
    - "Images persist in Supabase Storage and URLs are stored in database"
  artifacts:
    - path: "src/features/vehicles/actions/upload-images.ts"
      provides: "Server Action for uploading images to Supabase Storage"
      exports: ["uploadVehicleImage"]
    - path: "src/features/vehicles/actions/delete-image.ts"
      provides: "Server Action for deleting images from Storage + DB"
      exports: ["deleteVehicleImage"]
    - path: "src/features/vehicles/actions/reorder-images.ts"
      provides: "Server Action for persisting image order"
      exports: ["reorderVehicleImages"]
    - path: "src/features/vehicles/components/step-photos.tsx"
      provides: "Wizard Step 3: photo upload with dropzone and sortable grid"
      exports: ["StepPhotos"]
    - path: "src/features/vehicles/components/sortable-image-grid.tsx"
      provides: "dnd-kit sortable thumbnail grid"
      exports: ["SortableImageGrid"]
    - path: "src/features/vehicles/components/vehicle-detail-view.tsx"
      provides: "Read-only vehicle detail page with gallery"
      exports: ["VehicleDetailView"]
  key_links:
    - from: "src/features/vehicles/actions/upload-images.ts"
      to: "supabase.storage"
      via: "Admin client uploads to vehicle-images bucket"
      pattern: "supabase\\.storage.*from.*vehicle-images"
    - from: "src/features/vehicles/components/step-photos.tsx"
      to: "src/features/vehicles/utils/image-compression.ts"
      via: "compresses images before upload"
      pattern: "compressImage"
    - from: "src/features/vehicles/components/sortable-image-grid.tsx"
      to: "@dnd-kit/sortable"
      via: "provides drag-to-reorder functionality"
      pattern: "useSortable"
    - from: "src/features/vehicles/components/step-photos.tsx"
      to: "src/features/vehicles/actions/upload-images.ts"
      via: "uploads compressed images via Server Action"
      pattern: "uploadVehicleImage"

user_setup:
  - service: supabase-storage
    why: "Vehicle images need a Storage bucket configured"
    env_vars: []
    dashboard_config:
      - task: "Create 'vehicle-images' public bucket with 5MB file limit and allowed MIME types (image/jpeg, image/png, image/webp, image/heic)"
        location: "Supabase Dashboard -> Storage -> New bucket"
---

<objective>
Build the image upload system (drag-and-drop upload, compression, Supabase Storage integration, sortable reorder) and vehicle detail view page. This completes the wizard's Step 3 (Photos) and adds read-only detail pages.

Purpose: Images are a critical part of vehicle listings. This plan adds the photo upload capability that makes vehicle listings compelling for customers browsing in Phase 5.
Output: Working image upload with drag-reorder, vehicle detail pages with photo gallery.
</objective>

<execution_context>
@/Users/kiyeol/.claude-work/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-vehicle-data-storage/03-CONTEXT.md
@.planning/phases/03-vehicle-data-storage/03-RESEARCH.md
@.planning/phases/03-vehicle-data-storage/03-01-SUMMARY.md
@.planning/phases/03-vehicle-data-storage/03-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 -->
From src/features/vehicles/utils/image-compression.ts:
```typescript
export async function compressImage(file: File): Promise<File>
export const ACCEPTED_IMAGE_TYPES: string[]
export const MAX_FILE_SIZE_MB: number
export const MAX_IMAGES_PER_VEHICLE: number
```

From src/features/vehicles/types/index.ts:
```typescript
export type ImageItem = { id: string; url: string; order: number }
export type VehicleWithDetails = Vehicle & { trim: ...; images: VehicleImage[]; dealer: Profile }
```

<!-- From Plan 02 -->
From src/features/vehicles/components/vehicle-wizard.tsx:
```typescript
// Step 3 renders <StepPhotos vehicleId={vehicleId} onBack={() => setStep(2)} />
// vehicleId is available after create in step 2, or from initialData in edit mode
```

From src/lib/supabase/admin.ts:
```typescript
export function createAdminClient()
```

From src/lib/auth/helpers.ts:
```typescript
export const getCurrentUser: () => Promise<UserProfile | null>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Image upload, delete, and reorder Server Actions with Supabase Storage</name>
  <files>src/features/vehicles/actions/upload-images.ts, src/features/vehicles/actions/upload-images.test.ts, src/features/vehicles/actions/delete-image.ts, src/features/vehicles/actions/reorder-images.ts</files>
  <behavior>
    - uploadVehicleImage: authenticated DEALER/ADMIN can upload, rejects CUSTOMER
    - uploadVehicleImage: dealer can only upload to own vehicles
    - uploadVehicleImage: rejects when vehicle already has 10 images
    - uploadVehicleImage: stores file in vehicle-images bucket at path vehicles/{vehicleId}/{timestamp}.{ext}
    - uploadVehicleImage: creates VehicleImage record with url, order, isPrimary (first = true)
    - deleteVehicleImage: removes from Storage and deletes DB record, ownership check for dealers
    - reorderVehicleImages: updates order field for list of image IDs, sets isPrimary on order=0
  </behavior>
  <action>
    1. Create src/features/vehicles/actions/upload-images.ts ('use server'):
       - Takes vehicleId and FormData with file
       - Auth check: DEALER or ADMIN only
       - Ownership check for DEALER (vehicle.dealerId === user.id)
       - Count check: existing images < MAX_IMAGES_PER_VEHICLE (10)
       - Upload to Supabase Storage via admin client: `vehicle-images` bucket, path `vehicles/${vehicleId}/${Date.now()}.${ext}`
       - Create VehicleImage record with publicUrl, order = existingCount, isPrimary = (existingCount === 0)
       - Return { success: true, image: { id, url, order } } or { error: string }

    2. Create src/features/vehicles/actions/delete-image.ts ('use server'):
       - Takes imageId
       - Fetch image with vehicle relation for ownership check
       - Remove from Supabase Storage (parse path from URL)
       - Delete VehicleImage record
       - If deleted image was isPrimary, set next image (order=0) as primary

    3. Create src/features/vehicles/actions/reorder-images.ts ('use server'):
       - Takes vehicleId and orderedImageIds: string[]
       - Auth + ownership check
       - Use prisma.$transaction to update all images with new order values
       - Set isPrimary = true for order 0, false for rest

    4. Write tests for upload-images.ts mocking prisma, getCurrentUser, and supabase storage client:
       - Test auth rejection for customers
       - Test ownership check for dealers
       - Test max image count enforcement
       - Test successful upload creates record
  </action>
  <verify>
    <automated>cd /Users/kiyeol/development/navid/navid-auto-rental-lease && yarn vitest run src/features/vehicles/actions/upload-images.test.ts</automated>
  </verify>
  <done>Upload, delete, and reorder Server Actions implemented with auth, ownership, and count enforcement. Tests pass for authorization and business logic.</done>
</task>

<task type="auto">
  <name>Task 2: Photo upload UI (dropzone, sortable grid), vehicle detail page, and wizard Step 3 integration</name>
  <files>src/features/vehicles/components/step-photos.tsx, src/features/vehicles/components/image-dropzone.tsx, src/features/vehicles/components/sortable-image-grid.tsx, src/features/vehicles/components/vehicle-detail-view.tsx, src/app/dealer/vehicles/[id]/page.tsx, src/app/admin/vehicles/[id]/page.tsx</files>
  <action>
    1. Create src/features/vehicles/components/image-dropzone.tsx ('use client'):
       - Large dropzone area with dashed border and upload icon
       - HTML5 native onDragOver/onDrop events for file dropping (NOT dnd-kit — that's for reorder only)
       - Click to browse via hidden input[type=file] with accept={ACCEPTED_IMAGE_TYPES.join(',')}
       - Multiple file selection enabled
       - Validate each file: type in ACCEPTED_IMAGE_TYPES, size < MAX_FILE_SIZE_MB
       - Show invalid file errors inline
       - On valid files: compress each via compressImage(), then call uploadVehicleImage Server Action
       - Upload progress: show uploading state per file (spinner overlay on thumbnail)
       - Use Promise.all for parallel uploads (batch)
       - Props: vehicleId, currentCount, onUploadComplete

    2. Create src/features/vehicles/components/sortable-image-grid.tsx ('use client'):
       - Follow RESEARCH.md Pattern (sortable grid with dnd-kit)
       - DndContext with closestCenter collision detection
       - SortableContext with rectSortingStrategy
       - Each image: thumbnail with aspect-[4/3], delete button (X, appears on hover), "대표" badge on order=0
       - On drag end: reorder in local state immediately (optimistic), then call reorderVehicleImages Server Action
       - On delete: call deleteVehicleImage, remove from local state
       - Props: images (ImageItem[]), vehicleId, onImagesChange

    3. Create src/features/vehicles/components/step-photos.tsx ('use client'):
       - Combines ImageDropzone (for adding new photos) and SortableImageGrid (for managing existing)
       - Shows count: "X / 10 장"
       - Hides dropzone when 10 images reached
       - "이전" button to go back to step 2
       - "완료" / "등록하기" button to finalize vehicle creation
       - Props: vehicleId, onBack, mode ('create' | 'edit')
       - Note: In create mode, vehicleId comes from the vehicle created at end of step 2. The wizard must create the vehicle record first (step 2 submit), then pass vehicleId to step 3 for image uploads.

    4. Update src/features/vehicles/components/vehicle-wizard.tsx:
       - Wire Step 3 to render StepPhotos instead of placeholder
       - In create mode: step 2 "다음" creates the vehicle first (createVehicle), gets vehicleId, then shows step 3 for photos
       - In edit mode: vehicleId already available from initialData

    5. Create src/features/vehicles/components/vehicle-detail-view.tsx:
       - Server Component friendly (receives data as props)
       - Photo gallery: large primary image + thumbnail row below
       - Click thumbnail to switch primary display image (client interaction)
       - Vehicle info sections: basic info (brand/model/year/color), specs (mileage, fuel, transmission, engine), pricing (price, monthly rental/lease if set)
       - Status badge
       - Description section
       - Dealer info (name)
       - Use formatKRW, formatDistance from lib/utils/format

    6. Create src/app/dealer/vehicles/[id]/page.tsx:
       - Server Component, fetch vehicle by id with ownership check
       - Render VehicleDetailView + "수정" button linking to edit page

    7. Create src/app/admin/vehicles/[id]/page.tsx:
       - Server Component, fetch vehicle by id (no ownership restriction)
       - Render VehicleDetailView + "수정" button + StatusChangeDialog
  </action>
  <verify>
    <automated>cd /Users/kiyeol/development/navid/navid-auto-rental-lease && yarn type-check && yarn lint</automated>
  </verify>
  <done>Image dropzone accepts drag-and-drop or click uploads with compression, sortable grid enables drag-to-reorder with persist, vehicle detail pages display gallery and full specs, wizard step 3 is fully integrated</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete vehicle management system end-to-end</name>
  <files>n/a</files>
  <action>
    Human verifies the complete vehicle management flow including wizard, image upload, vehicle list, and detail pages.
  </action>
  <verify>
    1. Ensure Supabase Storage bucket 'vehicle-images' exists (create in dashboard if not)
    2. Start dev server: `yarn dev`
    3. Log in as a dealer account
    4. Navigate to /dealer/vehicles — verify empty state or vehicle list renders
    5. Click "차량 등록" — verify multi-step wizard renders with progress bar
    6. Step 1: Try plate lookup with "12가1234" — verify mock data auto-fills. Or click "직접 입력" and select Brand > Model > Generation > Trim cascade
    7. Step 2: Fill year, mileage, color, price — verify validation (negative values rejected)
    8. Step 3: Upload 2-3 photos via drag-and-drop or click browse — verify thumbnails appear
    9. Drag thumbnails to reorder — verify "대표" badge moves to first position
    10. Click "등록하기" — verify redirect to vehicle list with new vehicle
    11. Click vehicle row — verify detail page with photo gallery
    12. Test status change: click status badge, change to "정비 중", add note
    13. Log in as admin — verify /admin/vehicles shows all vehicles including dealer's
    14. Verify admin can edit/delete any vehicle
  </verify>
  <done>All vehicle management features verified working by user: wizard, image upload, drag-reorder, vehicle list, detail pages, status management</done>
</task>

</tasks>

<verification>
- `yarn vitest run src/features/vehicles/` passes all tests
- `yarn type-check` passes
- `yarn lint` passes
- Images upload to Supabase Storage and display correctly
- Drag-to-reorder persists image order
- Vehicle detail pages show photo gallery
</verification>

<success_criteria>
- Dealer can upload up to 10 photos per vehicle
- Photos are compressed before upload (under 2MB)
- Drag-to-reorder changes are persisted to database
- Vehicle detail page displays photo gallery with all vehicle info
- Full wizard flow works end-to-end (plate/manual -> details -> photos -> submit)
</success_criteria>

<output>
After completion, create `.planning/phases/03-vehicle-data-storage/03-03-SUMMARY.md`
</output>
