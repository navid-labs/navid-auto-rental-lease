---
phase: 05-public-search-discovery
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(public)/page.tsx
  - src/features/marketing/components/hero-section.tsx
  - src/features/marketing/components/featured-vehicles.tsx
  - src/features/marketing/components/brand-shortcuts.tsx
  - src/features/marketing/components/how-it-works.tsx
  - src/features/marketing/components/trust-metrics.tsx
autonomous: true
requirements:
  - UIEX-02

must_haves:
  truths:
    - "Landing page displays a hero section with quick search (brand + model cascade + search button)"
    - "Landing page shows newest approved vehicles in a horizontal scroll section"
    - "Landing page shows brand shortcuts linking to pre-filtered search results"
    - "Landing page shows a 3-step process section explaining how the service works"
    - "Landing page is responsive (sections stack vertically on mobile)"
  artifacts:
    - path: "src/app/(public)/page.tsx"
      provides: "Landing page Server Component composing all marketing sections"
    - path: "src/features/marketing/components/hero-section.tsx"
      provides: "Full-width dark navy hero with glassmorphism quick search widget"
    - path: "src/features/marketing/components/featured-vehicles.tsx"
      provides: "Newest approved vehicles horizontal scroll section"
    - path: "src/features/marketing/components/brand-shortcuts.tsx"
      provides: "Brand logo/text grid linking to filtered search results"
  key_links:
    - from: "src/features/marketing/components/hero-section.tsx"
      to: "/vehicles?brand=X&model=Y"
      via: "Form action navigating to search page with query params"
      pattern: "router.push.*vehicles.*brand"
    - from: "src/features/marketing/components/featured-vehicles.tsx"
      to: "prisma.vehicle.findMany"
      via: "Server Component data fetch for newest APPROVED vehicles"
      pattern: "approvalStatus.*APPROVED.*approvedAt.*desc"
---

<objective>
Build the landing page with hero quick search, featured vehicles, brand shortcuts, how-it-works, and trust metrics sections.

Purpose: Create a polished public storefront entry point that showcases inventory and provides a frictionless path to vehicle search.
Output: Full landing page at / with 5 sections matching the premium dark navy + glassmorphism aesthetic.
</objective>

<execution_context>
@/Users/kiyeol/.claude-work/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-public-search-discovery/05-CONTEXT.md
@.planning/phases/05-public-search-discovery/05-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/features/vehicles/types/index.ts:
```typescript
export type VehicleWithDetails = Vehicle & {
  trim: Trim & {
    generation: Generation & {
      carModel: CarModel & { brand: Brand }
    }
  }
  images: VehicleImage[]
  dealer: Pick<Profile, 'id' | 'name' | 'email' | 'phone'>
}
```

From src/lib/utils/format.ts:
```typescript
export function formatKRW(amount: number, options?: { monthly?: boolean }): string
export function formatDistance(km: number, options?: { compact?: boolean }): string
export function formatYearModel(year: number): string
```

From src/features/vehicles/actions/get-cascade-data.ts:
```typescript
export async function getBrands(): Promise<Array<{ id: string; name: string; nameKo: string | null }>>
export async function getModelsByBrand(brandId: string): Promise<Array<{ id: string; name: string; nameKo: string | null }>>
```

From prisma/schema.prisma:
```prisma
model Brand {
  id      String  @id @default(uuid())
  name    String  @unique
  nameKo  String?
  logoUrl String?
}
```

From src/app/(public)/page.tsx (current placeholder):
```typescript
// Simple hero with "차량 둘러보기" link. Will be replaced entirely.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hero section with glassmorphism quick search widget</name>
  <files>
    src/features/marketing/components/hero-section.tsx
  </files>
  <action>
    Create src/features/marketing/components/hero-section.tsx as a 'use client' component:

    1. Full-width section with dark navy background: bg-primary (navy-900) with subtle CSS gradient (linear-gradient from navy-900 to navy-800 with radial accent glow). Min-height: min-h-[60vh] on desktop, min-h-[50vh] on mobile.

    2. Centered content: headline "프리미엄 중고차 렌탈 & 리스" (large, white text), subheadline "Navid Auto에서 시작하세요. 온라인으로 간편하게 비교하고 계약까지 완료하세요." (muted white).

    3. Glassmorphism quick search widget below text:
       - Container: bg-white/10 backdrop-blur-md rounded-2xl border border-white/20 p-6 shadow-xl
       - Two select dropdowns side by side: Brand (브랜드) and Model (모델)
       - Brand select fetches brands via getBrands on mount. Model select fetches models when brand selected via getModelsByBrand.
       - Use simple HTML select elements styled with Tailwind (white text on transparent bg) for simplicity in the hero context. Don't reuse the full CascadeSelect (it's too complex for the hero widget).
       - "차량 검색" primary button (bg-accent text-accent-foreground rounded-xl)
       - On submit: navigate to /vehicles?brand={brandId}&model={modelId} using useRouter

    4. Responsive: on mobile, stack selects vertically, full-width button. On desktop, inline row.
  </action>
  <verify>
    <automated>cd /Users/kiyeol/development/navid/navid-auto-rental-lease && yarn type-check</automated>
  </verify>
  <done>Hero section renders with dark navy background, headline, glassmorphism search widget with brand/model cascade and search button.</done>
</task>

<task type="auto">
  <name>Task 2: Featured vehicles, brand shortcuts, how-it-works, trust metrics, and landing page assembly</name>
  <files>
    src/features/marketing/components/featured-vehicles.tsx,
    src/features/marketing/components/brand-shortcuts.tsx,
    src/features/marketing/components/how-it-works.tsx,
    src/features/marketing/components/trust-metrics.tsx,
    src/app/(public)/page.tsx
  </files>
  <action>
    1. Create FeaturedVehicles (src/features/marketing/components/featured-vehicles.tsx):
       - Server Component that queries newest 8 APPROVED vehicles ordered by approvedAt DESC
       - Include vehicle relations (trim.generation.carModel.brand, images)
       - Section title: "신착 차량" with "전체 보기" link to /vehicles
       - Horizontal scroll container: flex overflow-x-auto snap-x snap-mandatory gap-4 pb-4
       - Each item: reuse the same VehicleCard pattern (import from Plan 01 if available, otherwise create inline mini-card with same design: 16:9 image, name, year/mileage, monthly price). Use min-w-[280px] snap-start for scroll items.
       - NOTE: This plan runs parallel to Plan 01. If VehicleCard is not yet available, create an inline VehicleCardMini with the same design. Plan 01's VehicleCard can be used later.

    2. Create BrandShortcuts (src/features/marketing/components/brand-shortcuts.tsx):
       - Server Component that fetches all brands via prisma.brand.findMany()
       - Grid of brand items: grid-cols-3 sm:grid-cols-4 md:grid-cols-6 gap-4
       - Each item: Link to /vehicles?brand={brandId}
       - If brand.logoUrl exists, show logo image. Otherwise, show text fallback: brand initial letter in a rounded circle (bg-muted, text-primary, font-bold, size-12) with brand name below.
       - Section title: "브랜드 바로가기"

    3. Create HowItWorks (src/features/marketing/components/how-it-works.tsx):
       - Static Server Component with 3-step process
       - Steps: 1) 검색 (Search icon) "원하는 차량을 검색하세요", 2) 비교 (BarChart3 icon) "조건을 비교해 보세요", 3) 계약 (FileCheck icon) "비대면으로 계약하세요"
       - Grid: grid-cols-1 md:grid-cols-3, each step in a card with icon, step number, title, description
       - Use lucide-react icons (Search, BarChart3, FileCheck)
       - Section title: "이용 방법"

    4. Create TrustMetrics (src/features/marketing/components/trust-metrics.tsx):
       - Server Component that queries counts: total approved vehicles, total brands with vehicles, total dealers (profiles with role DEALER)
       - Uses Promise.all for parallel DB queries
       - Display: 3-4 stat cards in a row (grid-cols-2 md:grid-cols-4)
       - Each stat: large number (text-3xl font-bold), label below (text-muted-foreground)
       - Stats: "등록 차량" (vehicle count), "제휴 브랜드" (brand count), "협력 딜러" (dealer count)
       - Section bg: bg-muted/50 for visual separation

    5. Assemble landing page (src/app/(public)/page.tsx):
       - Replace existing placeholder entirely
       - Server Component, import and compose: HeroSection, FeaturedVehicles, BrandShortcuts, HowItWorks, TrustMetrics
       - Each section wrapped in appropriate container (max-w-7xl mx-auto px-4 py-16) except Hero (full-width)
       - Add section spacing: space-y-0 (sections manage their own padding)
  </action>
  <verify>
    <automated>cd /Users/kiyeol/development/navid/navid-auto-rental-lease && yarn type-check && yarn lint && yarn test --run</automated>
  </verify>
  <done>
    - Landing page at / renders all 5 sections: hero with quick search, featured vehicles scroll, brand shortcuts grid, how-it-works 3-step, trust metrics stats
    - Featured vehicles section shows newest approved vehicles from DB
    - Brand shortcuts link to /vehicles?brand={id}
    - Hero search navigates to /vehicles with brand/model params
    - Responsive: sections stack on mobile, hero search simplifies
  </done>
</task>

</tasks>

<verification>
1. `yarn type-check` -- no TypeScript errors
2. `yarn lint` -- no ESLint errors
3. `yarn test --run` -- all tests pass
4. Visit / -- landing page with all 5 sections visible
5. Click brand shortcut -- navigates to /vehicles?brand=X
6. Use hero search -- navigates to /vehicles with brand+model params
7. Featured vehicles show real data from DB
</verification>

<success_criteria>
- Landing page has premium dark navy hero with glassmorphism quick search
- Featured vehicles section shows newest approved vehicles in horizontal scroll
- Brand shortcuts grid links to pre-filtered search results
- How-it-works shows 3-step process
- Trust metrics shows real DB counts
- All sections responsive on mobile
- Hero quick search navigates to /vehicles with correct URL params
</success_criteria>

<output>
After completion, create `.planning/phases/05-public-search-discovery/05-02-SUMMARY.md`
</output>
