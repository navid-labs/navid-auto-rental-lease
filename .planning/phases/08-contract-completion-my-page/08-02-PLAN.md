---
phase: 08-contract-completion-my-page
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/features/contracts/actions/get-my-contracts.ts
  - src/features/contracts/components/contract-card.tsx
  - src/features/contracts/components/contract-list.tsx
  - src/app/(protected)/mypage/page.tsx
  - src/app/(protected)/contracts/[id]/page.tsx
  - src/features/contracts/types/index.ts
autonomous: true
requirements:
  - CONT-04
  - UIEX-03

must_haves:
  truths:
    - "Customer sees all their contracts (both rental and lease) on my page"
    - "Contract cards show vehicle name, contract type badge, status badge, monthly payment, and period"
    - "Status filter tabs (All / In Progress / Completed / Canceled) filter the contract list"
    - "Clicking a contract card navigates to /contracts/[id]?type=TYPE detail page"
    - "Download PDF button appears on approved+ contracts (APPROVED, ACTIVE, COMPLETED)"
    - "Download button triggers /api/contracts/[id]/pdf?type=TYPE"
    - "Empty state shows when customer has no contracts with CTA to browse vehicles"
    - "Contract detail page has a PDF download button"
  artifacts:
    - path: "src/features/contracts/actions/get-my-contracts.ts"
      provides: "Server action to fetch customer rental + lease contracts in parallel"
      exports: ["getMyContracts"]
    - path: "src/features/contracts/components/contract-card.tsx"
      provides: "Compact contract card with status badge and download button"
      exports: ["ContractCard"]
    - path: "src/features/contracts/components/contract-list.tsx"
      provides: "Client component with status filter tabs and contract grid"
      exports: ["ContractList"]
    - path: "src/app/(protected)/mypage/page.tsx"
      provides: "Expanded my page with profile section and contract list"
  key_links:
    - from: "src/app/(protected)/mypage/page.tsx"
      to: "src/features/contracts/actions/get-my-contracts.ts"
      via: "server component data fetch"
      pattern: "getMyContracts"
    - from: "src/features/contracts/components/contract-card.tsx"
      to: "/api/contracts/[id]/pdf"
      via: "download link href"
      pattern: "api/contracts.*pdf"
    - from: "src/features/contracts/components/contract-list.tsx"
      to: "searchParams tab state"
      via: "URL-based filter (Phase 4 pattern)"
      pattern: "tab="
---

<objective>
Customer my page with contract list, status filtering, and PDF download integration.

Purpose: Deliver CONT-04 (contract tracking) and UIEX-03 (my page with contract list + PDF download). Customers can view all their contracts, filter by status, and download PDFs for approved contracts.
Output: Expanded /mypage with contract card list, status filter tabs, and PDF download buttons wired to Plan 01's API route.
</objective>

<execution_context>
@/Users/kiyeol/.claude-work/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-contract-completion-my-page/08-CONTEXT.md
@.planning/phases/08-contract-completion-my-page/08-RESEARCH.md
@.planning/phases/08-contract-completion-my-page/08-01-SUMMARY.md

<interfaces>
<!-- Types from Plan 01 and existing codebase -->

From src/features/contracts/types/index.ts (existing + Plan 01 addition):
```typescript
export type ContractWithVehicle = (RentalContract | LeaseContract) & {
  vehicle: VehicleWithDetails
}
export type ContractPDFData = { /* from Plan 01 */ }
```

From src/features/contracts/components/contract-status-badge.tsx:
```typescript
export function ContractStatusBadge({ status }: { status: ContractStatus })
```

From src/features/contracts/utils/contract-machine.ts:
```typescript
export const CONTRACT_STATUS_LABELS: Record<ContractStatus, string>
```

From src/lib/utils/format.ts:
```typescript
export function formatKRW(amount: number, options?: { monthly?: boolean }): string
export function formatDate(date: Date | string, options?: { short?: boolean }): string
```

From src/app/(protected)/mypage/page.tsx (current):
```typescript
// Currently only shows ProfileForm in a Card
// Must be expanded with contract list section below profile
```

From src/app/(protected)/contracts/[id]/page.tsx:
```typescript
// Shows ContractStatusTracker
// Needs PDF download button added
```

From Phase 4 admin searchParams tab pattern:
```typescript
// URL: /mypage?tab=active
// searchParams.tab determines active filter
```

PDF API route (from Plan 01):
```
GET /api/contracts/[id]/pdf?type=RENTAL|LEASE
Returns: application/pdf with Content-Disposition: inline
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create contract data fetcher, ContractCard, and ContractList components</name>
  <files>src/features/contracts/actions/get-my-contracts.ts, src/features/contracts/components/contract-card.tsx, src/features/contracts/components/contract-list.tsx, src/features/contracts/types/index.ts</files>
  <action>
1. Add `ContractListItem` type to `src/features/contracts/types/index.ts`:
   ```typescript
   export type ContractListItem = {
     id: string
     contractType: 'RENTAL' | 'LEASE'
     status: ContractStatus
     vehicleName: string
     vehicleYear: number
     vehicleImageUrl: string | null
     monthlyPayment: number
     deposit: number
     startDate: Date | null
     endDate: Date | null
     createdAt: Date
   }
   ```

2. Create `src/features/contracts/actions/get-my-contracts.ts`:
   - Export async function `getMyContracts(userId: string): Promise<ContractListItem[]>`
   - Use `Promise.all()` to fetch both rental and lease contracts in parallel (CRITICAL: per project CLAUDE.md performance rules)
   - Include vehicle with trim > generation > carModel > brand, and images (take 1)
   - Map each to `ContractListItem` with `contractType` discriminator
   - Build vehicleName: `${brand.name} ${carModel.name}`
   - Merge and sort by createdAt desc
   - Return unified list

3. Create `src/features/contracts/components/contract-card.tsx`:
   - 'use client' NOT needed -- this is a presentational server component
   - Props: `{ contract: ContractListItem }`
   - Layout per CONTEXT.md locked decision: compact card with flex row
     - Left: vehicle thumbnail (48x48 rounded, Next/Image with fill + object-cover, fallback gray bg)
     - Middle: vehicle name (font-medium), below: contract type label ("렌탈" | "리스") + pipe + monthly payment (formatKRW with monthly:true) in text-sm text-muted-foreground
     - Right: ContractStatusBadge + PDF download button
   - Download button: only show if status is APPROVED, ACTIVE, or COMPLETED
     - Use `<a>` tag with `href="/api/contracts/${contract.id}/pdf?type=${contract.contractType}"` and `target="_blank"` (mobile Safari compatibility)
     - Download icon from lucide-react (Download or FileDown)
     - Small icon button style: `size-8 rounded-md hover:bg-muted flex items-center justify-center`
   - Wrap entire card in `<Link href="/contracts/${contract.id}?type=${contract.contractType}">` for navigation
   - Prevent download button click from triggering card navigation: `onClick={(e) => e.stopPropagation()}` on the download link, and use `<a>` not `<Link>` for download
   - Actually, wrap card in Link but make download an `<a>` with stopPropagation

4. Create `src/features/contracts/components/contract-list.tsx`:
   - 'use client' -- handles tab state
   - Props: `{ contracts: ContractListItem[], initialTab?: string }`
   - Status filter tabs using searchParams pattern (Phase 4 admin reuse):
     - Tabs: "전체" (all), "진행중" (active), "완료" (completed), "취소" (canceled)
     - Use `useSearchParams()` and `useRouter()` for tab state in URL: `?tab=active`
     - Filter mapping:
       - all: show everything
       - active: DRAFT, PENDING_EKYC, PENDING_APPROVAL, APPROVED, ACTIVE
       - completed: COMPLETED
       - canceled: CANCELED
   - Tab UI: flex row with gap-2, each tab is a button with rounded-full px-4 py-1.5 text-sm, active tab gets bg-navy-900 text-white, inactive gets bg-gray-100 text-gray-700 hover:bg-gray-200
   - Filtered list renders ContractCard for each item
   - Empty state when filtered list is empty:
     - If tab is "all" (no contracts at all): icon (FileText from lucide), "아직 계약이 없습니다", CTA button "차량 둘러보기" linking to /vehicles
     - If filtered tab empty: "해당 상태의 계약이 없습니다" (no CTA)
  </action>
  <verify>
    <automated>cd /Users/kiyeol/development/navid/navid-auto-rental-lease && yarn type-check</automated>
  </verify>
  <done>ContractListItem type exported, getMyContracts fetches both contract types in parallel, ContractCard renders compact card with conditional download button, ContractList provides filter tabs with URL state</done>
</task>

<task type="auto">
  <name>Task 2: Expand my page with contract section and add PDF download to contract detail</name>
  <files>src/app/(protected)/mypage/page.tsx, src/app/(protected)/contracts/[id]/page.tsx</files>
  <action>
1. Rewrite `src/app/(protected)/mypage/page.tsx`:
   - Keep existing auth check (getCurrentUser, redirect if !user)
   - Keep existing profile Card section
   - Add contract list section below profile:
     - Fetch contracts: `const contracts = await getMyContracts(user.id)`
     - Section heading: "내 계약" with contract count badge
     - Render `<ContractList contracts={contracts} />`
   - Expand max-w from 2xl to 4xl for wider layout to accommodate contract cards
   - Update metadata title to remain "마이페이지 | 나비드오토"
   - Read `searchParams` from page props (for tab state pass-through): `searchParams: Promise<{ tab?: string }>`

2. Update `src/app/(protected)/contracts/[id]/page.tsx`:
   - Add PDF download button below the ContractStatusTracker
   - Only show for APPROVED, ACTIVE, or COMPLETED status
   - Button: "계약서 다운로드" with Download icon, primary style (bg-navy-900 text-white rounded-lg px-6 py-3)
   - Link as `<a href="/api/contracts/${contractId}/pdf?type=${contractType}" target="_blank" rel="noopener noreferrer">`
   - Add "마이페이지로 돌아가기" link at top (back navigation) linking to /mypage
   - Center the download button below status tracker with mt-8
  </action>
  <verify>
    <automated>cd /Users/kiyeol/development/navid/navid-auto-rental-lease && yarn type-check && yarn lint</automated>
  </verify>
  <done>My page shows profile section + contract list with filter tabs. Contract detail page has PDF download button for approved+ contracts. Both pages type-check and lint clean.</done>
</task>

</tasks>

<verification>
- `yarn type-check` passes
- `yarn lint` passes
- `yarn build` succeeds
- My page renders profile form and contract list sections
- Contract cards show vehicle info, type badge, status badge, monthly payment
- Filter tabs switch URL state and filter contract list
- Download button appears only on APPROVED/ACTIVE/COMPLETED contracts
- Contract detail page has download button and back link to mypage
</verification>

<success_criteria>
- Customer sees all their rental and lease contracts on /mypage
- Status filter tabs (All / In Progress / Completed / Canceled) work via URL searchParams
- Contract cards display vehicle name, type, status badge, monthly payment
- Clicking card navigates to /contracts/[id]?type=TYPE
- Download button on approved+ contracts opens PDF in new tab
- Empty state with CTA shows when no contracts exist
- Contract detail page has PDF download button
- All pages responsive (mobile card layout works)
</success_criteria>

<output>
After completion, create `.planning/phases/08-contract-completion-my-page/08-02-SUMMARY.md`
</output>
