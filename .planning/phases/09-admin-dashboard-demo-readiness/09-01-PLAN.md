---
phase: 09-admin-dashboard-demo-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/admin/dashboard/page.tsx
  - src/app/admin/dashboard/stats-cards.tsx
  - src/app/admin/dashboard/chart-section.tsx
  - src/app/admin/dashboard/recent-activity.tsx
  - src/app/admin/dashboard/loading.tsx
  - src/features/admin/actions/get-dashboard-stats.ts
  - src/app/layout.tsx
  - src/features/pricing/components/residual-value-table.tsx
  - src/features/pricing/components/residual-value-form.tsx
  - src/features/contracts/components/admin-contract-list.tsx
  - src/features/vehicles/components/vehicle-table.tsx
  - src/features/vehicles/components/approval-queue-table.tsx
  - src/components/ui/skeleton.tsx
autonomous: true
requirements:
  - ADMN-03
  - ADMN-02

must_haves:
  truths:
    - "Admin sees summary cards with vehicle count, active contracts, user count, pending approvals"
    - "Admin sees bar/line charts showing vehicle registrations and contract trends"
    - "Admin sees recent 5 contracts/vehicles as activity feed"
    - "Clicking pending approval card navigates to /admin/vehicles?tab=approval-queue"
    - "All existing alert() calls replaced with sonner toast notifications"
  artifacts:
    - path: "src/app/admin/dashboard/page.tsx"
      provides: "Server component with Prisma aggregation queries"
      exports: ["default"]
    - path: "src/app/admin/dashboard/stats-cards.tsx"
      provides: "Clickable stat cards with counts"
      contains: "use client"
    - path: "src/app/admin/dashboard/chart-section.tsx"
      provides: "Recharts bar/line charts"
      contains: "use client"
    - path: "src/app/admin/dashboard/recent-activity.tsx"
      provides: "Recent 5 activity items"
    - path: "src/features/admin/actions/get-dashboard-stats.ts"
      provides: "Dashboard stats aggregation function"
    - path: "src/components/ui/skeleton.tsx"
      provides: "Skeleton loading component"
  key_links:
    - from: "src/app/admin/dashboard/page.tsx"
      to: "src/features/admin/actions/get-dashboard-stats.ts"
      via: "server-side function call"
      pattern: "getDashboardStats"
    - from: "src/app/admin/dashboard/stats-cards.tsx"
      to: "/admin/vehicles?tab=approval-queue"
      via: "Link navigation on pending card click"
      pattern: "href.*approval-queue"
    - from: "src/app/layout.tsx"
      to: "sonner"
      via: "Toaster component in root layout"
      pattern: "Toaster"
---

<objective>
Build the admin stats dashboard with recharts charts, actionable summary cards, and recent activity feed. Install sonner and replace all alert() calls across the app with toast notifications. Install skeleton component.

Purpose: ADMN-03 requires a stats dashboard showing registered vehicle count, active contract count, and user count. The dashboard serves as the admin's operational hub and the primary investor demo screen.

Output: Fully functional admin dashboard page with charts, stats cards, activity feed. Global sonner toast setup replacing all 9 alert() calls.
</objective>

<execution_context>
@/Users/kiyeol/.claude-work/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-admin-dashboard-demo-readiness/09-CONTEXT.md
@.planning/phases/09-admin-dashboard-demo-readiness/09-RESEARCH.md

@src/app/admin/dashboard/page.tsx
@src/app/admin/layout.tsx
@src/app/layout.tsx
@src/components/layout/admin-sidebar.tsx
@src/features/vehicles/components/dealer-stats-sidebar.tsx
@src/lib/utils/format.ts

<interfaces>
<!-- Existing patterns the executor needs -->

From prisma/schema.prisma:
```prisma
enum VehicleStatus { AVAILABLE RESERVED RENTED LEASED MAINTENANCE HIDDEN }
enum ApprovalStatus { PENDING APPROVED REJECTED }
enum ContractStatus { DRAFT PENDING_EKYC PENDING_APPROVAL APPROVED ACTIVE COMPLETED CANCELED }
```

From src/lib/utils/format.ts:
```typescript
export function formatKRW(amount: number): string
export function formatDate(date: Date | string): string
```

Alert calls to replace (9 total, 5 files):
- residual-value-table.tsx: 3 calls (validation, update error, delete error)
- residual-value-form.tsx: 1 call (create error)
- admin-contract-list.tsx: 2 calls (approve error, reject error)
- vehicle-table.tsx: 2 calls (status change error, delete error)
- approval-queue-table.tsx: 1 call (batch action error)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies, add Skeleton component, setup sonner Toaster, replace all alert() calls</name>
  <files>
    src/components/ui/skeleton.tsx,
    src/app/layout.tsx,
    src/features/pricing/components/residual-value-table.tsx,
    src/features/pricing/components/residual-value-form.tsx,
    src/features/contracts/components/admin-contract-list.tsx,
    src/features/vehicles/components/vehicle-table.tsx,
    src/features/vehicles/components/approval-queue-table.tsx
  </files>
  <action>
    1. Install dependencies: `yarn add recharts sonner` and `npx shadcn@latest add skeleton -y`
    2. Add sonner Toaster to root layout (src/app/layout.tsx): import { Toaster } from 'sonner', add `<Toaster position="top-right" richColors closeButton />` inside body after {children}
    3. Replace ALL 9 alert() calls with sonner toast:
       - In each file, add `import { toast } from 'sonner'`
       - `alert('1~99 사이의 값을 입력해주세요.')` → `toast.error('1~99 사이의 값을 입력해주세요.')`
       - `alert(result.error)` → `toast.error(result.error)`
       - For success cases where alert was used, use `toast.success()`
    4. After replacement, verify zero alert() calls remain: `grep -r "alert(" src/ --include="*.tsx" --include="*.ts"` should return nothing
  </action>
  <verify>
    <automated>cd /Users/kiyeol/development/navid/navid-auto-rental-lease && grep -rn "alert(" src/ --include="*.tsx" --include="*.ts" | grep -v "// " | grep -v "node_modules" && echo "FAIL: alert() calls still exist" || echo "PASS: no alert() calls" && yarn type-check</automated>
  </verify>
  <done>recharts and sonner installed. Skeleton component exists. Toaster in root layout. Zero alert() calls in src/. Type check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Build admin stats dashboard with charts, stat cards, and activity feed</name>
  <files>
    src/features/admin/actions/get-dashboard-stats.ts,
    src/app/admin/dashboard/page.tsx,
    src/app/admin/dashboard/stats-cards.tsx,
    src/app/admin/dashboard/chart-section.tsx,
    src/app/admin/dashboard/recent-activity.tsx,
    src/app/admin/dashboard/loading.tsx
  </files>
  <action>
    1. Create `src/features/admin/actions/get-dashboard-stats.ts`:
       - Export async function `getDashboardStats()` that runs all queries in parallel with Promise.all():
         - prisma.vehicle.count({ where: { status: { not: 'HIDDEN' } } })
         - prisma.vehicle.count({ where: { approvalStatus: 'PENDING' } })
         - prisma.profile.count()
         - prisma.rentalContract.groupBy({ by: ['status'], _count: true })
         - prisma.leaseContract.groupBy({ by: ['status'], _count: true })
         - Recent 5 rental + lease contracts (orderBy createdAt desc, take 5 each) with vehicle trim->generation->carModel->brand and customer name
         - Monthly vehicle registration counts (last 6 months) via raw SQL or groupBy with createdAt filtering
         - Monthly contract counts (last 6 months)
       - Return typed object with: vehicleCount, pendingApprovals, userCount, contractsByStatus (merged rental+lease), recentActivity (sorted by date, take 5), monthlyVehicles, monthlyContracts
       - Calculate active contracts = ACTIVE status count from both rental and lease

    2. Create `src/app/admin/dashboard/stats-cards.tsx` ('use client'):
       - 4 cards in a responsive grid (grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4)
       - Cards: 전체 차량 (Car icon, vehicleCount), 활성 계약 (FileText icon, activeContracts), 전체 사용자 (Users icon, userCount), 승인 대기 (Clock icon, pendingApprovals with orange accent)
       - Each card uses shadcn Card component with hover effect
       - 승인 대기 card wraps in Link to /admin/vehicles?tab=approval-queue
       - 활성 계약 card wraps in Link to /admin/contracts
       - Use formatKRW for any monetary values if applicable

    3. Create `src/app/admin/dashboard/chart-section.tsx` ('use client'):
       - Import recharts via next/dynamic with { ssr: false } to avoid hydration issues and reduce bundle
       - Two charts side by side on lg, stacked on mobile (grid-cols-1 lg:grid-cols-2 gap-6)
       - Left: BarChart showing monthly vehicle registrations (last 6 months), bar color = primary
       - Right: LineChart showing monthly contracts (last 6 months), line color = accent blue
       - Both wrapped in ResponsiveContainer with height={300}
       - Korean month labels (1월, 2월, etc.)
       - Wrapped in shadcn Card with title headers

    4. Create `src/app/admin/dashboard/recent-activity.tsx`:
       - Server component (no 'use client' needed) or client component based on need
       - Display last 5 activities as a list in a Card
       - Each item: contract type badge (렌탈/리스), vehicle name (brand + model), customer name, status badge, relative time
       - Link each item to /admin/contracts
       - Title: "최근 활동"

    5. Update `src/app/admin/dashboard/page.tsx`:
       - Make it a Server Component with `export const dynamic = 'force-dynamic'`
       - Call getDashboardStats() at top level
       - Layout: page title "관리자 대시보드" at top, then StatsCards, then ChartSection, then RecentActivity
       - Pass serialized data as props to client components
       - Add Suspense boundary around the whole content with a loading skeleton fallback

    6. Create `src/app/admin/dashboard/loading.tsx`:
       - Next.js automatic loading UI using Skeleton components
       - 4 skeleton cards at top (matching stats cards), 2 skeleton rectangles for charts, list skeleton for activity
  </action>
  <verify>
    <automated>cd /Users/kiyeol/development/navid/navid-auto-rental-lease && yarn type-check && yarn build 2>&1 | tail -5</automated>
  </verify>
  <done>Admin dashboard shows 4 stat cards with real counts, 2 charts with monthly trends, recent activity feed. Pending approval card links to approval queue. Dashboard skeleton loading shows during data fetch. Build succeeds without errors.</done>
</task>

</tasks>

<verification>
1. `yarn type-check` passes
2. `yarn build` succeeds
3. `grep -rn "alert(" src/` returns no results
4. Dashboard page renders at /admin/dashboard with stats cards, charts, activity feed
5. Clicking "승인 대기" card navigates to /admin/vehicles?tab=approval-queue
</verification>

<success_criteria>
- Admin dashboard displays vehicle count, active contract count, user count, pending approvals as clickable cards
- Bar chart shows monthly vehicle registrations, line chart shows monthly contracts
- Recent 5 activities listed with contract type, vehicle, customer, status
- Sonner toasts replace all 9 former alert() calls
- Skeleton loading component available for use in subsequent plans
- Build and type-check pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-admin-dashboard-demo-readiness/09-01-SUMMARY.md`
</output>
