---
phase: 09-admin-dashboard-demo-readiness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/admin/actions/soft-delete-vehicle.ts
  - src/features/admin/actions/update-vehicle-admin.ts
  - src/features/admin/actions/deactivate-user.ts
  - src/features/admin/components/vehicle-edit-sheet.tsx
  - src/app/admin/vehicles/page.tsx
  - src/features/vehicles/components/vehicle-table.tsx
  - src/app/admin/users/page.tsx
  - src/app/admin/users/role-select.tsx
  - src/app/admin/contracts/page.tsx
  - src/features/contracts/components/admin-contract-list.tsx
autonomous: true
requirements:
  - ADMN-01
  - ADMN-04

must_haves:
  truths:
    - "Admin can click a vehicle table row to open a Sheet with editable vehicle details"
    - "Admin can soft-delete a vehicle (status changes to HIDDEN, vehicle disappears from public)"
    - "Admin can deactivate a user account"
    - "Admin can approve/reject/cancel contracts from the contracts page"
    - "Residual value table management is functional (already implemented, verified)"
  artifacts:
    - path: "src/features/admin/actions/soft-delete-vehicle.ts"
      provides: "Server action for vehicle soft delete"
      exports: ["softDeleteVehicle"]
    - path: "src/features/admin/actions/update-vehicle-admin.ts"
      provides: "Server action for admin vehicle editing"
      exports: ["updateVehicleAdmin"]
    - path: "src/features/admin/actions/deactivate-user.ts"
      provides: "Server action for user deactivation"
      exports: ["deactivateUser"]
    - path: "src/features/admin/components/vehicle-edit-sheet.tsx"
      provides: "Sheet slide-out panel for vehicle editing"
      contains: "use client"
  key_links:
    - from: "src/features/vehicles/components/vehicle-table.tsx"
      to: "src/features/admin/components/vehicle-edit-sheet.tsx"
      via: "row click opens Sheet"
      pattern: "VehicleEditSheet"
    - from: "src/features/admin/components/vehicle-edit-sheet.tsx"
      to: "src/features/admin/actions/update-vehicle-admin.ts"
      via: "form submit calls server action"
      pattern: "updateVehicleAdmin"
    - from: "src/features/vehicles/components/vehicle-table.tsx"
      to: "src/features/admin/actions/soft-delete-vehicle.ts"
      via: "delete button calls server action"
      pattern: "softDeleteVehicle"
---

<objective>
Complete admin CRUD capabilities: vehicle inline edit via Sheet, soft delete (HIDDEN status), user deactivation, and verify existing contract management and residual value table.

Purpose: ADMN-01 requires admin can view/edit/delete all vehicles, contracts, users. ADMN-04 (residual value) already works from Phase 6 but needs verification in the admin context.

Output: Full admin CRUD operations for vehicles (edit sheet + soft delete), users (deactivation), and contracts (status transitions). All existing admin pages enhanced.
</objective>

<execution_context>
@/Users/kiyeol/.claude-work/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-admin-dashboard-demo-readiness/09-CONTEXT.md
@.planning/phases/09-admin-dashboard-demo-readiness/09-RESEARCH.md

@src/app/admin/vehicles/page.tsx
@src/app/admin/users/page.tsx
@src/app/admin/users/role-select.tsx
@src/app/admin/contracts/page.tsx
@src/features/vehicles/components/vehicle-table.tsx
@src/features/contracts/components/admin-contract-list.tsx
@src/components/ui/sheet.tsx
@prisma/schema.prisma

<interfaces>
<!-- Key types executor needs -->

From prisma/schema.prisma:
```prisma
enum VehicleStatus { AVAILABLE RESERVED RENTED LEASED MAINTENANCE HIDDEN }
enum ApprovalStatus { PENDING APPROVED REJECTED }
enum ContractStatus { DRAFT PENDING_EKYC PENDING_APPROVAL APPROVED ACTIVE COMPLETED CANCELED }
enum Role { CUSTOMER DEALER ADMIN }

model Vehicle {
  id, trimId, dealerId, year, mileage, color, price, status, approvalStatus, description, ...
}

model Profile {
  id, email, name, phone, role, createdAt, updatedAt
}
```

From src/components/ui/sheet.tsx:
```typescript
// shadcn Sheet with base-ui render prop pattern (not asChild)
export { Sheet, SheetContent, SheetHeader, SheetTitle, SheetTrigger, SheetClose }
```

Established patterns:
- Server Actions: getCurrentUser() + role check + prisma operation
- Result: { success: true } | { error: string }
- Toast: import { toast } from 'sonner' (after Plan 01 installs)
- Admin pages: export const dynamic = 'force-dynamic'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create admin vehicle edit Sheet, soft delete action, and update vehicle action</name>
  <files>
    src/features/admin/actions/soft-delete-vehicle.ts,
    src/features/admin/actions/update-vehicle-admin.ts,
    src/features/admin/components/vehicle-edit-sheet.tsx,
    src/features/vehicles/components/vehicle-table.tsx,
    src/app/admin/vehicles/page.tsx
  </files>
  <action>
    1. Create `src/features/admin/actions/soft-delete-vehicle.ts`:
       - 'use server' Server Action
       - getCurrentUser() → verify ADMIN role
       - prisma.vehicle.update({ where: { id }, data: { status: 'HIDDEN' } })
       - revalidatePath('/admin/vehicles')
       - Return { success: true } | { error: string }

    2. Create `src/features/admin/actions/update-vehicle-admin.ts`:
       - 'use server' Server Action
       - Accept vehicleId + update data (year, mileage, color, price, description, status)
       - getCurrentUser() → verify ADMIN role
       - Validate with Zod schema (year: 2000-2026, mileage: >= 0, price: >= 0)
       - prisma.vehicle.update(...)
       - revalidatePath('/admin/vehicles')
       - Return { success: true } | { error: string }

    3. Create `src/features/admin/components/vehicle-edit-sheet.tsx` ('use client'):
       - Props: vehicle data object, open state, onOpenChange callback
       - Sheet slide-out from right side with form fields:
         - Year (number input), Mileage (number input), Color (text), Price (number input, display in 만원)
         - Status select (VehicleStatus enum options except HIDDEN)
         - Description (textarea)
       - Use react-hook-form + Zod for validation
       - Submit calls updateVehicleAdmin server action
       - On success: toast.success('차량 정보가 수정되었습니다.'), close sheet
       - On error: toast.error(result.error)

    4. Update `src/features/vehicles/components/vehicle-table.tsx`:
       - Add row click handler that opens VehicleEditSheet with selected vehicle data (admin role only)
       - Add "삭제" button (Trash icon) in each row's action column for admin (calls softDeleteVehicle)
       - After soft delete success: toast.success('차량이 삭제되었습니다.'), router.refresh()
       - Confirm before delete with a simple confirm dialog or inline confirmation
       - Non-admin users (dealers) keep existing behavior unchanged

    5. Verify admin vehicles page at /admin/vehicles still works with the enhanced table
  </action>
  <verify>
    <automated>cd /Users/kiyeol/development/navid/navid-auto-rental-lease && yarn type-check && yarn test 2>&1 | tail -10</automated>
  </verify>
  <done>Admin can click vehicle row to open edit Sheet, modify fields, save. Admin can soft-delete vehicles (HIDDEN status). Type check and tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add user deactivation and enhance contracts admin page</name>
  <files>
    src/features/admin/actions/deactivate-user.ts,
    src/app/admin/users/page.tsx,
    src/app/admin/users/role-select.tsx,
    src/app/admin/contracts/page.tsx,
    src/features/contracts/components/admin-contract-list.tsx
  </files>
  <action>
    1. Create `src/features/admin/actions/deactivate-user.ts`:
       - 'use server' Server Action
       - getCurrentUser() → verify ADMIN role
       - Deactivation approach: Add isActive boolean field concept -- but since Profile schema may not have isActive, use role downgrade to CUSTOMER + add "(비활성)" suffix to name as simple approach. OR if schema supports it, add isActive field.
       - Check: If Profile model has no isActive field, use simpler approach: keep the user but this is primarily for demo. Just implement a button that shows toast "사용자가 비활성화되었습니다" and visually marks the row. For a real implementation, add migration for isActive column.
       - Practical approach for demo: Create the action that updates a user's role or marks them, revalidatePath('/admin/users')
       - Return { success: true } | { error: string }

    2. Enhance `src/app/admin/users/page.tsx`:
       - Add user count display at top
       - Add role filter tabs (전체, 고객, 딜러, 관리자) using searchParams tab pattern
       - Display user table with columns: name, email, phone, role (with RoleSelect), createdAt, actions
       - Actions column: "비활성화" button that calls deactivateUser
       - After deactivation: toast.success('사용자가 비활성화되었습니다.')
       - Mobile: Convert to card layout on small screens (responsive)

    3. Verify `src/app/admin/contracts/page.tsx` and `admin-contract-list.tsx`:
       - Ensure contract approve/reject/cancel still works correctly
       - The alert() calls were already replaced with toast in Plan 01
       - Add a cancel button for contracts in PENDING_APPROVAL or APPROVED status (transition to CANCELED)
       - If cancel action doesn't exist, create a simple cancelContract server action following the existing approve/reject pattern

    4. Add mobile-responsive improvements to admin pages:
       - Vehicle table: horizontal scroll wrapper on mobile with min-width
       - User list: card layout on mobile (md:hidden card, hidden md:table-row)
       - Contract list: compact card layout on mobile
  </action>
  <verify>
    <automated>cd /Users/kiyeol/development/navid/navid-auto-rental-lease && yarn type-check && yarn build 2>&1 | tail -5</automated>
  </verify>
  <done>Admin can deactivate users. User list has role filter tabs and mobile card layout. Contract management includes cancel action. All admin pages are mobile-responsive. Build succeeds.</done>
</task>

</tasks>

<verification>
1. `yarn type-check` passes
2. `yarn build` succeeds
3. Vehicle edit Sheet opens on row click, saves changes via server action
4. Vehicle soft delete changes status to HIDDEN
5. User deactivation works from admin users page
6. Contract cancel action works for appropriate statuses
7. All admin pages render correctly on mobile viewport
</verification>

<success_criteria>
- Admin can view, edit (Sheet), and soft-delete vehicles from /admin/vehicles
- Admin can view users with role filters and deactivate users from /admin/users
- Admin can approve/reject/cancel contracts from /admin/contracts
- Residual value management works at /admin/residual-value (existing, verified)
- All admin pages have mobile-responsive layouts
- Build and type-check pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-admin-dashboard-demo-readiness/09-02-SUMMARY.md`
</output>
