---
phase: 09-admin-dashboard-demo-readiness
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
  - 09-02
files_modified:
  - prisma/seed.ts
  - src/app/admin/vehicles/loading.tsx
  - src/app/admin/contracts/loading.tsx
  - src/app/admin/users/loading.tsx
  - src/app/admin/residual-value/loading.tsx
  - src/app/(marketing)/vehicles/loading.tsx
  - src/app/(marketing)/vehicles/[id]/loading.tsx
  - src/components/shared/empty-state.tsx
  - DEMO.md
autonomous: false

must_haves:
  truths:
    - "Demo seed creates 20+ vehicles, 3 dealer accounts, 5 customer accounts, contracts in all 7 statuses"
    - "Demo accounts can log in with fixed credentials (admin@navid.kr etc.)"
    - "All admin pages show skeleton loading during data fetch"
    - "Empty states show icon + message + CTA button when no data"
    - "All monetary values display in KRW format, dates in Korean format"
    - "DEMO.md contains step-by-step walkthrough for investor presentation"
  artifacts:
    - path: "prisma/seed.ts"
      provides: "Full demo seed with auth users, profiles, vehicles, contracts"
      contains: "supabase.auth.admin"
    - path: "src/components/shared/empty-state.tsx"
      provides: "Reusable empty state component"
      exports: ["EmptyState"]
    - path: "DEMO.md"
      provides: "Demo walkthrough checklist"
      contains: "customer journey"
  key_links:
    - from: "prisma/seed.ts"
      to: "Supabase Auth"
      via: "admin.createUser API for demo accounts"
      pattern: "auth.admin.createUser"
    - from: "src/app/admin/*/loading.tsx"
      to: "src/components/ui/skeleton.tsx"
      via: "Skeleton component import"
      pattern: "Skeleton"

user_setup:
  - service: supabase
    why: "Seed script needs service_role key for auth.admin.createUser()"
    env_vars:
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard -> Settings -> API -> service_role key (secret)"
    dashboard_config: []
---

<objective>
Create comprehensive demo seed data with loginable accounts, add skeleton loading screens and empty states to all pages, audit KRW/date formatting, and write DEMO.md walkthrough.

Purpose: The platform must be demo-ready for investor presentations. This means realistic data, polished loading states, consistent formatting, and a documented walkthrough script.

Output: Expanded seed.ts with full lifecycle data, loading.tsx files for all key pages, EmptyState component, format audit fixes, DEMO.md.
</objective>

<execution_context>
@/Users/kiyeol/.claude-work/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-admin-dashboard-demo-readiness/09-CONTEXT.md
@.planning/phases/09-admin-dashboard-demo-readiness/09-RESEARCH.md
@.planning/phases/09-admin-dashboard-demo-readiness/09-01-SUMMARY.md
@.planning/phases/09-admin-dashboard-demo-readiness/09-02-SUMMARY.md

@prisma/seed.ts
@prisma/schema.prisma
@src/lib/utils/format.ts
@src/components/ui/skeleton.tsx

<interfaces>
<!-- Key patterns from prior plans -->

From prisma/schema.prisma:
```prisma
enum ContractStatus { DRAFT PENDING_EKYC PENDING_APPROVAL APPROVED ACTIVE COMPLETED CANCELED }
enum ContractType { RENTAL LEASE }
enum VehicleStatus { AVAILABLE RESERVED RENTED LEASED MAINTENANCE HIDDEN }
enum Role { CUSTOMER DEALER ADMIN }
```

Existing seed.ts creates:
- 3 profiles (1 admin, 2 dealers) with fixed UUIDs
- Brand/Model/Generation/Trim hierarchy (8 brands, 20+ models)
- 50+ vehicles with Unsplash images
- 26 residual value rates
- Does NOT create: customer profiles, contracts, Supabase Auth users

Demo account spec (from CONTEXT.md):
- admin@navid.kr (admin)
- dealer1@navid.kr, dealer2@navid.kr, dealer3@navid.kr (dealers)
- customer1@navid.kr through customer5@navid.kr (customers)
- Shared password for all demo accounts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand seed script with demo accounts (Supabase Auth), customer profiles, and contracts in all states</name>
  <files>prisma/seed.ts</files>
  <action>
    1. Add Supabase Admin client setup at top of seed.ts:
       - Import createClient from @supabase/supabase-js
       - Create admin client with SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY from process.env
       - Add helper function `ensureAuthUser(email, password, metadata)` that uses supabase.auth.admin.createUser() with email_confirm: true, or fetches existing user by email if already exists (idempotent)

    2. Update existing profile creation to use Supabase Auth:
       - For admin: ensureAuthUser('admin@navid.kr', 'navid1234!', { role: 'ADMIN', name: '관리자' })
       - For dealers: dealer1@navid.kr ('서울모터스'), dealer2@navid.kr ('강남오토'), dealer3@navid.kr ('부산카센터') -- 3 dealers per spec
       - For customers: customer1@navid.kr ('김민수'), customer2@navid.kr ('이영희'), customer3@navid.kr ('박정훈'), customer4@navid.kr ('최수진'), customer5@navid.kr ('정대한') -- 5 customers per spec
       - All use shared password: 'navid1234!'
       - Use the returned auth user ID as Profile.id (maps Supabase Auth to Profile)
       - Profile upsert with correct role

    3. Add contract seed data after vehicles:
       - Pick random vehicles and customers
       - Create contracts covering ALL ContractStatus values:
         - 2x DRAFT (1 rental, 1 lease)
         - 1x PENDING_EKYC (rental)
         - 2x PENDING_APPROVAL (1 rental, 1 lease)
         - 2x APPROVED (1 rental, 1 lease)
         - 3x ACTIVE (2 rental, 1 lease) -- most common state for demo
         - 2x COMPLETED (1 rental, 1 lease)
         - 1x CANCELED (lease)
       - Total: ~13 contracts across both types
       - For ACTIVE/COMPLETED contracts, set realistic dates (startDate in past, endDate in future for ACTIVE)
       - Use realistic Korean pricing: monthly 300,000~800,000원, deposit 3,000,000~10,000,000원
       - For lease: include residual value percentage from seed rates
       - Create EkycVerification records for contracts past PENDING_EKYC stage

    4. Add seed summary section showing contract counts by status

    5. Make seed idempotent: Use upsert where possible, handle existing auth users gracefully (catch "User already registered" and fetch existing user instead)

    6. Graceful fallback: If SUPABASE_SERVICE_ROLE_KEY is not set, skip auth user creation and log a warning, still create profiles with fixed UUIDs (preserves existing behavior for environments without the key)
  </action>
  <verify>
    <automated>cd /Users/kiyeol/development/navid/navid-auto-rental-lease && yarn type-check && grep -c "customer" prisma/seed.ts && grep -c "Contract" prisma/seed.ts</automated>
  </verify>
  <done>Seed script creates 9 Supabase Auth accounts (1 admin, 3 dealers, 5 customers), matching profiles, 13+ contracts in all statuses. Script is idempotent and gracefully handles missing service_role key.</done>
</task>

<task type="auto">
  <name>Task 2: Add skeleton loading screens, empty states, format audit, and DEMO.md</name>
  <files>
    src/components/shared/empty-state.tsx,
    src/app/admin/vehicles/loading.tsx,
    src/app/admin/contracts/loading.tsx,
    src/app/admin/users/loading.tsx,
    src/app/admin/residual-value/loading.tsx,
    src/app/(marketing)/vehicles/loading.tsx,
    src/app/(marketing)/vehicles/[id]/loading.tsx,
    DEMO.md
  </files>
  <action>
    1. Create `src/components/shared/empty-state.tsx`:
       - Reusable component: Props = { icon: LucideIcon, title: string, description: string, action?: { label: string, href: string } }
       - Centered layout with icon (size-12, text-muted-foreground), title (text-lg font-semibold), description (text-muted-foreground), optional Button as Link
       - Used across pages when no data found

    2. Create loading.tsx files for admin pages:
       - Each loading.tsx exports a default function with Skeleton components matching the page layout
       - `/admin/vehicles/loading.tsx`: Page title skeleton + filter tabs skeleton + table rows skeleton (6 rows)
       - `/admin/contracts/loading.tsx`: Title skeleton + tab skeleton + 6 card/row skeletons
       - `/admin/users/loading.tsx`: Title skeleton + tab skeleton + 6 user row skeletons
       - `/admin/residual-value/loading.tsx`: Title skeleton + form skeleton + table skeleton
       - All use Skeleton from '@/components/ui/skeleton'

    3. Create loading.tsx files for public pages:
       - `/(marketing)/vehicles/loading.tsx`: Filter sidebar skeleton + grid of 6 card skeletons
       - `/(marketing)/vehicles/[id]/loading.tsx`: Image gallery skeleton + detail panel skeleton

    4. Format audit -- search for raw number/date displays and ensure formatKRW/formatDate are used:
       - `grep -rn "toLocaleString\|\.toFixed\|new Date.*toString" src/ --include="*.tsx"` to find candidates
       - Verify all price displays use formatKRW()
       - Verify all date displays use formatDate()
       - Fix any inconsistencies found

    5. Add EmptyState usage to key pages where data might be empty:
       - Admin vehicles: "등록된 차량이 없습니다" + "차량 등록하기" CTA (if no vehicles)
       - Admin contracts: "계약이 없습니다" (if no contracts)
       - Admin users: "사용자가 없습니다" (if no users)
       - Public vehicle search: "검색 결과가 없습니다" + "필터 초기화" CTA (if no results)
       - Integration: Wrap list rendering with `{items.length === 0 ? <EmptyState ... /> : <List ... />}` pattern

    6. Create `DEMO.md` at project root:
       - Title: "Navid Auto 데모 가이드"
       - Demo accounts section with all emails and shared password
       - Customer journey walkthrough (numbered steps):
         1. Visit landing page, verify hero + featured vehicles
         2. Click "차량 검색" or use quick search
         3. Apply filters (brand: Hyundai, year: 2023+)
         4. Click a vehicle card -- vehicle detail page
         5. Click "렌탈 신청" -- contract wizard
         6. Complete eKYC step
         7. Review and submit
         8. Navigate to 마이페이지 -- see contract in list
         9. Download PDF
       - Admin journey walkthrough:
         1. Login as admin@navid.kr
         2. View dashboard (stats, charts, activity)
         3. Click 승인 대기 -- approval queue
         4. Approve a vehicle
         5. Navigate to 계약 관리 -- approve a contract
         6. Check 사용자 관리 -- view users by role
         7. Check 잔존가치 관리 -- edit a rate
       - Dealer journey (brief):
         1. Login as dealer1@navid.kr
         2. View dealer dashboard
         3. Register a new vehicle
       - Mobile testing checklist (key pages to verify on 375px)
       - Known limitations section (v2 items)
  </action>
  <verify>
    <automated>cd /Users/kiyeol/development/navid/navid-auto-rental-lease && yarn type-check && test -f DEMO.md && echo "DEMO.md exists" && ls src/app/admin/*/loading.tsx 2>/dev/null | wc -l</automated>
  </verify>
  <done>All admin and key public pages have skeleton loading screens. EmptyState component used across pages. DEMO.md contains complete walkthrough. Format audit complete -- all prices use formatKRW, dates use formatDate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of complete Phase 9 demo readiness</name>
  <action>
    Verify the complete admin dashboard, CRUD operations, demo seed data, loading states, toasts, and DEMO.md walkthrough.

    What was built: Complete admin dashboard with stats/charts, full CRUD for vehicles/contracts/users, demo seed data with loginable accounts, skeleton loading screens, empty states, sonner toasts, and DEMO.md walkthrough guide.

    How to verify:
    1. Run `yarn dev` and visit http://localhost:3000/admin/dashboard
       - Verify: 4 stat cards with counts, 2 charts, recent activity feed
       - Click "승인 대기" card -- should navigate to approval queue
    2. Visit /admin/vehicles
       - Click a row -- Sheet opens with editable fields
       - Test soft delete (trash icon) on a vehicle
    3. Visit /admin/users -- verify role filter tabs, deactivation button
    4. Visit /admin/contracts -- verify approve/reject/cancel actions
    5. Test on mobile viewport (375px) -- verify cards stack, charts scroll, tables responsive
    6. Check loading states: hard refresh any admin page, observe skeleton placeholders
    7. Verify toast notifications: trigger an error action -- see sonner toast (no browser alert)
    8. Follow DEMO.md customer journey end-to-end
    9. Verify: all prices show KRW format, dates show Korean format
  </action>
  <verify>Manual human verification</verify>
  <done>User confirms all Phase 9 features work correctly on desktop and mobile. Type "approved" or describe issues to fix.</done>
</task>

</tasks>

<verification>
1. `yarn type-check` passes
2. `yarn build` succeeds
3. `yarn db:seed` completes without errors (if SUPABASE_SERVICE_ROLE_KEY available)
4. Demo accounts can log in
5. All admin pages show skeleton loading
6. Empty states render when no data
7. DEMO.md exists with complete walkthrough
8. Manual demo walkthrough completes without errors on desktop and mobile
</verification>

<success_criteria>
- Seed script creates 20+ vehicles, 3 dealers, 5 customers, 13+ contracts in all statuses
- Demo accounts (admin@navid.kr, dealer1@navid.kr, customer1@navid.kr) can log in with shared password
- All admin pages and key public pages have skeleton loading screens
- Empty states show icon + message + CTA when no data
- All monetary values in KRW format, dates in Korean format throughout the app
- DEMO.md provides complete investor presentation walkthrough
- End-to-end demo flow works on desktop and mobile without errors
</success_criteria>

<output>
After completion, create `.planning/phases/09-admin-dashboard-demo-readiness/09-03-SUMMARY.md`
</output>
