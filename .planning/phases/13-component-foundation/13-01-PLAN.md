---
phase: 13-component-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - yarn.lock
  - src/components/ui/accordion.tsx
  - src/components/ui/tabs.tsx
  - src/components/ui/carousel.tsx
  - src/components/ui/collapsible.tsx
  - src/components/ui/progress.tsx
  - src/components/ui/pagination.tsx
  - src/components/ui/popover.tsx
  - src/components/ui/scroll-area.tsx
  - src/components/ui/avatar.tsx
  - src/components/ui/breadcrumb.tsx
  - src/components/ui/toggle-group.tsx
  - src/components/ui/radio-group.tsx
  - src/components/ui/dropdown-menu.tsx
  - tests/unit/features/component-foundation/packages.test.ts
  - tests/unit/features/component-foundation/shadcn-components.test.ts
autonomous: true
requirements:
  - COMP-01
  - COMP-02

must_haves:
  truths:
    - "Embla autoplay, auto-scroll, YARL lightbox, and react-intersection-observer are importable without errors"
    - "13 new shadcn/ui components render without errors in a test environment"
    - "Existing 16 shadcn components are unchanged (no git diff on existing ui/ files)"
  artifacts:
    - path: "src/components/ui/carousel.tsx"
      provides: "Embla-backed Carousel component"
    - path: "src/components/ui/accordion.tsx"
      provides: "Accordion component for expandable sections"
    - path: "src/components/ui/tabs.tsx"
      provides: "Tabs component for tabbed content"
    - path: "tests/unit/features/component-foundation/packages.test.ts"
      provides: "Import smoke tests for COMP-01 packages"
    - path: "tests/unit/features/component-foundation/shadcn-components.test.ts"
      provides: "Render tests for COMP-02 shadcn components"
  key_links:
    - from: "package.json"
      to: "src/components/ui/carousel.tsx"
      via: "embla-carousel dependency"
      pattern: "embla-carousel-react"
    - from: "src/components/ui/carousel.tsx"
      to: "embla-carousel-autoplay"
      via: "plugin compatibility"
      pattern: "embla-carousel-autoplay"
---

<objective>
Install all new npm packages (Embla plugins, YARL lightbox, react-intersection-observer) and add 13 shadcn/ui components required by downstream Phases 14-17.

Purpose: Establish the package and component foundation so all subsequent page-level work can import these without installation steps.
Output: 13 new shadcn component files, 4 new npm packages in package.json, test files proving importability and renderability.
</objective>

<execution_context>
@/Users/kiyeol/.claude-work/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-component-foundation/13-CONTEXT.md
@.planning/phases/13-component-foundation/13-RESEARCH.md

<interfaces>
<!-- Existing shadcn config (components.json) -->
{
  "style": "base-nova",
  "rsc": true,
  "tsx": true,
  "aliases": { "ui": "@/components/ui" }
}

<!-- Existing 16 shadcn components (DO NOT re-add or modify) -->
badge.tsx, button.tsx, card.tsx, checkbox.tsx, dialog.tsx, input.tsx,
label.tsx, navigation-menu.tsx, select.tsx, separator.tsx, sheet.tsx,
skeleton.tsx, slider.tsx, table.tsx, textarea.tsx, tooltip.tsx

<!-- Package manager: yarn (npm forbidden per CLAUDE.md) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install packages and shadcn components</name>
  <files>
    package.json,
    yarn.lock,
    src/components/ui/carousel.tsx,
    src/components/ui/accordion.tsx,
    src/components/ui/tabs.tsx,
    src/components/ui/collapsible.tsx,
    src/components/ui/progress.tsx,
    src/components/ui/pagination.tsx,
    src/components/ui/popover.tsx,
    src/components/ui/scroll-area.tsx,
    src/components/ui/avatar.tsx,
    src/components/ui/breadcrumb.tsx,
    src/components/ui/toggle-group.tsx,
    src/components/ui/radio-group.tsx,
    src/components/ui/dropdown-menu.tsx
  </files>
  <read_first>
    package.json,
    components.json,
    src/components/ui/button.tsx
  </read_first>
  <action>
NOTE: 13 of the 15 files_modified are auto-generated by a single `npx shadcn@latest add` command -- no manual authoring required for those.

Step 1: Install shadcn Carousel FIRST (auto-installs embla-carousel + embla-carousel-react):
```bash
npx shadcn@latest add carousel --yes --overwrite
```

Step 2: Install Embla plugins (must match Embla core major version 8.x):
```bash
yarn add embla-carousel-autoplay embla-carousel-auto-scroll
```

Step 3: Install YARL lightbox:
```bash
yarn add yet-another-react-lightbox
```

Step 4: Install react-intersection-observer:
```bash
yarn add react-intersection-observer
```

Step 5: Install remaining 12 shadcn components in batch:
```bash
npx shadcn@latest add accordion tabs collapsible progress pagination popover scroll-area avatar breadcrumb toggle-group radio-group dropdown-menu --yes --overwrite
```

Step 6: Verify no existing components were modified:
```bash
git diff --name-only src/components/ui/badge.tsx src/components/ui/button.tsx src/components/ui/card.tsx src/components/ui/checkbox.tsx src/components/ui/dialog.tsx src/components/ui/input.tsx src/components/ui/label.tsx src/components/ui/navigation-menu.tsx src/components/ui/select.tsx src/components/ui/separator.tsx src/components/ui/sheet.tsx src/components/ui/skeleton.tsx src/components/ui/slider.tsx src/components/ui/table.tsx src/components/ui/textarea.tsx src/components/ui/tooltip.tsx
```
If any existing files show changes, `git checkout` those files to restore originals.

IMPORTANT: Use `npx shadcn@latest` (NOT `npx shadcn-ui@latest`). The project uses shadcn v4.0.2.
IMPORTANT: Use `yarn add` (NOT `npm install`). Project uses yarn exclusively.
IMPORTANT: Do NOT re-add any of the existing 16 components listed in the interfaces block above.
  </action>
  <verify>
    <automated>ls src/components/ui/carousel.tsx src/components/ui/accordion.tsx src/components/ui/tabs.tsx src/components/ui/collapsible.tsx src/components/ui/progress.tsx src/components/ui/pagination.tsx src/components/ui/popover.tsx src/components/ui/scroll-area.tsx src/components/ui/avatar.tsx src/components/ui/breadcrumb.tsx src/components/ui/toggle-group.tsx src/components/ui/radio-group.tsx src/components/ui/dropdown-menu.tsx && node --input-type=module -e "import 'embla-carousel-autoplay'; import 'embla-carousel-auto-scroll'; import 'yet-another-react-lightbox'; import 'react-intersection-observer'; console.log('All packages importable')"</automated>
  </verify>
  <acceptance_criteria>
    - 13 new files exist in src/components/ui/ (accordion, tabs, carousel, collapsible, progress, pagination, popover, scroll-area, avatar, breadcrumb, toggle-group, radio-group, dropdown-menu)
    - package.json contains "embla-carousel-autoplay", "embla-carousel-auto-scroll", "yet-another-react-lightbox", "react-intersection-observer"
    - All Embla packages share major version 8.x (check: grep embla-carousel yarn.lock)
    - Existing 16 component files show zero git diff
  </acceptance_criteria>
  <done>4 npm packages installed, 13 shadcn components added, no existing components modified</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write verification tests for packages and components</name>
  <files>
    tests/unit/features/component-foundation/packages.test.ts,
    tests/unit/features/component-foundation/shadcn-components.test.ts
  </files>
  <read_first>
    vitest.config.mts,
    src/components/ui/carousel.tsx,
    src/components/ui/accordion.tsx,
    src/components/ui/tabs.tsx
  </read_first>
  <behavior>
    packages.test.ts:
    - Test 1: `import Autoplay from "embla-carousel-autoplay"` resolves without error
    - Test 2: `import AutoScroll from "embla-carousel-auto-scroll"` resolves without error
    - Test 3: `import Lightbox from "yet-another-react-lightbox"` resolves without error
    - Test 4: `import { useInView } from "react-intersection-observer"` resolves without error

    shadcn-components.test.ts:
    - Test 1-13: Each of the 13 new shadcn components renders without throwing (import + render with @testing-library/react)
    - Components to test: Accordion (AccordionTrigger+AccordionContent), Tabs (TabsList+TabsTrigger+TabsContent), Carousel, Collapsible, Progress, Pagination (PaginationContent+PaginationItem), Popover, ScrollArea, Avatar, Breadcrumb (BreadcrumbList+BreadcrumbItem+BreadcrumbLink), ToggleGroup+ToggleGroupItem, RadioGroup+RadioGroupItem, DropdownMenu
  </behavior>
  <action>
Create `tests/unit/features/component-foundation/packages.test.ts`:
- Import each of the 4 new packages
- Assert they export the expected default/named exports
- For Autoplay: `expect(typeof Autoplay).toBe('function')`
- For AutoScroll: `expect(typeof AutoScroll).toBe('function')`
- For Lightbox: `expect(Lightbox).toBeDefined()`
- For useInView: `expect(typeof useInView).toBe('function')`

Create `tests/unit/features/component-foundation/shadcn-components.test.ts`:
- Import each of the 13 new shadcn components from `@/components/ui/{name}`
- Use `render()` from @testing-library/react to mount each component
- For compound components (Accordion, Tabs, Breadcrumb, etc.), render with required children structure
- Use `describe` block per component, single `it('renders without error')` test each
- Example pattern:
```tsx
import { render } from '@testing-library/react'
import { Accordion, AccordionItem, AccordionTrigger, AccordionContent } from '@/components/ui/accordion'

describe('Accordion', () => {
  it('renders without error', () => {
    expect(() => render(
      <Accordion type="single" collapsible>
        <AccordionItem value="item-1">
          <AccordionTrigger>Test</AccordionTrigger>
          <AccordionContent>Content</AccordionContent>
        </AccordionItem>
      </Accordion>
    )).not.toThrow()
  })
})
```

Run tests RED first (before Task 1 if following TDD), then GREEN after Task 1 completes.
  </action>
  <verify>
    <automated>yarn test tests/unit/features/component-foundation/packages.test.ts tests/unit/features/component-foundation/shadcn-components.test.ts --run</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/features/component-foundation/packages.test.ts exists and contains 4 test cases
    - tests/unit/features/component-foundation/shadcn-components.test.ts exists and contains 13 test cases (one per shadcn component)
    - `yarn test tests/unit/features/component-foundation/ --run` exits with code 0
    - All 17 tests pass (4 package + 13 component)
  </acceptance_criteria>
  <done>17 tests pass: 4 package import tests + 13 component render tests</done>
</task>

</tasks>

<verification>
- `yarn test tests/unit/features/component-foundation/ --run` -- all 17 tests pass
- `ls src/components/ui/ | wc -l` -- should be 29 (16 existing + 13 new)
- `git diff --name-only src/components/ui/button.tsx` -- empty (existing components untouched)
- `grep embla-carousel-autoplay package.json` -- present
- `grep yet-another-react-lightbox package.json` -- present
- `grep react-intersection-observer package.json` -- present
</verification>

<success_criteria>
- 13 new shadcn component files in src/components/ui/
- 4 new npm packages in package.json (embla-carousel-autoplay, embla-carousel-auto-scroll, yet-another-react-lightbox, react-intersection-observer)
- 17 tests passing (4 package imports + 13 component renders)
- Zero changes to existing 16 component files
</success_criteria>

<output>
After completion, create `.planning/phases/13-component-foundation/13-01-SUMMARY.md`
</output>
