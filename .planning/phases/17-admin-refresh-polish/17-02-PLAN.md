---
phase: 17-admin-refresh-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - tests/e2e/mobile-audit.spec.ts
  - src/app/(public)/vehicles/page.tsx
  - src/app/(public)/vehicles/[id]/page.tsx
  - src/components/layout/header.tsx
  - src/components/layout/footer.tsx
autonomous: false
requirements:
  - ADMIN-03
  - ADMIN-04

must_haves:
  truths:
    - "No page has horizontal scroll at 375px viewport width"
    - "All existing vitest tests pass after all Phase 17 changes"
    - "yarn build completes successfully"
    - "yarn type-check completes without errors"
    - "Homepage, search, detail, compare pages render without errors at 375px"
  artifacts:
    - path: "tests/e2e/mobile-audit.spec.ts"
      provides: "Playwright 375px viewport audit for all redesigned pages"
      contains: "375"
  key_links:
    - from: "tests/e2e/mobile-audit.spec.ts"
      to: "all public pages"
      via: "Playwright page.goto with 375px viewport"
      pattern: "width.*375"
---

<objective>
Mobile responsive audit at 375px and full regression verification of demo flow.

Purpose: Ensure all Phase 13-16 redesigned pages display correctly at 375px mobile viewport without horizontal overflow, text truncation, or layout breaks. Verify the complete demo flow (home -> search -> detail -> contract) and all existing tests still pass after Phase 17 changes.

Output: Mobile audit e2e test file, CSS fixes for any discovered mobile issues, full green test suite + clean build.
</objective>

<execution_context>
@/Users/kiyeol/.claude-work/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-admin-refresh-polish/17-CONTEXT.md
@.planning/phases/17-admin-refresh-polish/17-RESEARCH.md
@.planning/phases/17-admin-refresh-polish/17-01-SUMMARY.md

<interfaces>
<!-- Playwright config and existing e2e patterns -->

From playwright.config.ts (expected):
```typescript
// Existing config uses chromium, localhost:3000
```

From tests/e2e/demo-flow.spec.ts (existing pattern):
```typescript
import { test, expect } from '@playwright/test'
// Tests navigate through: landing -> search -> detail -> admin
```

Target pages for mobile audit:
- / (homepage - Phase 16)
- /vehicles (search listing - Phase 15)
- /vehicles/[id] (detail - Phase 14)
- /vehicles/compare (compare - Phase 15)
- /calculator (pricing - v1.0)
- /inquiry (inquiry form - v1.0)
- /sell (sell page - Phase 16)
- /admin/dashboard (admin - v1.0, desktop-first)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mobile 375px audit -- Playwright tests + CSS fixes for any discovered issues</name>
  <files>
    tests/e2e/mobile-audit.spec.ts
  </files>
  <read_first>
    tests/e2e/demo-flow.spec.ts,
    playwright.config.ts,
    src/app/page.tsx,
    src/app/(public)/vehicles/page.tsx,
    src/app/(public)/vehicles/[id]/page.tsx,
    src/app/(public)/vehicles/compare/page.tsx,
    src/app/globals.css
  </read_first>
  <action>
**Create `tests/e2e/mobile-audit.spec.ts`** -- a Playwright e2e test that checks all redesigned pages at 375px viewport.

The test should:

1. Set viewport to `{ width: 375, height: 812 }` (iPhone SE equivalent) using `test.use()`

2. Define all target pages:
```typescript
const PAGES = [
  { path: '/', name: 'Homepage' },
  { path: '/vehicles', name: 'Search Listing' },
  { path: '/vehicles/compare', name: 'Compare' },
  { path: '/calculator', name: 'Calculator' },
  { path: '/inquiry', name: 'Inquiry' },
  { path: '/sell', name: 'Sell' },
]
```
Note: Skip `/vehicles/[id]` (requires valid UUID from DB), skip `/admin/*` (requires auth + desktop-first). These are checked manually if Supabase is active.

3. For each page, test that:
- Page loads without console errors (listen to `page.on('console')` for error-level messages)
- No horizontal overflow: `document.documentElement.scrollWidth <= document.documentElement.clientWidth + 1` (1px tolerance)
- Main content container is visible

4. Structure tests in a `test.describe('Mobile 375px Audit', ...)` block with a `for...of` loop generating individual test cases per page.

5. Add a comment at top: `// ADMIN-03: 375px mobile responsive audit for Phase 13-16 redesigned pages`

**After creating the test, attempt to run it:**
- `npx playwright test tests/e2e/mobile-audit.spec.ts --reporter=line`
- If dev server isn't running or Supabase is paused, the test may fail on data-dependent pages -- this is expected. The test file itself must be valid and compilable.

**If any horizontal overflow is detected**, fix the offending component's CSS:
- Common fixes: Add `overflow-x-hidden` to container, change `min-w-[X]` to responsive variant, add `max-w-full` to images, wrap flex containers with `flex-wrap`
- Files that MAY need fixes (only modify if audit reveals issues): header, footer, homepage sections, vehicle cards, compare table
- Keep fixes minimal -- Tailwind responsive classes only, no layout rewrites
- Admin pages: fix CRITICAL only (horizontal scroll), ignore minor spacing issues

**Important constraints:**
- The `files_modified` list above includes potential CSS fix targets. Only modify them if the audit reveals actual issues.
- If no CSS fixes are needed, only `tests/e2e/mobile-audit.spec.ts` is created.
  </action>
  <verify>
    <automated>cd /Users/kiyeol/development/navid/navid-auto-rental-lease && yarn type-check && yarn test</automated>
  </verify>
  <acceptance_criteria>
- File exists: `tests/e2e/mobile-audit.spec.ts`
- `grep "width: 375" tests/e2e/mobile-audit.spec.ts` returns match
- `grep "scrollWidth" tests/e2e/mobile-audit.spec.ts` returns match (horizontal overflow check)
- `grep "ADMIN-03" tests/e2e/mobile-audit.spec.ts` returns match (requirement traced)
- `yarn type-check` passes
- `yarn test` passes (unit tests unaffected)
  </acceptance_criteria>
  <done>Mobile audit e2e test created covering all redesigned public pages at 375px. Any discovered horizontal overflow or layout issues fixed with minimal CSS changes. Unit tests and type-check pass.</done>
</task>

<task type="auto">
  <name>Task 2: Full regression verification -- vitest + build + type-check</name>
  <files></files>
  <read_first>
    package.json,
    vitest.config.mts
  </read_first>
  <action>
Run the complete regression test suite to verify all Phase 17 changes (token unification + comparison highlighting + mobile fixes) have not broken anything.

**Execute in sequence:**

1. `yarn type-check` -- TypeScript compilation must complete with exit code 0, no errors
2. `yarn test` -- all vitest unit tests must pass (expect ~439+ tests across ~50 files)
3. `yarn build` -- production Next.js build must complete successfully

**If any test fails:**
- Read the failure output carefully
- Determine if the failure is caused by Phase 17 changes (token/highlight/CSS)
- Fix the root cause in the relevant file
- Re-run the failing test to confirm the fix
- Re-run full suite to ensure no cascading failures

**If build fails:**
- Check for import errors from the new `compare-utils.ts`
- Check for TypeScript type mismatches from COMPARE_FIELDS betterIs additions
- Fix and re-run

**Expected results:**
- type-check: 0 errors
- test: All ~439+ tests pass, 0 failures
- build: Exit code 0, "Route (app)" summary printed

Record exact test count and build status for the SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/kiyeol/development/navid/navid-auto-rental-lease && yarn type-check && yarn test && yarn build</automated>
  </verify>
  <acceptance_criteria>
- `yarn type-check` exits with code 0
- `yarn test` output shows 0 failures (all tests pass)
- `yarn build` exits with code 0 and prints route summary
- No regressions introduced by Phase 17 changes
  </acceptance_criteria>
  <done>Full regression suite passes: type-check clean, all vitest tests green, production build successful. Phase 17 changes verified to not break any existing functionality.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Visual verification -- admin tokens, comparison highlighting, mobile, demo flow</name>
  <files></files>
  <action>
Human verifies the final v2.0 output visually. Phase 17 complete: Admin design tokens unified (semantic tokens replace hardcoded slate colors), comparison table highlighting added (green winner / red loser cells), mobile audit test created, full regression suite passes. This is the final phase of v2.0 K Car Style Redesign.

Verification steps:
1. Admin token check: Visit /admin/dashboard and /admin/users -- table headers should use subtle gray (not distinct from public pages), stats cards should retain their distinct blue/emerald/violet/amber colors
2. Comparison highlighting: Go to /vehicles, add 2-3 vehicles to compare, open compare dialog -- winner cells should have green background, loser cells should have red background, equal values should have no highlight
3. Compare page: Visit /vehicles/compare -- same highlighting should appear. The "add vehicle" slot should only appear when fewer than 3 vehicles are selected (not 4)
4. Mobile check: Resize browser to 375px width (or use DevTools responsive mode) and check: homepage, /vehicles, /vehicles/[id] (if data available), /vehicles/compare. No horizontal scrollbar should appear.
5. Demo flow: Navigate Home -> /vehicles -> click a vehicle -> verify detail page loads -> check contract CTA buttons are present

Resume signal: Type "approved" to complete v2.0, or describe any issues found.
  </action>
  <verify>
    <automated>echo "Checkpoint: awaiting human verification"</automated>
  </verify>
  <acceptance_criteria>
- Human confirms admin pages use unified design tokens
- Human confirms comparison tables show green/red highlighting for winner/loser
- Human confirms no horizontal scroll at 375px mobile viewport
- Human confirms demo flow works end-to-end
  </acceptance_criteria>
  <done>Human has verified all Phase 17 deliverables and approved the v2.0 K Car Style Redesign as complete.</done>
</task>

</tasks>

<verification>
1. `yarn type-check` -- TypeScript compilation clean (0 errors)
2. `yarn test` -- all unit tests pass (~439+ tests)
3. `yarn build` -- production build succeeds
4. `tests/e2e/mobile-audit.spec.ts` exists with 375px viewport check
5. Human verifies admin tokens, comparison highlighting, and mobile layouts
</verification>

<success_criteria>
- Mobile audit e2e test exists and is valid Playwright TypeScript
- No horizontal overflow at 375px on any redesigned public page (CSS fixes applied if needed)
- yarn type-check + yarn test + yarn build all pass with 0 errors
- Human confirms admin design consistency, comparison highlighting, and mobile responsiveness
- v2.0 K Car Style Redesign milestone complete
</success_criteria>

<output>
After completion, create `.planning/phases/17-admin-refresh-polish/17-02-SUMMARY.md`
</output>
