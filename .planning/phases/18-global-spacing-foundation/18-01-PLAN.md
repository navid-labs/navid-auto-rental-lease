---
phase: 18-global-spacing-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/mega-menu.tsx
  - src/app/(public)/layout.tsx
  - src/app/(protected)/layout.tsx
  - src/app/(public)/page.tsx
  - src/app/(public)/vehicles/page.tsx
  - src/app/(public)/vehicles/[id]/page.tsx
  - src/app/(public)/sell/page.tsx
  - src/app/(public)/calculator/page.tsx
  - src/app/(public)/inquiry/page.tsx
autonomous: false
requirements:
  - GLBL-01
  - GLBL-02
  - GLBL-03

must_haves:
  truths:
    - "Mega menu navigation bar renders at 52px height on all public desktop pages"
    - "Every public and protected page has exactly 24px of breathing room between the sticky header bottom and the first content element"
    - "Homepage hero banner still touches the navigation bottom edge with no gap"
    - "Vehicle detail gallery still touches the navigation bottom edge with no gap"
    - "No page has double-padding (48px+ gap from layout + page-level padding stacking)"
    - "Admin dashboard content area has 24px padding on all sides, unchanged from current"
    - "Dealer portal content area has 24px padding on all sides, unchanged from current"
  artifacts:
    - path: "src/components/layout/mega-menu.tsx"
      provides: "52px mega menu nav bar height"
      contains: "h-[52px]"
    - path: "src/app/(public)/layout.tsx"
      provides: "24px top padding on public layout main"
      contains: "pt-6"
    - path: "src/app/(protected)/layout.tsx"
      provides: "24px top padding on protected layout main"
      contains: "pt-6"
    - path: "src/app/(public)/page.tsx"
      provides: "Edge-to-edge hero with negative margin override"
      contains: "-mt-6"
    - path: "src/app/(public)/vehicles/[id]/page.tsx"
      provides: "Edge-to-edge gallery with negative margin override"
      contains: "-mt-6"
  key_links:
    - from: "src/app/(public)/layout.tsx"
      to: "all public page children"
      via: "pt-6 on main element"
      pattern: 'className="flex-1 pt-6"'
    - from: "src/app/(public)/page.tsx"
      to: "HeroBanner component"
      via: "-mt-6 wrapper div counteracting layout padding"
      pattern: "-mt-6"
    - from: "src/app/(public)/vehicles/[id]/page.tsx"
      to: "VehicleDetailPage component"
      via: "-mt-6 wrapper div counteracting layout padding"
      pattern: "-mt-6"
---

<objective>
Implement consistent navigation height and content top margins across all pages.

Purpose: Establish the global spacing foundation that all subsequent v2.1 phases (19, 20) build upon. Navigation bar height and content start position are the most foundational visual rules -- they cascade to every page.

Output: All public/protected pages have 24px breathing room below nav. Mega menu is 52px tall. Admin/dealer layouts verified as already compliant. Edge-to-edge hero and gallery preserved with negative margin overrides.
</objective>

<execution_context>
@/Users/kiyeol/.claude-work/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-global-spacing-foundation/18-RESEARCH.md
@.planning/phases/18-global-spacing-foundation/18-UI-SPEC.md

<interfaces>
<!-- Key files and their current state. Executor reads these to see actual code before modifying. -->

From src/components/layout/mega-menu.tsx (line 39):
```tsx
<Link className={`flex h-12 items-center px-5 text-[14px] font-medium transition-colors ${...}`}>
```

From src/app/(public)/layout.tsx (line 19):
```tsx
<main className="flex-1">{children}</main>
```

From src/app/(protected)/layout.tsx (line 12):
```tsx
<main className="min-h-[calc(100vh-4rem)]">{children}</main>
```

From src/app/admin/layout.tsx (line 58):
```tsx
<main className="flex-1 p-6">{children}</main>
```

From src/app/dealer/layout-client.tsx (line 62):
```tsx
<main className="flex-1 p-6">{children}</main>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Increase mega menu height and add layout-level top padding</name>
  <files>
    src/components/layout/mega-menu.tsx,
    src/app/(public)/layout.tsx,
    src/app/(protected)/layout.tsx
  </files>
  <read_first>
    src/components/layout/mega-menu.tsx,
    src/app/(public)/layout.tsx,
    src/app/(protected)/layout.tsx,
    .planning/phases/18-global-spacing-foundation/18-UI-SPEC.md
  </read_first>
  <action>
    **1. Mega menu height (GLBL-01):**
    In `src/components/layout/mega-menu.tsx`, line 39, change `h-12` to `h-[52px]` in the Link className string.

    Current (line 39):
    ```tsx
    className={`flex h-12 items-center px-5 text-[14px] font-medium transition-colors ${
    ```
    Target:
    ```tsx
    className={`flex h-[52px] items-center px-5 text-[14px] font-medium transition-colors ${
    ```

    Do NOT change anything else in this file. Do NOT change the main header bar height (`h-[68px]` in header.tsx). Only the mega menu nav links get the height change.

    **2. Public layout main padding (GLBL-02):**
    In `src/app/(public)/layout.tsx`, line 19, change `className="flex-1"` to `className="flex-1 pt-6"`.

    Current:
    ```tsx
    <main className="flex-1">{children}</main>
    ```
    Target:
    ```tsx
    <main className="flex-1 pt-6">{children}</main>
    ```

    **3. Protected layout main padding (GLBL-02):**
    In `src/app/(protected)/layout.tsx`, line 12, add `pt-6` to the existing className.

    Current:
    ```tsx
    <main className="min-h-[calc(100vh-4rem)]">{children}</main>
    ```
    Target:
    ```tsx
    <main className="min-h-[calc(100vh-4rem)] pt-6">{children}</main>
    ```

    Do NOT use `mt-6` (margin-top) -- it creates a visible gap during scroll behind sticky header. Use `pt-6` (padding-top) only.
    Do NOT introduce CSS custom properties (`--nav-height`, `--content-top-gap`). Use Tailwind classes per codebase convention.
  </action>
  <verify>
    <automated>cd /Users/kiyeol/development/navid/navid-auto-rental-lease && yarn type-check</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/layout/mega-menu.tsx` contains `h-[52px]` and does NOT contain `h-12` in the Link className
    - `src/app/(public)/layout.tsx` contains `className="flex-1 pt-6"`
    - `src/app/(protected)/layout.tsx` contains `pt-6` in the main className
    - `yarn type-check` exits with code 0
  </acceptance_criteria>
  <done>Mega menu renders at 52px height. Public and protected layouts provide 24px top padding to all child pages.</done>
</task>

<task type="auto">
  <name>Task 2: De-duplicate per-page top padding and add edge-to-edge overrides</name>
  <files>
    src/app/(public)/page.tsx,
    src/app/(public)/vehicles/page.tsx,
    src/app/(public)/vehicles/[id]/page.tsx,
    src/app/(public)/sell/page.tsx,
    src/app/(public)/calculator/page.tsx,
    src/app/(public)/inquiry/page.tsx
  </files>
  <read_first>
    src/app/(public)/page.tsx,
    src/app/(public)/vehicles/page.tsx,
    src/app/(public)/vehicles/[id]/page.tsx,
    src/app/(public)/sell/page.tsx,
    src/app/(public)/calculator/page.tsx,
    src/app/(public)/inquiry/page.tsx,
    .planning/phases/18-global-spacing-foundation/18-UI-SPEC.md
  </read_first>
  <action>
    The public layout now provides `pt-6` (24px) on `<main>`. Each page that has its OWN top padding must be adjusted to prevent double-padding. Additionally, two pages need edge-to-edge content preserved with negative margins.

    **Edge-to-edge overrides (CRITICAL -- these pages intentionally have zero top gap):**

    **A. Homepage (`src/app/(public)/page.tsx`):**
    The HeroBanner is the first child and must touch the nav bottom edge. Wrap it in a div with `-mt-6` to counteract the layout's `pt-6`.

    Current (line 14-15):
    ```tsx
    <>
      {/* 1. Hero Banner Carousel */}
      <HeroBanner />
    ```
    Target:
    ```tsx
    <>
      {/* 1. Hero Banner Carousel */}
      <div className="-mt-6">
        <HeroBanner />
      </div>
    ```

    **B. Vehicle detail (`src/app/(public)/vehicles/[id]/page.tsx`):**
    The VehicleDetailPage renders SectionGallery as its first child, which must be edge-to-edge. Wrap the outer `<div className="pb-safe">` content with `-mt-6`.

    Current (line 109):
    ```tsx
    <div className="pb-safe">
      <VehicleDetailPage
    ```
    Target:
    ```tsx
    <div className="-mt-6 pb-safe">
      <VehicleDetailPage
    ```

    **Per-page padding de-duplication (remove top padding that now comes from layout):**

    **C. Vehicles search (`src/app/(public)/vehicles/page.tsx`):**
    Line 46 has `pt-4` on the breadcrumb wrapper. Remove `pt-4` since layout provides 24px.

    Current:
    ```tsx
    <div className="mx-auto max-w-[1440px] px-4 pt-4 lg:px-8 xl:px-[120px]">
    ```
    Target:
    ```tsx
    <div className="mx-auto max-w-[1440px] px-4 lg:px-8 xl:px-[120px]">
    ```

    **D. Sell page (`src/app/(public)/sell/page.tsx`):**
    Line 19 has `pt-6` on the breadcrumb wrapper. Remove `pt-6` since layout provides 24px.

    Current:
    ```tsx
    <div className="mx-auto max-w-7xl px-4 pt-6">
    ```
    Target:
    ```tsx
    <div className="mx-auto max-w-7xl px-4">
    ```

    **E. Calculator page (`src/app/(public)/calculator/page.tsx`):**
    Line 14 has `py-10` on the wrapper div. Change to `pb-10` only (layout provides top, keep bottom).

    Current:
    ```tsx
    <div className="mx-auto max-w-4xl px-4 py-10">
    ```
    Target:
    ```tsx
    <div className="mx-auto max-w-4xl px-4 pb-10">
    ```

    **F. Inquiry page (`src/app/(public)/inquiry/page.tsx`):**
    Line 13 has `py-12` on the wrapper div. Change to `pb-12` only (layout provides top, keep bottom).

    Current:
    ```tsx
    <div className="mx-auto max-w-4xl px-4 py-12 md:px-6">
    ```
    Target:
    ```tsx
    <div className="mx-auto max-w-4xl px-4 pb-12 md:px-6">
    ```

    Do NOT touch admin or dealer pages -- they already have `p-6` on their `<main>` elements and are not affected by this layout change.
  </action>
  <verify>
    <automated>cd /Users/kiyeol/development/navid/navid-auto-rental-lease && yarn type-check && yarn test --run</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(public)/page.tsx` contains `className="-mt-6"` wrapping the HeroBanner
    - `src/app/(public)/vehicles/[id]/page.tsx` contains `-mt-6` in the outer div className
    - `src/app/(public)/vehicles/page.tsx` does NOT contain `pt-4` on the breadcrumb wrapper line (the `max-w-[1440px]` div)
    - `src/app/(public)/sell/page.tsx` does NOT contain `pt-6` on the breadcrumb wrapper line (the `max-w-7xl` div)
    - `src/app/(public)/calculator/page.tsx` contains `pb-10` and does NOT contain `py-10`
    - `src/app/(public)/inquiry/page.tsx` contains `pb-12` and does NOT contain `py-12`
    - `yarn type-check` exits with code 0
    - `yarn test --run` exits with code 0
  </acceptance_criteria>
  <done>All per-page top padding de-duplicated. Homepage hero and vehicle detail gallery remain edge-to-edge via negative margin overrides. No page has double-padding.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of spacing across all page types</name>
  <files>none</files>
  <action>
    Start the dev server (`yarn dev`) and visually verify all spacing changes across 8 page types. This is a human verification checkpoint -- no code changes needed. The executor presents the verification checklist and waits for user approval.
  </action>
  <read_first>
    .planning/phases/18-global-spacing-foundation/18-UI-SPEC.md
  </read_first>
  <what-built>
    Global spacing foundation: mega menu height increased to 52px, all public/protected pages have 24px top breathing room, homepage hero and vehicle detail gallery remain edge-to-edge, admin/dealer layouts unchanged (already compliant with p-6).
  </what-built>
  <how-to-verify>
    Start the dev server if not running: `yarn dev`

    1. **Homepage** (http://localhost:3000):
       - Hero banner should touch the bottom edge of the navigation (no 24px gap -- the `-mt-6` override is working)
       - Mega menu category links (on desktop) should be slightly taller than before (52px vs 48px)

    2. **Vehicle search** (http://localhost:3000/vehicles):
       - Breadcrumb / search bar should have 24px gap below the navigation
       - No double-padding (should NOT look like 48px+ gap)

    3. **Vehicle detail** (click any vehicle from search):
       - Gallery should touch the navigation bottom edge (no gap -- the `-mt-6` override is working)

    4. **Sell page** (http://localhost:3000/sell):
       - Breadcrumb should have 24px gap below navigation
       - Should NOT have extra 24px gap (no double-padding)

    5. **Calculator** (http://localhost:3000/calculator):
       - Content should start with 24px gap below navigation
       - Bottom padding should still be generous (40px)

    6. **Inquiry** (http://localhost:3000/inquiry):
       - Content should start with 24px gap below navigation
       - Bottom padding should still be generous (48px)

    7. **Admin dashboard** (http://localhost:3000/admin):
       - Content padding should look the same as before (24px on all sides)
       - No visual changes should be apparent

    8. **Scroll-spy test** on vehicle detail page:
       - Scroll down through sections -- the correct tab should be highlighted in the sticky nav
       - (The 4px height change is small and unlikely to break this, but worth a quick check)
  </how-to-verify>
  <verify>Human visual inspection of 8 page types confirms correct spacing</verify>
  <acceptance_criteria>
    - User confirms hero banner is edge-to-edge on homepage (no gap)
    - User confirms vehicle gallery is edge-to-edge on detail page (no gap)
    - User confirms 24px breathing room on /vehicles, /sell, /calculator, /inquiry
    - User confirms admin dashboard spacing is unchanged
    - User confirms no double-padding on any page
  </acceptance_criteria>
  <done>User has approved visual spacing across all page types. Phase 18 global spacing foundation is complete.</done>
  <resume-signal>Type "approved" if all spacing looks correct, or describe any issues you see</resume-signal>
</task>

</tasks>

<verification>
- `yarn type-check` passes (TypeScript compilation)
- `yarn test --run` passes (all existing tests green)
- Visual inspection confirms:
  - Mega menu is 52px tall (desktop only)
  - All public pages have 24px top breathing room
  - Homepage hero is edge-to-edge (no gap)
  - Vehicle detail gallery is edge-to-edge (no gap)
  - No page has double-padding
  - Admin pages are unchanged
</verification>

<success_criteria>
1. Navigation bar across all pages renders at 52px height (up from 48px) -- GLBL-01
2. Every page's content starts with 24px of breathing room below the navigation -- GLBL-02
3. Admin dashboard pages follow the same spacing rules (24px content padding, already compliant) -- GLBL-03
4. Edge-to-edge hero and gallery are preserved with negative margin overrides
5. No double-padding on any page
6. All existing tests pass, type-check passes
</success_criteria>

<output>
After completion, create `.planning/phases/18-global-spacing-foundation/18-01-SUMMARY.md`
</output>
