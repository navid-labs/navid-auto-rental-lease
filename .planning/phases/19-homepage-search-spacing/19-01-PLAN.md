---
phase: 19-homepage-search-spacing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(public)/page.tsx
  - src/features/marketing/components/hero-search-box.tsx
  - src/features/marketing/components/quick-links.tsx
  - src/features/marketing/components/recommended-vehicles-tabs.tsx
  - src/features/marketing/components/promo-banners.tsx
  - src/features/marketing/components/partner-logos.tsx
autonomous: true
requirements: [HOME-01, HOME-02, HOME-03, HOME-04]

must_haves:
  truths:
    - "Homepage sections have 80px+ visible breathing room between them"
    - "Featured vehicles display in a 3-column grid on desktop"
    - "Search section has generous internal padding (32px title gap, 16px field gaps, 24px filter padding)"
    - "Promo cards have 24px gaps between them"
    - "Loading skeleton matches 3-column grid layout"
  artifacts:
    - path: "src/app/(public)/page.tsx"
      provides: "mt-10 wrapper on HeroSearchBox, 3-col SectionSkeleton"
      contains: "mt-10"
    - path: "src/features/marketing/components/hero-search-box.tsx"
      provides: "Increased section padding and internal spacing"
      contains: "py-10 md:py-12"
    - path: "src/features/marketing/components/quick-links.tsx"
      provides: "Increased section padding"
      contains: "py-8"
    - path: "src/features/marketing/components/recommended-vehicles-tabs.tsx"
      provides: "3-column grid layout"
      contains: "lg:grid-cols-3"
    - path: "src/features/marketing/components/promo-banners.tsx"
      provides: "24px card gap"
      contains: "gap-6"
    - path: "src/features/marketing/components/partner-logos.tsx"
      provides: "Increased section padding"
      contains: "py-10"
  key_links:
    - from: "src/app/(public)/page.tsx"
      to: "src/features/marketing/components/hero-search-box.tsx"
      via: "mt-10 wrapper div around HeroSearchBox import"
      pattern: 'className="mt-10"'
    - from: "src/app/(public)/page.tsx SectionSkeleton"
      to: "src/features/marketing/components/recommended-vehicles-tabs.tsx"
      via: "Both must use lg:grid-cols-3 (not 4)"
      pattern: "lg:grid-cols-3"
---

<objective>
Apply generous spacing to all homepage sections: increase inter-section gaps to 80px+, change featured vehicles grid from 4-column to 3-column, increase search section internal padding, and widen promo card gaps.

Purpose: Meet HOME-01 through HOME-04 requirements -- homepage sections feel spacious with premium visual hierarchy matching K Car density standards.
Output: 6 modified component files with updated Tailwind spacing classes.
</objective>

<execution_context>
@/Users/kiyeol/.claude-work/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-global-spacing-foundation/18-01-SUMMARY.md
@.planning/phases/19-homepage-search-spacing/19-RESEARCH.md
@.planning/phases/19-homepage-search-spacing/19-UI-SPEC.md

<interfaces>
<!-- Phase 18 established patterns the executor must preserve -->

From src/app/(public)/page.tsx (current):
- Line 15: `<div className="-mt-6">` wraps HeroBanner for edge-to-edge override -- DO NOT CHANGE
- Line 20: `<HeroSearchBox />` has no wrapper -- needs `<div className="mt-10">` wrapper added
- Line 39-59: SectionSkeleton function with `lg:grid-cols-4` -- must change to `lg:grid-cols-3`

From src/features/marketing/components/hero-search-box.tsx (current):
- Line 74: `<section className="bg-white py-6 md:py-8">` -- change to `py-10 md:py-12`
- Line 76: `<h2 className="mb-4 ...">` -- change `mb-4` to `mb-8`
- Line 102: `<div className="flex flex-col gap-3 px-6 py-5 ...">` -- change `gap-3` to `gap-4`, `py-5` to `py-6`

From src/features/marketing/components/quick-links.tsx (current):
- Line 20: `<section className="bg-white py-6">` -- change to `py-8`

From src/features/marketing/components/recommended-vehicles-tabs.tsx (current):
- Line 60: `<div className="grid grid-cols-2 gap-4 lg:grid-cols-4">` -- change `lg:grid-cols-4` to `lg:grid-cols-3`

From src/features/marketing/components/promo-banners.tsx (current):
- Line 46: `<div className="grid grid-cols-1 gap-4 md:grid-cols-3">` -- change `gap-4` to `gap-6`

From src/features/marketing/components/partner-logos.tsx (current):
- Line 12: `<section className="border-t border-[#F0F0F0] bg-white py-8">` -- change `py-8` to `py-10`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Homepage section spacing and grid changes</name>
  <files>
    src/app/(public)/page.tsx,
    src/features/marketing/components/hero-search-box.tsx,
    src/features/marketing/components/quick-links.tsx,
    src/features/marketing/components/recommended-vehicles-tabs.tsx,
    src/features/marketing/components/promo-banners.tsx,
    src/features/marketing/components/partner-logos.tsx
  </files>
  <read_first>
    src/app/(public)/page.tsx,
    src/features/marketing/components/hero-search-box.tsx,
    src/features/marketing/components/quick-links.tsx,
    src/features/marketing/components/recommended-vehicles-tabs.tsx,
    src/features/marketing/components/promo-banners.tsx,
    src/features/marketing/components/partner-logos.tsx
  </read_first>
  <action>
Apply all homepage spacing changes across 6 files. Each change is a single Tailwind class substitution.

**File 1: `src/app/(public)/page.tsx`**

1. Wrap HeroSearchBox with a margin div for 80px+ gap from hero. Change:
   ```
   {/* 2. Search Box (extracted from old hero) */}
   <HeroSearchBox />
   ```
   To:
   ```
   {/* 2. Search Box (extracted from old hero) */}
   <div className="mt-10">
     <HeroSearchBox />
   </div>
   ```
   This adds 40px margin. Combined with hero bottom padding (12px) and search top padding (40px from py-10), total gap = 12 + 40 + 40 = 92px.

2. In the `SectionSkeleton` function, change the grid from 4 columns to 3 columns. On line 51, change:
   ```
   <div className="grid grid-cols-2 gap-4 lg:grid-cols-4">
   ```
   To:
   ```
   <div className="grid grid-cols-2 gap-4 lg:grid-cols-3">
   ```

CRITICAL: Do NOT change the `-mt-6` wrapper on HeroBanner (line 15). That is the Phase 18 edge-to-edge override.

**File 2: `src/features/marketing/components/hero-search-box.tsx`**

3. Line 74 -- Section padding: Change `py-6 md:py-8` to `py-10 md:py-12`
   ```
   <section className="bg-white py-10 md:py-12">
   ```

4. Line 76 -- Title bottom margin: Change `mb-4` to `mb-8`
   ```
   <h2 className="mb-8 text-center text-[18px] font-bold text-[#0D0D0D]">
   ```

5. Line 102 -- Filter row gap and padding: Change `gap-3` to `gap-4` AND `py-5` to `py-6`
   ```
   <div className="flex flex-col gap-4 px-6 py-6 sm:flex-row sm:items-center">
   ```

**File 3: `src/features/marketing/components/quick-links.tsx`**

6. Line 20 -- Section padding: Change `py-6` to `py-8`
   ```
   <section className="bg-white py-8">
   ```

**File 4: `src/features/marketing/components/recommended-vehicles-tabs.tsx`**

7. Line 60 -- Grid columns: Change `lg:grid-cols-4` to `lg:grid-cols-3`
   ```
   <div className="grid grid-cols-2 gap-4 lg:grid-cols-3">
   ```

**File 5: `src/features/marketing/components/promo-banners.tsx`**

8. Line 46 -- Card gap: Change `gap-4` to `gap-6`
   ```
   <div className="grid grid-cols-1 gap-6 md:grid-cols-3">
   ```

**File 6: `src/features/marketing/components/partner-logos.tsx`**

9. Line 12 -- Section padding: Change `py-8` to `py-10`
   ```
   <section className="border-t border-[#F0F0F0] bg-white py-10">
   ```

**Anti-patterns to avoid:**
- Do NOT use `space-y-*` on the homepage parent -- sections have different background colors (white, #F9FAFB) and margin would create visible strips of page background between them.
- Do NOT add both `mt-10` AND change HeroSearchBox to `py-10` without understanding the combined gap math. The design contract specifies: hero bottom (12px) + margin (40px) + search top padding (40px) = 92px total.
  </action>
  <verify>
    <automated>cd /Users/kiyeol/development/navid/navid-auto-rental-lease && yarn type-check</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(public)/page.tsx` contains `className="mt-10"` wrapping HeroSearchBox
    - `src/app/(public)/page.tsx` contains `className="-mt-6"` wrapping HeroBanner (unchanged)
    - `src/app/(public)/page.tsx` SectionSkeleton contains `lg:grid-cols-3` (not `lg:grid-cols-4`)
    - `src/features/marketing/components/hero-search-box.tsx` contains `py-10 md:py-12` (not `py-6 md:py-8`)
    - `src/features/marketing/components/hero-search-box.tsx` contains `mb-8` on the h2 element (not `mb-4`)
    - `src/features/marketing/components/hero-search-box.tsx` contains `gap-4` and `py-6` in the filter row (not `gap-3` and `py-5`)
    - `src/features/marketing/components/quick-links.tsx` contains `py-8` (not `py-6`)
    - `src/features/marketing/components/recommended-vehicles-tabs.tsx` contains `lg:grid-cols-3` (not `lg:grid-cols-4`)
    - `src/features/marketing/components/promo-banners.tsx` contains `gap-6` (not `gap-4`)
    - `src/features/marketing/components/partner-logos.tsx` contains `py-10` (not `py-8`)
    - `yarn type-check` exits with code 0
  </acceptance_criteria>
  <done>All 6 homepage component files have updated spacing classes. Homepage sections have 80px+ visible gaps, featured vehicles use 3-column grid, search section has increased internal padding, promo cards have 24px gaps, and skeleton matches actual grid.</done>
</task>

</tasks>

<verification>
- `yarn type-check` passes (TypeScript integrity)
- `yarn test --run` passes (no regression in existing tests)
- grep confirms: no `lg:grid-cols-4` remains in `page.tsx` or `recommended-vehicles-tabs.tsx`
- grep confirms: no `py-6 md:py-8` remains in `hero-search-box.tsx`
- grep confirms: `mt-10` wrapper exists around HeroSearchBox in `page.tsx`
</verification>

<success_criteria>
- HOME-01: Section vertical padding increased so adjacent bottom+top >= 80px for all section pairs. `mt-10` wrapper on HeroSearchBox provides hero-to-search gap of 92px.
- HOME-02: Featured vehicles grid uses `lg:grid-cols-3`. SectionSkeleton also uses `lg:grid-cols-3`.
- HOME-03: HeroSearchBox has `py-10 md:py-12` section padding, `mb-8` title margin, `gap-4` filter gap, `py-6` filter padding.
- HOME-04: PromoBanners grid uses `gap-6` (24px between cards).
</success_criteria>

<output>
After completion, create `.planning/phases/19-homepage-search-spacing/19-01-SUMMARY.md`
</output>
