---
phase: 23-design-system-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/(public)/page.tsx
  - src/features/marketing/components/hero-section.tsx
  - src/features/marketing/components/hero-search-box.tsx
  - src/features/marketing/components/promotion-banner.tsx
  - src/features/marketing/components/sell-my-car-sections.tsx
  - src/features/marketing/components/event-banners.tsx
  - src/features/inventory/components/quote-builder.tsx
  - src/features/admin/components/vehicle-edit-sheet.tsx
  - src/features/vehicles/components/vehicle-search-bar.tsx
  - src/features/vehicles/components/public-vehicle-detail.tsx
  - src/features/pricing/components/residual-value-form.tsx
  - src/features/pricing/components/pricing-calculator.tsx
  - src/features/pricing/components/brand-filter-client.tsx
  - src/features/settings/components/settings-auth-gate.tsx
  - src/features/contracts/components/admin-contract-list.tsx
  - src/components/ui/navigation-menu.tsx
  - src/components/layout/header-search.tsx
  - src/components/layout/floating-cta.tsx
autonomous: true
requirements: [DS-04, DS-05, DS-06, DS-07]

must_haves:
  truths:
    - "Pressing Tab through homepage, search, and contract wizard shows visible focus ring on every focusable element"
    - "Toggling prefers-reduced-motion: reduce in DevTools disables all animations and transitions"
    - "Homepage has exactly one h1 element"
    - "Dark mode token values exist in .dark selector for all brand tokens"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Global prefers-reduced-motion CSS reset, complete dark mode tokens"
      contains: "prefers-reduced-motion"
    - path: "src/app/(public)/page.tsx"
      provides: "Homepage with h1 element"
      contains: "<h1"
  key_links:
    - from: "src/app/globals.css"
      to: "all components"
      via: "@media (prefers-reduced-motion: reduce) global rule"
      pattern: "prefers-reduced-motion"
    - from: "outline-none components"
      to: "focus-visible ring"
      via: "Tailwind focus-visible utilities"
      pattern: "focus-visible:ring"
---

<objective>
Resolve all accessibility gaps (focus-visible, reduced-motion, heading hierarchy) and ensure dark mode token completeness.

Purpose: WCAG AA compliance for keyboard navigation (2.4.7), motion sensitivity (2.3.3), and heading hierarchy (1.3.1). Dark mode tokens enable future v4.0 toggle.
Output: Accessible focus rings on all interactive elements, global reduced-motion support, semantic h1 on homepage, complete dark mode CSS variable definitions.
</objective>

<execution_context>
@/Users/kiyeol/.claude-work/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-design-system-migration/23-CONTEXT.md
@.planning/phases/21-infrastructure-foundation/21-02-SUMMARY.md

<interfaces>
From src/app/globals.css (Phase 21 tokens already defined):
```css
:root {
  --brand-blue: hsl(217 91% 60%);
  --brand-navy: hsl(220 50% 15%);
  --brand-bg: hsl(220 30% 98%);
  --brand-text: hsl(220 30% 10%);
  --text-tertiary: hsl(0 0% 60%);
  --accent-muted: hsl(217 100% 96%);
  --surface-hover: hsl(220 15% 97%);
  --border-subtle: hsl(0 0% 91%);
  --text-caption: hsl(0 0% 48%);
}
.dark {
  --brand-blue: hsl(217 91% 65%);
  --brand-navy: hsl(220 50% 20%);
  /* ... all 9 tokens have dark values */
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add global reduced-motion CSS + homepage h1 + dark mode token audit</name>
  <files>src/app/globals.css, src/app/(public)/page.tsx</files>
  <read_first>
    - src/app/globals.css
    - src/app/(public)/page.tsx
  </read_first>
  <action>
1. In `src/app/globals.css`, add a `@media (prefers-reduced-motion: reduce)` rule inside `@layer base` (after the existing body/html rules):
```css
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
    scroll-behavior: auto !important;
  }
}
```

2. In `src/app/(public)/page.tsx`, add an `<h1>` element. The hero section is the first visual element, so add a visually-hidden h1 at the top of the page component's return JSX (before any other content):
```tsx
<h1 className="sr-only">Navid Auto - 중고차 렌탈 리스</h1>
```
The `sr-only` class makes it invisible visually but available to screen readers and SEO crawlers.

3. Verify the `.dark` selector in `globals.css` has values for ALL 9 brand tokens defined in Phase 21. The tokens to verify exist in `.dark`: `--brand-blue`, `--brand-navy`, `--brand-bg`, `--brand-text`, `--text-tertiary`, `--accent-muted`, `--surface-hover`, `--border-subtle`, `--text-caption`. All 9 are already present per Phase 21 -- confirm no gaps.

Also verify the `@theme inline` block has `--color-*` mappings for all 9 brand tokens. All should already be present. If any are missing, add them.
  </action>
  <verify>
    <automated>cd /Users/kiyeol/development/navid/navid-auto-rental-lease && grep -c 'prefers-reduced-motion' src/app/globals.css && grep -c '<h1' src/app/\(public\)/page.tsx && grep -c 'brand-blue' src/app/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'prefers-reduced-motion' src/app/globals.css` returns the media query rule
    - `grep '<h1' src/app/(public)/page.tsx` returns at least 1 match containing "sr-only"
    - `grep 'animation-duration: 0.01ms' src/app/globals.css` returns a match
    - `grep 'scroll-behavior: auto' src/app/globals.css` returns a match
    - `.dark` selector contains all 9 brand tokens (brand-blue, brand-navy, brand-bg, brand-text, text-tertiary, accent-muted, surface-hover, border-subtle, text-caption)
    - `bun run type-check` passes
  </acceptance_criteria>
  <done>Homepage has exactly one h1 element. Global CSS resets all animation/transition durations to near-zero when user prefers reduced motion. All 9 dark mode brand tokens verified present.</done>
</task>

<task type="auto">
  <name>Task 2: Replace outline-none with focus-visible ring on all 33 unprotected elements</name>
  <files>
    src/features/marketing/components/hero-section.tsx,
    src/features/marketing/components/hero-search-box.tsx,
    src/features/marketing/components/promotion-banner.tsx,
    src/features/marketing/components/sell-my-car-sections.tsx,
    src/features/marketing/components/event-banners.tsx,
    src/features/inventory/components/quote-builder.tsx,
    src/features/admin/components/vehicle-edit-sheet.tsx,
    src/features/vehicles/components/vehicle-search-bar.tsx,
    src/features/vehicles/components/public-vehicle-detail.tsx,
    src/features/pricing/components/residual-value-form.tsx,
    src/features/pricing/components/pricing-calculator.tsx,
    src/features/pricing/components/brand-filter-client.tsx,
    src/features/settings/components/settings-auth-gate.tsx,
    src/features/contracts/components/admin-contract-list.tsx,
    src/components/layout/header-search.tsx,
    src/components/layout/floating-cta.tsx
  </files>
  <read_first>
    - src/features/marketing/components/hero-section.tsx
    - src/features/marketing/components/hero-search-box.tsx
    - src/features/inventory/components/quote-builder.tsx
    - src/features/admin/components/vehicle-edit-sheet.tsx
    - src/features/marketing/components/promotion-banner.tsx
    - src/features/marketing/components/sell-my-car-sections.tsx
    - src/features/marketing/components/event-banners.tsx
    - src/features/vehicles/components/vehicle-search-bar.tsx
    - src/features/vehicles/components/public-vehicle-detail.tsx
    - src/features/pricing/components/residual-value-form.tsx
    - src/features/pricing/components/pricing-calculator.tsx
    - src/features/pricing/components/brand-filter-client.tsx
    - src/features/settings/components/settings-auth-gate.tsx
    - src/features/contracts/components/admin-contract-list.tsx
    - src/components/layout/header-search.tsx
    - src/components/layout/floating-cta.tsx
  </read_first>
  <action>
For each file listed, find every `outline-none` that does NOT already have a corresponding `focus-visible:ring` on the same element. Replace the bare `outline-none` with:
```
outline-none focus-visible:ring-2 focus-visible:ring-brand-blue focus-visible:ring-offset-2
```

The `ring-brand-blue` uses the Tailwind `--color-brand-blue` token mapped in `@theme inline`. The `ring-offset-2` provides spacing between the element and the focus ring.

**Files with multiple violations (fix all in each file):**
- `quote-builder.tsx` -- 6 instances (likely select/input elements)
- `vehicle-edit-sheet.tsx` -- 6 instances (likely form inputs)
- `hero-section.tsx` -- 4 instances (selects, buttons)
- `hero-search-box.tsx` -- 4 instances (search input, selects)
- `promotion-banner.tsx` -- 2 instances
- `residual-value-form.tsx` -- 2 instances

**Files with single violations (1 each):**
- `sell-my-car-sections.tsx`
- `event-banners.tsx`
- `vehicle-search-bar.tsx`
- `public-vehicle-detail.tsx`
- `pricing-calculator.tsx`
- `brand-filter-client.tsx`
- `settings-auth-gate.tsx`
- `admin-contract-list.tsx`
- `header-search.tsx`
- `floating-cta.tsx`

**DO NOT MODIFY these shadcn UI files** -- they already have proper focus-visible handling via the base `outline-ring/50` in globals.css:
- `src/components/ui/button.tsx`
- `src/components/ui/input.tsx`
- `src/components/ui/textarea.tsx`
- `src/components/ui/select.tsx`
- `src/components/ui/checkbox.tsx`
- `src/components/ui/radio-group.tsx`
- `src/components/ui/dialog.tsx`
- `src/components/ui/tabs.tsx`
- `src/components/ui/toggle.tsx`
- `src/components/ui/scroll-area.tsx`
- `src/components/ui/accordion.tsx`
- `src/components/ui/dropdown-menu.tsx`
- `src/components/ui/navigation-menu.tsx`
  </action>
  <verify>
    <automated>cd /Users/kiyeol/development/navid/navid-auto-rental-lease && TOTAL_OUTLINE=$(grep -rn 'outline-none' src/ --include='*.tsx' --include='*.ts' | grep -v node_modules | grep -v '.test.' | wc -l) && WITHOUT_FOCUS=$(grep -rn 'outline-none' src/ --include='*.tsx' --include='*.ts' | grep -v node_modules | grep -v '.test.' | grep -v 'focus-visible' | wc -l) && echo "Total outline-none: $TOTAL_OUTLINE, Without focus-visible: $WITHOUT_FOCUS" && bun run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'outline-none' src/ --include='*.tsx' | grep -v node_modules | grep -v '.test.' | grep -v 'focus-visible' | grep -v 'src/components/ui/'` returns 0 results (all non-shadcn outline-none have focus-visible)
    - All 16 listed files have been modified to include `focus-visible:ring-2 focus-visible:ring-brand-blue`
    - `bun run type-check` passes with no errors
    - `bun run build` succeeds
  </acceptance_criteria>
  <done>All 33 bare outline-none instances outside shadcn UI components now have focus-visible:ring-2 focus-visible:ring-brand-blue focus-visible:ring-offset-2. Keyboard Tab navigation shows visible blue focus ring on every interactive element.</done>
</task>

</tasks>

<verification>
1. `grep -c 'prefers-reduced-motion' src/app/globals.css` returns >= 1
2. `grep -c '<h1' src/app/(public)/page.tsx` returns >= 1
3. Non-shadcn `outline-none` without `focus-visible` count is 0
4. `bun run type-check` passes
5. `bun run build` succeeds
</verification>

<success_criteria>
- Homepage has exactly one h1 element (verifiable via grep)
- All prefers-reduced-motion support is global via CSS reset
- Every non-shadcn outline-none has a focus-visible:ring companion
- Dark mode tokens verified complete (all 9 brand tokens in .dark)
- Build and type-check pass
</success_criteria>

<output>
After completion, create `.planning/phases/23-design-system-migration/23-01-SUMMARY.md`
</output>
