# Sprint Design Spec: HOME + LIST + SELL + USED + Mobile + Header

> Date: 2026-04-16
> Status: Reviewed (critic pass complete)
> Platform identity: 니치 금융 앱 톤 (토스/카카오뱅크 스타일), "계약 승계" 플랫폼

---

## 1. Header / Navigation 재구성

### 1.1 Desktop Header

```
[로고]   매물보기  ·  내 차 등록  ·  이용가이드         [알림] [로그인/회원가입]
```

- **3개 메뉴**: 매물보기(`/list`), 내 차 등록(`/sell`), 이용가이드(`/guide`)
- "중고 리스·렌트" 1차 메뉴에서 제거 — 모든 매물이 이미 리스/렌트/승계이므로 별도 탭 불필요
- `max-w-7xl`로 통일 (header.tsx:19, footer.tsx:24 모두 현재 `max-w-6xl` → `max-w-7xl`로 변경)
- 메뉴 라벨 변경: "매물등록" → "내 차 등록" (인간적 톤)

### 1.2 Mobile Nav (하단 5탭)

```
홈  ·  매물  ·  등록  ·  채팅  ·  MY
```

- 구조 유지, 변경 없음
- "매물" 진입 시 LIST 페이지 상단에 타입 서브탭 노출 (1.4 참조)

### 1.3 Footer

- `max-w-7xl` 통일
- 서비스 링크: 매물보기 / 리스·렌트 안내(`/used`) / 내 차 등록 / 이용가이드
- "중고 리스·렌트" → "리스·렌트 안내"로 라벨 변경

### 1.4 LIST 페이지 타입 서브탭 (신규)

리스트 상단, ResultMeta 위에 타입 필터 탭 추가:

```
[전체] [승계] [리스] [렌트]
```

- 현재 URL의 `type` 파라미터와 연동
- 활성 탭: primary 배경 + 흰색 텍스트
- 비활성: surface 배경 + sub 텍스트
- 탭 선택 시 `?type=TRANSFER|USED_LEASE|USED_RENTAL` 또는 파라미터 제거(전체)
- 모바일: 가로 스크롤 (`overflow-x-auto`, `scrollbar-none`), 각 탭 `min-w-fit shrink-0`

---

## 2. HOME 섹션 재구성

### 2.1 섹션 순서 변경

| 순서 | 섹션 | 변경사항 |
|------|------|----------|
| 1 | **Hero** | 유지 — 헤드라인 + 미리보기 위젯 |
| 2 | **SearchHub** | 유지 — 빠른 검색 |
| 3 | **TrustStripe** | **Hero 직후로 이동** (현재 5번째 → 3번째) |
| 4 | **추천 매물** | 그리드 유지 |
| 5 | **StoryCards** | 유지 |
| 6 | **HowItWorks** | 유지 |
| 7 | **판매 유도 CTA (신규)** | 하단 배너 |

### 2.2 "지금, 이 매물 어때요?" 가로스크롤 제거

- 추천 매물 그리드와 데이터 중복 (동일 listings 사용)
- "지금, 이 매물 어때요?" section JSX 블록(page.tsx 186~245행) 전체 삭제
- 추천 매물 섹션 헤더를 "지금, 이 매물 어때요?"로 변경 (문구 재활용)

### 2.3 판매 유도 CTA 배너 (신규)

HowItWorks 아래, 페이지 최하단에:

```
┌─────────────────────────────────────────────┐
│  리스·렌트 계약, 부담되시나요?               │
│  차용에서 안전하게 승계하세요.               │
│                                             │
│  [내 차 등록하기 →]                          │
└─────────────────────────────────────────────┘
```

- surface 배경 + rounded-2xl
- CTA 버튼: primary 색상, `/sell` 링크

---

## 3. LIST 로딩 + 정렬 드롭다운

### 3.1 정렬 드롭다운 추가

ResultMeta 우측에 정렬 select 배치:

```
총 42개 매물  [승계 ✕]                    [최신순 ▾]
```

정렬 옵션 (URL param `sort` 값):
- 최신순 (기본값, sort 파라미터 없음 → `buildOrderBy` default)
- 월납입금 낮은순 (`sort=price_asc`)
- 월납입금 높은순 (`sort=price_desc`)
- 연식 최신순 (`sort=year_desc`)
- 주행거리 짧은순 (`sort=mileage_asc`)

구현:
- `<select>` 또는 커스텀 드롭다운
- URL searchParam `sort` 연동 (서버사이드 정렬 이미 구현되어 있음, `list/page.tsx:buildOrderBy`)
- ResultMeta 컴포넌트에 sort prop 추가

### 3.2 로딩 개선

현재: `<Suspense fallback={<p>불러오는 중…</p>}>`

변경:
- Suspense fallback을 스켈레톤 카드 그리드로 교체 (pulse 애니메이션)
- 스켈레톤에 `aria-busy="true"` + `aria-label="매물 불러오는 중"` 접근성 속성 추가
- ListingGrid 내부 필터/페이지 변경 시: 그리드 영역에 `opacity-50 pointer-events-none` 오버레이 (서버 응답 대기 표시)

스켈레톤 구조 (lg: 3열 6개, md: 2열 4개, sm: 2열 4개):
```
┌──────────┐ ┌──────────┐ ┌──────────┐
│ ████████ │ │ ████████ │ │ ████████ │
│ ████     │ │ ████     │ │ ████     │
│ ██       │ │ ██       │ │ ██       │
└──────────┘ └──────────┘ └──────────┘
```

---

## 4. SELL 가이드 텍스트 보강

각 Step의 서브텍스트에 소요시간/혜택 문구 추가:

| Step | 현재 제목 | 보강 서브텍스트 |
|------|----------|----------------|
| 0 | 차량번호로 빠르게 조회 | "번호판만 입력하면 30초 만에 차량 정보가 채워져요" |
| 1 | 차종을 확인해주세요 | "자동 조회 결과가 맞는지 확인하고, 틀린 부분만 수정하세요" |
| 2 | 연식과 주행거리를 입력해주세요 | "정확한 정보일수록 매칭 속도가 빨라져요" |
| 3 | 매물 유형을 선택해주세요 | 각 유형 desc 보강 (아래 참조) |
| 4 | 월 납입금을 입력해주세요 | "구매자에게 가장 먼저 보이는 정보예요" (유지) |
| 5 | 잔여 개월과 보증금을 입력해주세요 | "남은 계약 기간이 짧을수록 매칭이 빨라요" |
| 6 | 차량 사진을 올려주세요 | "12장 모두 올리면 안심마크가 자동 부여돼요" (유지) |
| 7 | 차량 설명을 입력해주세요 | "특이사항, 옵션, 승계 사유 등을 적으면 신뢰도가 올라가요" |

Step 3 매물유형 — `LISTING_TYPE_OPTIONS`의 `desc` 필드를 교체:
- 승계: ~~"리스·렌트 계약 승계"~~ → "현재 진행 중인 리스·렌트 계약을 넘기고 싶을 때"
- 중고 리스: ~~"만기 후 중고 리스"~~ → "만기 후 차량을 리스 조건으로 다시 내놓을 때"
- 중고 렌트: ~~"만기 후 중고 렌트"~~ → "만기 후 차량을 렌트 조건으로 다시 내놓을 때"

---

## 5. 중고 리스·렌트 전용 랜딩 페이지 (`/used`)

### 5.1 현재 → 변경

현재: `redirect("/list?type=USED_LEASE")` (단순 리다이렉트)
변경: 전용 랜딩 페이지

### 5.2 페이지 구조

```
┌─────────────────────────────────────────────┐
│ Hero                                         │
│ "중고 리스·렌트, 월 납입금부터 비교하세요"    │
│ "신차 대비 평균 40% 저렴한 월 납입금"         │
│                                              │
│ [리스 매물 보기]  [렌트 매물 보기]            │
├─────────────────────────────────────────────┤
│ 리스 vs 렌트 비교표                          │
│ ┌──────────────┬──────────────┐             │
│ │ 중고 리스     │ 중고 렌트     │             │
│ │ 소유권 선택   │ 반납 원칙     │             │
│ │ 법인 세제혜택 │ 보험·정비 포함│             │
│ │ 장기 계약     │ 단기 유연     │             │
│ └──────────────┴──────────────┘             │
├─────────────────────────────────────────────┤
│ 최신 리스·렌트 매물 (그리드 4~6개)           │
├─────────────────────────────────────────────┤
│ FAQ (리스/렌트 특화)                         │
│ - 중고 리스란?                               │
│ - 중고 렌트란?                               │
│ - 승계와 뭐가 다른가요?                      │
│ - 세제 혜택은?                               │
├─────────────────────────────────────────────┤
│ CTA: "내 리스·렌트 차량 등록하기"            │
└─────────────────────────────────────────────┘
```

### 5.3 데이터 및 구현

- 최신 매물: `type IN (USED_LEASE, USED_RENTAL)`, `status=ACTIVE`, `take 6`
- 서버 컴포넌트로 구현 (SSR)
- FAQ: 하드코딩 (const 배열, 별도 CMS 불필요)
- SEO metadata: `title: "중고 리스·렌트 | 차용"`, `description: "중고 리스와 렌트 매물을 월 납입금으로 비교하세요."`

### 5.4 컴포넌트 구조

`/used` 페이지는 단일 서버 컴포넌트 (`page.tsx`)로 구현. 정적 콘텐츠 위주이므로 별도 분할 불필요.
매물 그리드 부분만 기존 `VehicleCard` 재사용.

---

## 6. 모바일 점검

### 6.1 터치타겟 44px 보장

점검 대상:
- FilterBar 칩 버튼: 현재 `py-1.5` → `min-h-[44px]` 보장
- SearchHub 칩: 현재 `py-1.5` → 패딩 증가
- VehicleCard 내 작은 요소: 카드 전체가 Link이므로 OK
- PaginationBar: `h-9 w-9` (36px) → `h-11 w-11` (44px) 가로·세로 모두 44px 보장
- MobileNav 탭: flex-1 분할로 폭 OK, 높이 `h-16` OK

### 6.2 폰트

- **Pretendard 유지** — Geist는 한글 미지원, 혼용 시 불일치
- 변경 없음

### 6.3 기타

- 모바일에서 SidebarFilters는 이미 `hidden lg:block` — OK
- 가로 스크롤 영역(FilterBar, 추천매물 미니카드): 미니카드 제거 후 FilterBar만 남음

---

## 변경 파일 목록 (예상)

| 파일 | 변경 |
|------|------|
| `src/components/layout/header.tsx` | 메뉴 3개로 축소, max-w-7xl |
| `src/components/layout/footer.tsx` | 링크 업데이트, max-w-7xl |
| `src/app/(public)/page.tsx` | 섹션 순서 변경, 미니카드 제거, CTA 배너 추가 |
| `src/app/(public)/list/page.tsx` | 타입 서브탭 추가 |
| `src/features/listings/components/result-meta.tsx` | 정렬 드롭다운 추가 |
| `src/features/listings/components/listing-grid.tsx` | 스켈레톤 로딩 추가 |
| `src/features/listings/components/listing-skeleton.tsx` | **신규** — 스켈레톤 컴포넌트 |
| `src/features/listings/components/type-tabs.tsx` | **신규** — 타입 서브탭 |
| `src/features/listings/components/sort-select.tsx` | **신규** — 정렬 드롭다운 |
| `src/features/sell/components/sell-wizard.tsx` | 가이드 텍스트 보강 |
| `src/app/(public)/used/page.tsx` | 리다이렉트 → 전용 랜딩 페이지 |
| `src/features/home/sell-cta-banner.tsx` | **신규** — 판매 유도 배너 |
| `src/components/ui/filter-bar.tsx` | 터치타겟 개선 |

---

## 범위 외 (이번 스프린트에서 제외)

- 검색바를 헤더에 넣기 (추후 검토)
- 드롭다운 서브메뉴 (현재 규모에서 불필요)
- 알림 시스템 변경
- 인증 플로우 변경
